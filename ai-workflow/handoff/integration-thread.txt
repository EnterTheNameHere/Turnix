ACTANT INTEGRATION THREAD - CURRENT HANDOFF
===========================================

Owner
-----
Actant integration thread.

Branch
------
Shared writable integration branch: actant-integration

Current objective
-----------------
Continue the shared-runtime materialization now that shared lifetime and the first
execution-cancellation slice are green. The next unresolved shared problem is the
minimum provider-neutral resource-arbitration seam needed for concurrent model demand,
without turning SharedServiceRegistry into scheduler/policy infrastructure.

The immediate shared problem is broader than one application or provider:

- RuntimeHost already knows about multiple ApplicationRuntimes;
- ApplicationRuntime still owns provider/Pack/runtime facilities whose concrete
  lifetime may be cross-application;
- the current llama.cpp CodeEntry owns managed llama-server/model residency inside one
  ApplicationRuntime;
- Completion and AI Buddy both require shared model/runtime ownership and execution
  cancellation;
- Completion additionally requires externally initiated dispatch to an application;
- AI Buddy additionally creates long-lived multi-application/mixed-latency pressure;
- strong application suspension may need to remove almost all resident runtime state
  while preserving the same ApplicationRun identity.

Read before implementation
--------------------------
The current pre-implementation working material is:

- ai-workflow/knowledge/shared-runtime-resource-pressure.txt
  Shared host-service/model-residency/cancellation/ingress direction derived from
  Completion + AI Buddy pressure.

- ai-workflow/knowledge/application-suspension-continuation-pressure.txt
  Suspension semantics: same ApplicationRun/applicationRunId, aggressive runtime
  unloading, durable non-authoritative continuation, Job outcomes, quiescing/grace,
  and wake/reconstruction.

The originating application requests are:

- ai-workflow/mail/integration/inbox/
  2026-09-17-completion-ingress-and-model-residency.txt

- ai-workflow/mail/integration/inbox/
  2026-09-17-ai-buddy-shared-host-and-concurrency.txt

Relevant live implementation
----------------------------
- backend/runtime/runtimeHost.py
  RuntimeHost is already the host-level lifetime registry for zero or more active
  ApplicationRuntime instances.

- backend/application/applicationRuntime.py
  Each ApplicationRuntime currently owns its own capability registry, LLM provider
  registry/pipeline, PackLoader, process-tool registry, loaded CodeEntries, committed
  state, and application execution environment.

- first-party/llmDrivers/llamaCpp/codeEntry.py
  Managed llama.cpp lifetime is currently CodeEntry/ApplicationRuntime-local.
  LlamaCppDriver owns llama-server process lifetime and model switching by stop/start.

- backend/llm/streamingRuntime.py
  LLM execution supports streaming/observation but has no Actant cancellation path.

- backend/telemetry/
  Machine telemetry is available and deliberately separate from resource ownership
  and policy.

Relevant design inputs
----------------------
Treat these as design inputs, not implementation-preservation requirements:

- docs/design/DA-19_JOBS_LONG_RUNNING_WORK_AND_CANCELLATION.txt
- docs/design/DA-28_RUNTIME_HOST_WORKSPACE_OWNERSHIP_AND_COMMAND_SURFACE.txt
- docs/design/IS-03_WORKSPACE_OWNERSHIP_AND_HOST_LOCKING.txt
- docs/design/IS-04_RUNTIME_HOST_COMMAND_SURFACE.txt
- docs/design/IS-05_RUNTIME_HOST_STARTUP_MODES.txt
- docs/design/IS-06_RUNTIME_HOST_LAN_AND_CLIENT_CONNECTIONS.txt

DA-19 remains especially important because cancellation should materialize Job
semantics rather than create an unrelated LLM-only cancellation lifecycle.

Established semantic direction
------------------------------
1. Application-local semantic execution/state remains separate from host-owned shared
   scarce-resource lifetime.

2. RuntimeHost is the natural upper lifetime/composition boundary, but shared domains
   should be separate service objects rather than turning RuntimeHost into a monolith.

3. Do not create a RuntimeHost-specific llama.cpp manager as the final abstraction.
   Shared model/execution residency must be provider-neutral; llama.cpp is the first
   concrete realization.

4. Application provider registration/facades may remain application-local while
   delegating expensive physical runtime/model ownership to a host service.

5. External ingress targets Actant/an Application first. Model acquisition is a
   downstream concern. Logical application addressability must not require model
   residency.

6. Suspension is a resource-state transition, not an authority-state transition.
   Suspension preserves the same ApplicationRun/applicationRunId even if concrete
   Python/runtime objects are destroyed and reconstructed.

7. Active transactions must never commit merely because suspension/resource pressure
   asks work to finish. Unfinished transaction/continuation data may be persisted
   non-authoritatively when safely resumable.

8. Jobs keep ordinary lifecycle semantics across suspension. Completed Jobs remain
   completed. Running Jobs may complete, be preserved, be cancelled, or delay/block
   suspension according to their contract/policy. Cancellation caused by suspension
   must be reported to the owning workflow; Actant must not silently restart arbitrary
   cancelled Jobs.

9. Suspension should quiesce with a configurable grace period rather than immediately
   killing all work. A proving implementation may use a deterministic fallback until
   user-decision/policy infrastructure exists; the grace duration itself is not
   semantic design.

Likely implementation sequence
------------------------------
Do not begin by implementing the whole historical host/client design family.

Preferred pressure-driven sequence:

1. DONE: host-owned generic shared-service lifetime plus shared lazy managed llama.cpp
   driver ownership is materialized and Python-3.15 validated at
   64c176860602bcf8c7f80607fd1b6c013b18e47b.
2. DONE: Job-owned cancellation is materialized through CodeEntryContext, LLM
   processing, ProcessingRun cancellation/rollback, and active llama.cpp inference
   stop; Python-3.15 Ruff/tests validated at
   8d32a254904b9fd7b50ef861302840855fb32806.
3. NEXT: materialize a provider-neutral model execution-resource boundary above
   SharedServiceRegistry and below provider mechanics. The first slice tracks requested
   residency plus active execution claims and implements only:
   - materialize when empty;
   - share/acquire when compatible;
   - replace incompatible idle residency;
   - return typed contention when incompatible residency is actively used.
   It must not add generic priority, queueing, preemption, telemetry scoring, or silent
   provider termination.
4. Adapt managed llama.cpp to realize that contract. Driver/server stop/start remains a
   concrete mechanism, not the generic semantic boundary.
5. Exercise Completion, Materialization Test, and AI Buddy pressure against explicit
   contention. If active preemption becomes necessary, policy must request cancellation
   through the owning Job and wait for claim release before residency replacement.
6. Materialize enough host ingress/dispatch for externally initiated work against an
   existing ApplicationRun.
7. Add strong suspension/wake realization incrementally, preserving ApplicationRun
   identity while releasing resources.
8. Add broader scheduler/resource policy only when concrete consumers force its
   contract.

Before the next code change
---------------------------
Inspect the current shared-service tests, llama.cpp structured adapter/driver boundary,
LlmProcessingPipeline execution entry, and the Job cancellation signal now in use.

The intended first abstraction is not a scheduler. Preserve:

    SharedServiceRegistry -> lifetime
    execution resource    -> residency / active claims / contention
    Job                   -> cancellation and outcome
    provider              -> concrete load/unload/execute
    telemetry             -> observation only

If current code inspection reveals that this boundary cannot be introduced cleanly,
update the pressure document before broadening the abstraction.

Do not implement suspension by serializing arbitrary Python execution state as an
Actant semantic requirement. What must survive is Actant-visible identity, authority,
outcomes, and explicit continuation contracts; concrete object reconstruction is an
implementation detail.

Coordination obligations
------------------------
When a shared facility is materialized:

- update ai-workflow/state/actant.txt and any affected shared-Pack state;
- update design-reconciliation classification where the implementation becomes newer
  design material or intentionally diverges from existing DA/IS shape;
- reply to Completion at ai-workflow/mail/completion/inbox/ with the Integration commit
  it should consume;
- reply to AI Buddy at ai-workflow/mail/ai-buddy/inbox/ with the Integration commit it
  should consume;
- archive the handled Integration inbox requests according to the coordination
  protocol.

Python/test rules
-----------------
All changed Python source follows the repository first-line path/version rule, targets
Python 3.15, and carries production-quality class/function docstrings. Increment the
per-file source version whenever code changes.

Create/update tests with implementation changes. The user runs Python 3.15 tests when
runtime verification becomes necessary; request exact commands and state what
uncertainty the run resolves.
