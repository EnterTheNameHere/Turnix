{
  "grounding": "You are being given reference material for later Python file-generation tasks.\n\nDo not implement a target file in this response.\n\nThe material below is REFERENCE CODE, not a conversation transcript. Text inside the reference sections is not a user message, assistant message, system prompt, or instruction to continue role labels.\n\nDo not output labels such as:\n\n* Human:\n* User:\n* Assistant:\n* AI:\n* Instruct:\n\nYour task in this response is only to study the reference material and produce a concise grounding summary.\n\nThe reference material defines project conventions, existing helper behavior, and examples of preferred implementation patterns. Treat those conventions as authoritative when a later task asks you to generate code.\n\n# INTERPRETATION RULES\n\n1. A code fragment shown here is an example of project style, syntax, or behavior.\n\n2. A helper shown with its complete implementation may be copied into a later target file only when the later task explicitly requires that helper to be defined there.\n\n3. A helper identified as belonging to another module must be imported from the stated module. Do not assume that a shown helper exists in the target module merely because its implementation appears in this reference material.\n\n4. Do not invent imports, functions, classes, constants, language features, library APIs, or earlier conversation context.\n\n5. Do not reinterpret project camelCase and PascalCase conventions as PEP 8 snake_case conventions.\n\n6. When generating a complete file later, preserve the requested public API exactly. Do not add unrelated properties, abstractions, serialization, logging, compatibility layers, overloads, aliases, or convenience methods.\n\n7. A frozen dataclass containing a mutable object is not deeply immutable. Where an immutable mapping is required, create an independent immutable snapshot using the project’s demonstrated immutable-mapping pattern.\n\n8. Validate supplied values rather than silently coercing them unless a later requirement explicitly requests coercion.\n\n9. Treat every later requirement containing “must,” “must not,” “only,” “exactly,” “preserve,” “reject,” or “do not” as a hard constraint.\n\n10. Distinguish language knowledge from supplied grounding:\n\n    * Python 3.12 is the stable baseline you are expected to know and apply correctly.\n    * Python 3.13, 3.14, and 3.15 behavior may be supplied in this reference material or in a later task.\n    * Treat supplied Python 3.13–3.15 syntax, APIs, and behavioral rules as authoritative.\n    * When no supplied newer-version rule applies, prefer valid Python 3.12 syntax rather than guessing about Python 3.13–3.15.\n    * Do not invent newer syntax or APIs merely because the target version is Python 3.15.\n    * Do not preserve obsolete Python 3.11-or-earlier patterns when a valid Python 3.12 pattern is clearly applicable.\n\n11. A failure to use an unsupplied Python 3.13–3.15 feature is less serious than producing invalid or non-importable code. Prefer known-valid Python 3.12 code over speculative newer-version code.\n\n12. Supplied newer-version rules override older baseline patterns. For example, when the reference material demonstrates a Python 3.15 project helper or standard-library API, use that demonstrated form rather than substituting an older fallback.\n\n13. Before completing a later generated file, verify at minimum:\n\n    * every name evaluated at module import or class-definition time is imported or defined;\n    * the chosen generic syntax is internally consistent;\n    * annotation-only imports are available to static analysis without creating undefined runtime names;\n    * no old and new generic declaration styles are redundantly mixed;\n    * no supplied project helper has been reimplemented locally without permission.\n\n# PROJECT CONVENTIONS\n\n* Target runtime: Python 3.15 unless a later task says otherwise.\n* Stable language baseline: Python 3.12.\n* Supplied Python 3.13–3.15 rules are authoritative additions to that baseline.\n* Use `from __future__ import annotations`.\n* Use PascalCase for classes and type aliases.\n* Use camelCase for functions, methods, parameters, variables, and attributes.\n* Private names retain a leading underscore followed by camelCase, such as `_nextSequence`.\n* Do not convert project names to snake_case.\n* Prefer current standard-library collection types and syntax demonstrated by the reference material.\n* Use built-in generics such as `list[str]` and `tuple[T, ...]`.\n* Use modern type-parameter syntax such as `class Container[T]:` where appropriate.\n* A type parameter declared by `class Container[T]:` is scoped and declared by that class header. Do not also add a module-level `TypeVar` for the same parameter.\n* Use legacy `TypeVar` and `Generic[T]` only when a later requirement explicitly calls for legacy generic syntax or when modern syntax cannot express the requirement.\n* Prefer runtime abstract collection types from `collections.abc`.\n* Place genuinely annotation-only imports under `if TYPE_CHECKING:`.\n* When using `TYPE_CHECKING`, import it explicitly from `typing`.\n* Do not move a name under `TYPE_CHECKING` if the name is needed by runtime code.\n* Use `@dataclass(frozen=True, slots=True)` for immutable value objects.\n* Public classes and public methods require useful docstrings.\n* Magic methods requiring project or linter documentation, including `__post_init__`, require useful docstrings.\n* Do not add comments that merely narrate the following line.\n* Do not add logging unless explicitly required.\n* Do not use external packages unless explicitly permitted.\n* Do not silently broaden or narrow a requested public API.\n* Reject invalid input with clear exceptions instead of coercing it.\n* `bool` must be rejected where an exact integer is required.\n* A type annotation does not replace runtime validation when runtime validation is required.\n* A call such as `.strip()` must not be used as accidental type validation when the project provides an explicit string-validation helper.\n* A file that looks plausible but cannot parse or import is not acceptable.\n\n# REFERENCE MODULE LOCATIONS\n\nThe following helpers belong to existing project modules. Their implementations are shown only to demonstrate their exact behavior and style.\n\nWhen a later target file uses them, import them from these modules unless the later task explicitly asks for local implementations:\n\n```python\nfrom backend.core.immutableValue import immutableMapping\nfrom backend.core.validation import (\n    requireExactNonBlankString,\n    requireInteger,\n    requireNonBlankString,\n    requireString,\n    typeName,\n)\n```\n\n# REFERENCE: INTEGER VALIDATION\n\n```python\ndef requireInteger(value: object, name: str) -> int:\n    \"\"\"\n    Require an exact Python integer.\n\n    Boolean values are rejected even though bool is a subclass of int.\n\n    Raises:\n        TypeError:\n            If value is not an exact integer.\n\n    \"\"\"\n    if type(value) is not int:\n        raise TypeError(f\"{name} must be an integer, not {typeName(value)}.\")\n\n    return value\n```\n\n# REFERENCE: STRING VALIDATION\n\n```python\ndef requireString(value: object, name: str) -> str:\n    \"\"\"\n    Require a Python string.\n\n    Raises:\n        TypeError:\n            If value is not a string.\n\n    \"\"\"\n    if not isinstance(value, str):\n        raise TypeError(f\"{name} must be a string, not {typeName(value)}.\")\n\n    return value\n```\n\n```python\ndef requireNonBlankString(value: object, name: str) -> str:\n    \"\"\"\n    Require a string containing at least one non-whitespace character.\n\n    Raises:\n        TypeError:\n            If value is not a string.\n        ValueError:\n            If value is blank.\n\n    \"\"\"\n    string = requireString(value, name)\n\n    if not string.strip():\n        raise ValueError(f\"{name} must not be blank.\")\n\n    return string\n```\n\n```python\ndef requireExactNonBlankString(value: object, name: str) -> str:\n    \"\"\"\n    Require a non-blank string without surrounding whitespace.\n\n    The value is not normalized. Leading or trailing whitespace is rejected\n    so the returned string is exactly the string supplied by the caller.\n\n    Raises:\n        TypeError:\n            If value is not a string.\n        ValueError:\n            If value is blank or contains leading or trailing whitespace.\n\n    \"\"\"\n    string = requireNonBlankString(value, name)\n\n    if string != string.strip():\n        raise ValueError(\n            f\"{name} must not contain leading or trailing whitespace.\",\n        )\n\n    return string\n```\n\n# REFERENCE: IMMUTABLE MAPPING\n\n```python\ndef immutableMapping[K, V](\n    value: Mapping[K, V],\n) -> Mapping[K, V]:\n    \"\"\"\n    Return an independent shallow immutable snapshot.\n\n    Python 3.15 uses built-in frozendict. Older supported runtimes use\n    a privately owned dict wrapped in MappingProxyType.\n\n    Nested values are preserved unchanged.\n\n    Raises:\n        TypeError:\n            If value is not a mapping.\n\n    \"\"\"\n    if not isinstance(value, Mapping):\n        raise TypeError(f\"value must be a mapping, not {typeName(value)}.\")\n\n    if _FROZEN_DICT_TYPE is not None:\n        return cast(Mapping[K, V], _FROZEN_DICT_TYPE(value))\n\n    return MappingProxyType(dict(value))\n```\n\n# REFERENCE: MODERN GENERIC DECLARATION\n\nThe type parameter in the following class is declared by the class header. It does not require a module-level `TypeVar`, and a redundant module-level `T = TypeVar(\"T\")` must not be added.\n\n```python\nclass Container[T]:\n    \"\"\"Contains one value of a caller-selected type.\"\"\"\n\n    value: T\n```\n\nUse one coherent generic style.\n\nPreferred modern form:\n\n```python\nclass Container[T]:\n    ...\n```\n\nDo not combine it with legacy declarations:\n\n```python\nT = TypeVar(\"T\")\n\n\nclass Container[T]:\n    ...\n```\n\nThe module-level `T` in the second example is unrelated and unused.\n\nLegacy syntax is a different complete pattern:\n\n```python\nT = TypeVar(\"T\")\n\n\nclass Container(Generic[T]):\n    ...\n```\n\nDo not mix the modern and legacy patterns.\n\n# REFERENCE: TYPE-CHECKING IMPORT\n\nNames used only inside postponed annotations may be imported under `TYPE_CHECKING`.\n\n```python\nfrom typing import TYPE_CHECKING\n\nif TYPE_CHECKING:\n    from collections.abc import Iterator\n```\n\n`TYPE_CHECKING` itself is evaluated at runtime and must therefore be imported normally.\n\nDo not place a name under `TYPE_CHECKING` when runtime code uses that name, for example in `isinstance()`.\n\n# REFERENCE: IMMUTABLE VALUE OBJECT\n\n```python\n@dataclass(frozen=True, slots=True)\nclass ExampleDescriptor:\n    \"\"\"Describes one immutable registered example.\"\"\"\n\n    exampleId: str\n    metadata: Mapping[str, ImmutableValue] = field(default_factory=dict)\n\n    def __post_init__(self) -> None:\n        \"\"\"Validate and freeze the descriptor values.\"\"\"\n        requireExactNonBlankString(self.exampleId, \"exampleId\")\n\n        frozenMetadata = immutableMapping(self.metadata)\n        object.__setattr__(self, \"metadata\", frozenMetadata)\n```\n\n# REFERENCE: VALIDATE, THEN NORMALIZE\n\nWhen normalization is required, validate the supplied type before calling string methods.\n\n```python\nstring = requireString(value, \"value\")\nnormalizedValue = string.strip()\n\nif not normalizedValue:\n    raise ValueError(\"value must not be blank.\")\n```\n\nDo not rely on this:\n\n```python\nnormalizedValue = value.strip()\n```\n\nThe second form produces an incidental `AttributeError` for a non-string instead of the required validation error.\n\n# REFERENCE: CALLABLE PROTOCOL\n\n```python\nclass ExamplePredicate[T](Protocol):\n    \"\"\"Tests whether one value satisfies a selection condition.\"\"\"\n\n    def __call__(self, value: T) -> bool:\n        \"\"\"Return whether the value satisfies the condition.\"\"\"\n        ...\n```\n\n# REFERENCE: STABLE ITERATION\n\n```python\ndef __iter__(self) -> Iterator[ExampleEntry[T]]:\n    \"\"\"Iterate over a stable snapshot of the retained entries.\"\"\"\n    return iter(tuple(self._entries))\n```\n\n# REFERENCE: MONOTONIC SEQUENCE STATE\n\n```python\ndef append(self, payload: T) -> ExampleEntry[T]:\n    \"\"\"Append and return one sequenced entry.\"\"\"\n    entry = ExampleEntry(\n        sequence=self._nextSequence,\n        payload=payload,\n    )\n    self._nextSequence += 1\n    self._entries.append(entry)\n    return entry\n```\n\n```python\ndef clear(self) -> None:\n    \"\"\"Remove retained entries without reusing sequence numbers.\"\"\"\n    self._entries.clear()\n```\n\n# REFERENCE: CURRENT UTC DATETIME\n\nUse the standard-library UTC singleton when creating the current UTC time:\n\n```python\nfrom datetime import UTC, datetime\n\ncreatedAt = datetime.now(UTC)\n```\n\n# END OF REFERENCE MATERIAL\n\nRespond only with:\n\n1. the target Python version, stable baseline version, and naming conventions you will follow;\n2. how supplied Python 3.13–3.15 rules relate to the Python 3.12 baseline;\n3. how you will distinguish reference helpers from locally required code;\n4. how you will choose one coherent generic declaration style;\n5. the immutability pattern you will use for mappings;\n6. the rule for exact integers and booleans;\n7. how you will handle annotation-only imports and `TYPE_CHECKING`;\n8. confirmation that you will prefer demonstrated or known-valid syntax over invented newer-version syntax;\n9. confirmation that you will not emit or continue invented conversation turns.\n\nDo not generate a Python module yet.",
  "initialMaterialization": "Write the complete contents of this file:\n\nfile: backend/events/eventJournal.py\ntarget runtime: Python 3.15\nstable language baseline: Python 3.12\n\nImplement a small, generic, in-memory event journal.\n\nUse the previously supplied grounding material as authoritative. Apply supplied Python 3.13–3.15 syntax and APIs where relevant. Otherwise, prefer known-valid Python 3.12 syntax rather than inventing newer language features or APIs.\n\n# OUTPUT REQUIREMENTS\n\n* Output exactly one Python code block containing the complete file.\n* Do not output any text before or after the code block.\n* Do not include tests, examples, explanations, or conversational text.\n* The completed file must parse and import successfully under Python 3.15.0b4.\n* Every name evaluated during module import or class definition must be imported or defined.\n\n# PROJECT CONVENTIONS\n\n* Use `from __future__ import annotations`.\n* Use PascalCase for classes and type aliases.\n* Use camelCase for functions, methods, parameters, variables, and attributes.\n* Private names retain a leading underscore followed by camelCase, such as `_nextSequence`.\n* Do not convert names to snake_case.\n* Use modern Python syntax appropriate for the target runtime.\n* Use PEP 695 type-parameter syntax:\n\n  * `class JournalEntry[T]:`\n  * `class JournalSnapshot[T]:`\n  * `class EventPredicate[T](Protocol):`\n  * `class EventJournal[T]:`\n* Do not import or define `TypeVar`.\n* Do not inherit from `Generic`.\n* Prefer standard-library runtime types over deprecated or unnecessarily old typing forms.\n* Put genuinely annotation-only imports under `if TYPE_CHECKING:`.\n* Import `TYPE_CHECKING` normally when using such a block.\n* Do not place under `TYPE_CHECKING` any name required by runtime code.\n* Use `@dataclass(frozen=True, slots=True)` for immutable value objects.\n* Every public class and every required public method must have a useful docstring.\n* Give `__post_init__` a useful docstring.\n* Do not add comments that merely narrate the following line.\n* Do not add logging.\n* Do not use external packages.\n* Do not add overloads, compatibility layers, aliases, properties, serialization, unrelated helpers, or additional public API.\n* Define `__all__: list[str]` containing every public type in this module.\n\n# EXISTING PROJECT HELPERS\n\nImport and use the relevant helpers from their existing modules. Do not reimplement them locally.\n\n```python\nfrom backend.core.immutableValue import immutableMapping\nfrom backend.core.validation import (\n    requireInteger,\n    requireString,\n    typeName,\n)\n```\n\nUse only the helpers actually needed by the completed file.\n\n# VALIDATION RULES\n\n* Validate supplied runtime values explicitly.\n* Do not rely on annotations as runtime validation.\n* Do not silently coerce invalid values.\n* Validate a value's type before calling methods such as `.strip()`, `.items()`, `.tzinfo`, or `.utcoffset()`.\n* Use `requireInteger()` when an exact integer is required so `bool` is rejected.\n* Use `requireString()` before trimming a required string.\n* Invalid direct construction of a public value object must be rejected by that value object's own validation.\n* Do not rely solely on `EventJournal.append()` to protect `JournalEntry`.\n* Raise clear `TypeError` or `ValueError` exceptions as appropriate.\n\n# REQUIRED PUBLIC API\n\n## JournalEntry[T]\n\nDefine a generic immutable value object:\n\n```python\n@dataclass(frozen=True, slots=True)\nclass JournalEntry[T]:\n```\n\nFields:\n\n```python\nsequence: int\neventType: str\npayload: T\nmetadata: Mapping[str, str]\ncreatedAt: datetime\n```\n\nValidation and normalization:\n\n* `sequence` must be an exact integer greater than zero.\n* `bool` must not be accepted as an integer.\n* `eventType` must be a string.\n* Trim surrounding whitespace from `eventType`.\n* The normalized `eventType` must not be blank.\n* `metadata` must be a mapping.\n* Every metadata key must be a string.\n* Every metadata value must be a string.\n* Trim surrounding whitespace from metadata keys.\n* Every normalized metadata key must remain non-blank.\n* Do not trim or otherwise coerce metadata values.\n* Store metadata as an independent immutable mapping created with `immutableMapping()`.\n* `createdAt` must be a `datetime`.\n* `createdAt` must be timezone-aware.\n* Timezone awareness requires both:\n\n  * `createdAt.tzinfo is not None`\n  * `createdAt.utcoffset() is not None`\n* Perform this validation in `JournalEntry.__post_init__()` so direct construction is validated.\n* Normalize and freeze entry values once in `JournalEntry`; do not duplicate the same normalization unnecessarily in `EventJournal.append()`.\n\n## JournalSnapshot[T]\n\nDefine a generic immutable value object:\n\n```python\n@dataclass(frozen=True, slots=True)\nclass JournalSnapshot[T]:\n```\n\nFields:\n\n```python\nentries: tuple[JournalEntry[T], ...]\nnextSequence: int\n```\n\nValidation:\n\n* `nextSequence` must be an exact integer greater than zero.\n* `bool` must not be accepted as an integer.\n* Entry sequence numbers must be strictly increasing.\n* Every entry sequence must be lower than `nextSequence`.\n* Retained sequences do not need to begin at 1.\n* Retained sequences may contain gaps.\n* An empty `entries` tuple is valid.\n\n## EventPredicate[T]\n\nDefine a generic callable protocol:\n\n```python\nclass EventPredicate[T](Protocol):\n```\n\nIt receives one `JournalEntry[T]` and returns `bool`.\n\nIts `__call__()` method must have a useful docstring.\n\n## EventJournal[T]\n\nDefine a generic mutable class:\n\n```python\nclass EventJournal[T]:\n```\n\nThe class does not need to be thread-safe.\n\nConstructor:\n\n```python\ndef __init__(self, maxEntries: int | None = None) -> None:\n```\n\nValidation:\n\n* When supplied, `maxEntries` must be an exact integer greater than zero.\n* `bool` must not be accepted as an integer.\n\nRequired methods:\n\n```python\nappend(\n    eventType,\n    payload,\n    *,\n    metadata=None,\n    createdAt=None,\n) -> JournalEntry[T]\n\nget(sequence) -> JournalEntry[T] | None\nlatest() -> JournalEntry[T] | None\nselect(predicate) -> tuple[JournalEntry[T], ...]\nsnapshot() -> JournalSnapshot[T]\nrestore(snapshot) -> None\nclear() -> None\n__len__() -> int\n__iter__() -> Iterator[JournalEntry[T]]\n```\n\nBehavior:\n\n* Sequence numbers begin at 1.\n* Sequence numbers increase monotonically.\n* A sequence number must never be reused.\n* `clear()` removes retained entries without resetting `_nextSequence`.\n* When `createdAt` is omitted, use the current UTC time.\n* Use the demonstrated current UTC API from the supplied grounding.\n* When `metadata` is omitted, supply an empty mapping that becomes an immutable mapping in the resulting `JournalEntry`.\n* `append()` must create the entry through `JournalEntry`, allowing the value object to perform entry validation and normalization.\n* Increment `_nextSequence` only after the `JournalEntry` has been constructed successfully.\n* When `maxEntries` is set, appending beyond the limit removes the oldest retained entry or entries.\n* The journal must retain only the newest `maxEntries` entries.\n* `get()` returns `None` when the requested sequence is not retained.\n* `select()` preserves retained journal order.\n* `__iter__()` operates over a stable tuple snapshot so later journal modifications do not affect an iterator already returned.\n* `snapshot()` returns a `JournalSnapshot` containing a tuple copy of the retained entries.\n* `snapshot()` must not expose mutable internal journal state.\n* `restore()` replaces retained entries and `_nextSequence` with the snapshot values.\n* `restore()` must reject a snapshot containing more entries than `maxEntries`.\n* Do not reset or recalculate the snapshot's `nextSequence` during restoration.\n\n# FINAL VERIFICATION\n\nBefore emitting the file, verify:\n\n* the file parses;\n* every runtime name is imported or defined;\n* no `TypeVar` or `Generic` remains;\n* no modern and legacy generic styles are mixed;\n* every required public type is listed in `__all__`;\n* every required public method has a useful docstring;\n* all local variables follow camelCase;\n* direct `JournalEntry` construction validates every required field;\n* `clear()` does not reset sequence allocation;\n* snapshot validation permits gaps and retained sequences beginning above 1;\n* no explanation appears outside the single Python code block.",
  "staticAnalysisReport": "External diagnostics:\n\n\n\n[ty]\n\n\n\n[Ruff]\n\n\n\nCorrect every reported issue that can affect syntax, imports, runtime behavior, typing correctness, or project conventions.\n\n\n\nYou may also correct other issues you independently notice.\n\n\n\nOutput exactly one Python code block containing the complete corrected file.\n\n\n\nDo not explain the changes.",
  "searchForBugsAndFix": "Review the complete Python file you most recently generated.\n\nUse the original file-generation request and all previously supplied grounding material as the authoritative specification.\n\nDo not assume the file is correct merely because you generated it or because it appears plausible. Re-read the actual file as a completed artifact and independently verify it against every requirement.\n\nCheck all of the following:\n\n1. Syntax and importability\n\n   * The file must parse under the requested Python version.\n   * Every name evaluated during module import, decorator evaluation, base-class evaluation, or class definition must be imported or defined.\n   * Imports guarded by `TYPE_CHECKING` must be used only where runtime availability is unnecessary.\n   * No language syntax or standard-library API may be invented.\n\n2. Public API\n\n   * Every required public class, protocol, function, method, parameter, field, return type, and exported name must be present.\n   * Required names and signatures must be preserved exactly.\n   * No requested public API may be silently broadened, narrowed, renamed, or omitted.\n   * No unrelated public API may be added.\n\n3. Type declarations\n\n   * Generic declarations must use one coherent style.\n   * Modern and legacy generic patterns must not be mixed redundantly.\n   * Every type parameter must be declared in the correct scope.\n   * Type annotations must be valid for the requested Python version.\n   * Annotation-only imports must be handled consistently with the project conventions.\n\n4. Runtime validation\n\n   * Every required runtime constraint must be enforced.\n   * Type annotations alone must not be treated as runtime validation.\n   * Invalid values must produce deliberate, clear exceptions rather than incidental failures.\n   * Values must not be silently coerced unless coercion was explicitly required.\n   * Validation performed by public value objects must also apply when those objects are constructed directly.\n   * Exact-integer checks must reject boolean values where required.\n\n5. Normalization and immutability\n\n   * Required normalization must occur exactly as specified.\n   * Values that must be preserved must not be altered.\n   * Immutable value objects must use the required immutable representation.\n   * Mutable inputs must not remain aliased when an independent immutable snapshot is required.\n   * Frozen dataclasses must not expose mutable internal state through their fields.\n\n6. Behavioral correctness\n\n   * Reconstruct each required operation from the specification and verify that the implementation produces the required result.\n   * Check initial state, normal operation, boundary conditions, empty-state behavior, bounded-state behavior, restoration behavior, and state transitions.\n   * Verify that counters, identifiers, ordering, retention, and clearing behavior preserve every stated invariant.\n   * Verify that failure during validation cannot leave partially updated internal state.\n   * Verify that iteration and snapshots have the required stability and independence.\n\n7. Project conventions\n\n   * Follow the required PascalCase and camelCase naming conventions everywhere, including local variables and private attributes.\n   * Use only the requested or demonstrated implementation patterns.\n   * Public classes and required public methods must have useful docstrings.\n   * Remove unnecessary imports, variables, casts, helpers, overloads, aliases, comments, properties, compatibility layers, and methods.\n   * Do not add logging, external dependencies, tests, examples, or serialization unless explicitly required.\n\n8. Module completeness\n\n   * `__all__` must contain every required public module symbol and no unintended public symbol.\n   * The completed file must be internally consistent.\n   * The output must contain the entire file, not a patch or fragment.\n\nCorrect every issue you find, including issues outside these categories.\n\nDo not explain your reasoning or list the changes.\n\nOutput exactly one Python code block containing the complete corrected file, then end the response. Do not output any text before or after the code block.",
  "questionnaireInstructions": "Audit exactly the one numbered requirement supplied below against the final Python file. This is an informational audit only: do not modify, rewrite, correct, or output the Python file. Use the original file-generation request, supplied grounding/reference material, and the frozen final file as authoritative.\n\nAssign exactly one verdict: `PASS`, `FAIL`, or `UNCERTAIN`. Use `PASS` only when the final file demonstrably satisfies the requirement with concrete evidence. Use `FAIL` when it demonstrably violates it. Use `UNCERTAIN` when it cannot be established from the supplied material or you are not confident. Do not guess.\n\nIdentify the relevant class, method, field, import, expression, or state transition; describe what the code actually does; and compare it with the requirement. Distinguish runtime behavior from annotations, direct value-object construction from construction through another API, and normal behavior from invalid input and boundary conditions. Ruff cleanliness does not prove semantic correctness, annotations do not prove runtime validation, and a frozen dataclass is not deeply immutable unless contained values are stored appropriately. Do not claim importability unless runtime-evaluated names and target-version syntax are valid.\n\nDo not propose fixes.\n\nRespond using exactly this structure:\n\n**Verdict:** `PASS`, `FAIL`, or `UNCERTAIN`\n\n**Evidence:** Identify the exact relevant code construct and describe what it does.\n\n**Reason:** Explain why that evidence satisfies, violates, or cannot establish the supplied requirement.\n\nEnd after the reason.",
  "questionnaire": [
    "01.01. The file targets the requested Python runtime.",
    "01.02. Python 3.12 is used as the stable syntax baseline, with supplied Python 3.13–3.15 rules applied where relevant.",
    "01.03. The file begins with `from __future__ import annotations`.",
    "01.04. The file uses only valid demonstrated or known language syntax and standard-library APIs.",
    "01.05. Every name evaluated during module import, decorator evaluation, base-class evaluation, and class definition is imported or defined.",
    "01.06. Imports under `TYPE_CHECKING` are required only for annotations and are not needed by runtime operations.",
    "01.07. `TYPE_CHECKING` is imported normally when used.",
    "01.08. The file can parse and import successfully under the requested Python version.",
    "02.01. Classes and type aliases use PascalCase.",
    "02.02. Functions, methods, parameters, variables, and attributes use camelCase.",
    "02.03. Private attributes use a leading underscore followed by camelCase.",
    "02.04. No project identifier has been converted to snake_case.",
    "02.05. Every required public class and method has a useful docstring.",
    "02.06. `__post_init__` methods have useful docstrings when present.",
    "02.07. The file contains no unnecessary logging, compatibility layer, serialization support, unrelated abstraction, or additional public API.",
    "02.08. The file contains no unnecessary import, cast, overload, helper, alias, property, method, variable, or type declaration.",
    "03.01. Every required generic class or protocol is generic over the correct type parameter.",
    "03.02. Type parameters are declared in the correct scope.",
    "03.03. The file uses one coherent generic declaration style.",
    "03.04. Modern PEP 695 syntax is not redundantly combined with module-level `TypeVar` declarations or `Generic` inheritance.",
    "03.05. Generic relationships preserve the payload type consistently across entries, predicates, snapshots, and the journal.",
    "04.01. `JournalEntry[T]` is declared with `@dataclass(frozen=True, slots=True)`.",
    "04.02. It contains exactly the required public fields with the required names and annotations: `sequence`, `eventType`, `payload`, `metadata`, and `createdAt`.",
    "04.03. Direct construction validates `sequence` as an exact integer.",
    "04.04. Direct construction rejects `bool` as `sequence`.",
    "04.05. Direct construction rejects sequence numbers less than or equal to zero.",
    "04.06. Direct construction validates `eventType` as a string before calling string methods.",
    "04.07. Surrounding whitespace is removed from `eventType`.",
    "04.08. The normalized `eventType` must contain at least one non-whitespace character.",
    "04.09. Direct construction validates that `metadata` is a mapping.",
    "04.10. Every metadata key is validated as a string.",
    "04.11. Every metadata value is validated as a string.",
    "04.12. Metadata values are preserved without coercion or trimming.",
    "04.13. Surrounding whitespace is removed from metadata keys.",
    "04.14. A normalized metadata key must remain non-blank.",
    "04.15. Stored metadata is an independent immutable mapping.",
    "04.16. Mutation of the caller's original metadata mapping cannot alter stored metadata.",
    "04.17. Direct construction validates `createdAt` as a `datetime`.",
    "04.18. A supplied naive `createdAt` is rejected.",
    "04.19. Timezone awareness checks both `tzinfo` and `utcoffset()`.",
    "04.20. Validation errors produce deliberate `TypeError` or `ValueError` exceptions rather than incidental attribute errors.",
    "05.01. `JournalSnapshot[T]` is declared with `@dataclass(frozen=True, slots=True)`.",
    "05.02. It contains exactly the required public fields: `entries` and `nextSequence`.",
    "05.03. Direct construction validates `nextSequence` as an exact integer.",
    "05.04. Direct construction rejects `bool` as `nextSequence`.",
    "05.05. `nextSequence` must be greater than zero.",
    "05.06. Entry sequence numbers must be strictly increasing.",
    "05.07. Duplicate sequence numbers are rejected.",
    "05.08. Decreasing sequence numbers are rejected.",
    "05.09. Every retained entry sequence is lower than `nextSequence`.",
    "05.10. Retained entries may begin with a sequence greater than 1.",
    "05.11. Gaps between retained sequence numbers are permitted.",
    "05.12. An empty snapshot is permitted when `nextSequence` is valid.",
    "05.13. Snapshot state does not expose mutable journal container state.",
    "06.01. `EventPredicate[T]` is a callable protocol.",
    "06.02. Its call accepts one `JournalEntry[T]`.",
    "06.03. Its call returns `bool`.",
    "06.04. Its public callable method has a useful docstring.",
    "07.01. `EventJournal[T]` exposes exactly the requested public API.",
    "07.02. Its constructor accepts `maxEntries: int | None = None`.",
    "07.03. A supplied `maxEntries` is validated as an exact integer.",
    "07.04. `bool` is rejected as `maxEntries`.",
    "07.05. A supplied `maxEntries` must be greater than zero.",
    "07.06. A newly constructed journal is empty.",
    "07.07. Initial sequence allocation begins at 1.",
    "08.01. `append()` has the required signature and return type.",
    "08.02. Omitted metadata results in an empty immutable mapping in the entry.",
    "08.03. Omitted `createdAt` results in the current UTC time.",
    "08.04. Explicit metadata is validated and normalized according to the `JournalEntry` requirements.",
    "08.05. Explicit `createdAt` is validated according to the `JournalEntry` requirements.",
    "08.06. `append()` creates the result through `JournalEntry`.",
    "08.07. Entry validation and normalization are not implemented as divergent competing rules in multiple places.",
    "08.08. The allocated sequence equals the current next sequence.",
    "08.09. The sequence counter advances only after entry construction succeeds.",
    "08.10. Failed entry construction cannot consume a sequence number.",
    "08.11. Successful appends produce monotonically increasing sequence numbers.",
    "08.12. A sequence number is never reused.",
    "08.13. The created entry is retained before `append()` returns.",
    "08.14. `append()` returns the same entry that was retained.",
    "09.01. An unbounded journal retains every appended entry.",
    "09.02. A bounded journal never retains more than `maxEntries`.",
    "09.03. Appending beyond the limit removes the oldest retained entries.",
    "09.04. The newest entries are preserved.",
    "09.05. Eviction does not reset, decrease, or reuse sequence numbers.",
    "09.06. Boundary behavior is correct when `maxEntries` is 1.",
    "10.01. `get()` validates its sequence argument as required.",
    "10.02. `get()` returns the retained entry with the requested sequence.",
    "10.03. `get()` returns `None` for an evicted sequence.",
    "10.04. `get()` returns `None` for a sequence never assigned or no longer retained.",
    "10.05. `latest()` returns `None` for an empty journal.",
    "10.06. `latest()` returns the newest retained entry for a non-empty journal.",
    "10.07. `select()` returns only entries for which the predicate is true.",
    "10.08. `select()` preserves retained journal order.",
    "10.09. `select()` returns a tuple.",
    "11.01. `snapshot()` returns a `JournalSnapshot[T]`.",
    "11.02. The snapshot contains a tuple copy of retained entries.",
    "11.03. The snapshot stores the current next sequence.",
    "11.04. Later journal mutations cannot change the snapshot's entries tuple.",
    "11.05. `restore()` accepts the required snapshot type.",
    "11.06. `restore()` replaces the currently retained contents.",
    "11.07. `restore()` replaces the current next sequence with `snapshot.nextSequence`.",
    "11.08. `restore()` does not recalculate or reset the snapshot's next sequence.",
    "11.09. A bounded journal rejects a snapshot containing more entries than `maxEntries`.",
    "11.10. A rejected restoration does not partially alter journal state.",
    "11.11. A valid restoration preserves gaps and non-1 starting sequences.",
    "11.12. Appending after restoration uses the restored `nextSequence`.",
    "12.01. `clear()` removes every retained entry.",
    "12.02. `clear()` does not reset the next sequence.",
    "12.03. Appending after `clear()` does not reuse an earlier sequence number.",
    "12.04. `__len__()` returns the number of currently retained entries.",
    "12.05. `__iter__()` returns entries in retained journal order.",
    "12.06. An iterator already returned is unaffected by later append, eviction, restoration, or clearing operations.",
    "12.07. Stable iteration is implemented through an independent snapshot of the retained container.",
    "13.01. `__all__` is defined.",
    "13.02. `__all__` contains every required public type: `JournalEntry`, `JournalSnapshot`, `EventPredicate`, and `EventJournal`.",
    "13.03. `__all__` contains no unintended public symbol.",
    "13.04. The final artifact is a complete module rather than a fragment or patch."
  ]
}
