TURNIX DV3 AUDIT NOTE
=====================

Source file:
    docs/design/DA-13_DEPENDENCIES_REQUIREMENTS_AND_PACK_REQUESTS.txt

Branch:
    Dv3

Loaded file metadata:
    DOC_REV: 14
    DOC_GIT: 023e3de
    GitHub blob SHA seen by assistant:
        21161debeb81a287cdade2cbb72bbb014d8e2de0

Audit note status:
    Consolidated Dv3 architecture rewrite recommendation.
    Action-focused revision pass for dependency semantics, shared pack-specifier
    meaning, dependency-context PackRequest use, accepted-entry normalization,
    dependency source policy, SaveOnly / GlobalPinned / GlobalNormal separation,
    version and fingerprint enforcement, provider-role boundaries, advisory
    pack relations, ambiguity handling, last-known-good fallback separation,
    trace wording, and cross-document guardrails.

Audit scope:
    This note audits architectural meaning, doctrine, and direction.
    It does not audit formatting unless formatting/structure affects meaning,
    machine interpretation, rule priority, or human usability as reference.

Important excluded source:
    AI_WORKFLOW_BOOTSTRAP.txt was intentionally not read.
    It is part of the workflow system being audited separately and should not
    bias this architecture pass.


SUMMARY
=======

DA-13 is strong and highly Dv3-compatible.

No heavy rewrite is recommended.

DA-13 defines dependency semantics as hard contracts and defines the policy layer
that sits above DA-11 generic resolution mechanics.

DA-13 should preserve the core rule:

    A dependency expresses a hard functional requirement.

    Dependencies are contracts, not preferences.

    Advisory pack relations are not dependencies.

Declaring a dependency does not:

    - load code
    - activate a pack
    - grant visibility
    - grant capabilities
    - define activation order
    - grant runtime access
    - grant capability permission

Failure to satisfy a hard dependency is a hard failure unless some other
accepted policy or user decision explicitly resolves it.

Main watch items:

    1. Provider-role registration:
        Later provider/runtime documents must define deterministic, scoped,
        trace-visible provider registration and selection behavior.

        The phrase "first registered provider wins by default" is acceptable
        only if registration order itself is deterministic and authority-safe.

    2. GlobalNormal root precedence:
        custom > first-party > third-party is correct inside GlobalNormal.

        It must not be applied to SaveOnly, GlobalPinned, copied SavePack
        material, pinned graph identity, or explicit selected graph semantics.

    3. DA-17 decisions:
        Ambiguity decisions and accepted policy input must not silently broaden
        across packs, versions, SavePacks, RuntimeHosts, trust classes, or
        provider roles.

    4. Trace wording:
        Follow the DA-03 cross-document trace wording check.

Rewrite-boundary guardrails:

    DA-13 owns dependency declaration semantics, PackRequest meaning in
    dependency context, shared pack-specifier structure, dependency-specific
    resolution policy above DA-11, deterministic dependency selection policy,
    saved/pinned/global dependency behavior, provider-role requirement semantics,
    advisory pack-relation semantics, ambiguity handling, and required external
    involvement.

    DA-13 should not absorb detailed PackRequest parsing grammar, generic
    Phase-A / Phase-B resolution mechanics, trace schema/storage, activation
    lifecycle, visibility rules beyond dependency gating, PackManager heuristics,
    PackManager load-list generation, capability invocation, capability
    permission, provider runtime registration, Job lifecycle, AppInstance
    lifecycle, RuntimeHost behavior, or concrete activation recovery behavior.

    Those details belong to their subject-owning documents.

Overall recommendation:

    KEEP WITH SMALL PROVIDER-ROLE / REGISTRATION WATCH


CURRENT CLASSIFICATION
======================

Overall classification:

    KEEP WITH SMALL PROVIDER-ROLE / REGISTRATION WATCH

Detailed classification:

    01. Interpretation Basis
        KEEP

    02. Core Concept
        KEEP

    03. Shared Pack-Specifier Model
        KEEP

    04. Declaring Hard Dependencies
        KEEP

    05. PackRequest in Dependency Context
        KEEP

    06. Accepted-Entry Normalization
        KEEP

    07. Hard Dependency Model
        KEEP

    08. Resolution Overview
        KEEP

    09. Dependency Resolution Source Model
        KEEP

    10. Version and Fingerprint Enforcement
        KEEP

    11. GlobalNormal Selection Procedure
        KEEP WITH WATCH
        Highest version and root precedence are correct inside GlobalNormal.
        They must not be applied to SaveOnly, GlobalPinned, copied SavePack
        material, pinned graph identity, or explicit selected graph semantics.

    12. Required Roles and Provided Roles
        KEEP WITH WATCH
        Provider-role declarations must remain dependency/resolution-layer
        semantics, not runtime capability calls. Provider registration and
        selection behavior must be owned by later provider/runtime documents.

    13. Advisory Pack Relations
        KEEP

    14. Ambiguity and Required External Involvement
        KEEP

    15. Failure Conditions
        KEEP

    16. Last-Known-Good Fallback Boundary
        KEEP

    17. Dependency Is Not Activation
        KEEP

    18. Locked Summary
        KEEP


PRESERVATION RULE 1: DEPENDENCIES ARE HARD CONTRACTS
====================================================

Preserve DA-13's core concept.

A dependency expresses a hard functional requirement:

    This pack cannot function correctly unless another pack exists that
    satisfies this request.

Dependencies are contracts, not preferences.

Declaring a dependency does not:

    - load code
    - activate a pack
    - grant visibility
    - grant capabilities
    - define activation order

Failure to satisfy a hard dependency is a hard failure unless some other
accepted policy or user decision explicitly resolves it.

DA-11 defines generic resolution mechanics.

DA-13 defines how dependency resolution chooses and constrains the resolution
context and outcome above those generic mechanics.

Advisory pack relations are not dependencies.

Provider-role requirements are dependency/resolution-layer semantics.

Provider-role requirements are similar in spirit to capabilities because they
depend on a named function or role rather than one concrete implementation.

They are not runtime capability invocation semantics.


PRESERVATION RULE 2: SHARED PACK-SPECIFIER STRUCTURE DOES NOT MEAN SHARED SEMANTICS
===================================================================================

Preserve the shared pack-specifier model.

Manifest fields that refer to packs use one shared pack-specifier structure
unless another authoritative document explicitly states otherwise.

A shared pack specifier may appear as:

    - one single specifier
    - an array of specifiers

The surrounding field defines the meaning of that acceptance.

Shared syntax does not imply shared semantics.

A hard dependency field, advisory field, and role-related field may reuse the
same pack-specifier structure while producing different higher-level meaning.

This prevents the bad model:

    same syntax, therefore same authority

Fingerprint is an optional strengthening of an exact concrete pack form.

Fingerprint is never a vague compatibility hint by itself.


PRESERVATION RULE 3: PACKS FIELD MEANS HARD DEPENDENCIES ONLY
=============================================================

Preserve hard dependency declaration semantics.

DA-13 defines the manifest field:

    packs

as the hard dependency field.

Rules:

    - absence of "packs" means no hard dependencies
    - an empty list means explicitly no hard dependencies
    - every entry under "packs" is a hard dependency meaning
    - list order carries no semantic meaning by itself

DA-13 does not define optional, soft, or advisory dependency semantics for the
"packs" field.

This keeps "packs" strong and unambiguous.


PRESERVATION RULE 4: PACKREQUEST IN DEPENDENCY CONTEXT ADDS DEPENDENCY POLICY
==============================================================================

Preserve the DA-11 / DA-13 split.

Dependencies are expressed using PackRequestRef and normalized into PackRequest
and accepted-entry form.

DA-11 defines PackRequest grammar and generic parsing rules.

DA-13 defines how a PackRequest is interpreted when the requesting relationship
is a dependency.

In dependency context, PackRequest means:

    - resolve one satisfying concrete dependency target
    - obey dependency-source policy
    - obey dependency version and fingerprint rules
    - fail explicitly if no permitted dependency outcome exists

Dependency context does not change PackRequest structure.

Dependency context changes how the surrounding policy layer uses it.


PRESERVATION RULE 5: ACCEPTED-ENTRY NORMALIZATION PRESERVES EXACT-ARTIFACT LOCKS
================================================================================

Preserve accepted-entry normalization.

DA-13 normalizes dependency requests into one or more accepted pack entries.

Each accepted entry contains:

    - canonicalPackId or PackRequest-identifiable pack reference
    - packVersion or version requirement according to the original form
    - optional fingerprint

A dependency is satisfied if at least one normalized accepted entry can be
resolved successfully under the active dependency-resolution source and context.

Name-only shorthand remains allowed where existing field semantics already allow
versionless PackRequestRef.

Fingerprint requires exact concrete version.

Invalid forms:

    - name plus fingerprint with no version
    - name plus range plus fingerprint
    - name plus wildcard plus fingerprint

Multiple accepted entries in the same dependency express one-of semantics at
the accepted-entry level.

This allows:

    - one of several known-good versions of the same pack
    - one of several concrete packs that satisfy the declared dependency intent

Fingerprint is an exact artifact lock, not a vague compatibility hint.


PRESERVATION RULE 6: HARD DEPENDENCY FAILURE DOES NOT SILENTLY DEGRADE
======================================================================

Preserve the hard dependency model.

All dependencies declared through "packs" are hard dependencies.

Consequences:

    - failure to resolve causes activation failure
    - no automatic disabling occurs
    - no silent downgrade occurs
    - no implicit fallback occurs
    - no best-effort substitution occurs

A dependency failure should not quietly turn into "disable the mod and continue"
unless a later explicit policy/user decision layer owns that behavior.


PRESERVATION RULE 7: DEPENDENCY-RESOLUTION SOURCES ARE MUTUALLY EXCLUSIVE
=========================================================================

Preserve the dependency resolution source model.

DA-13 defines three mutually exclusive dependency-resolution sources:

    - SaveOnly
    - GlobalPinned
    - GlobalNormal

Every hard dependency is resolved from exactly one dependency-resolution source.

Dependency-resolution sources are mutually exclusive.

The resolver does not mix these sources silently.

The resolver does not fall back between these sources silently.

Source meanings:

    SaveOnly:
        SavePack copied material only.

    GlobalPinned:
        pinned global identity only.

    GlobalNormal:
        ordinary global registry only.

No silent source mixing.


PRESERVATION RULE 8: SAVEONLY IS SAVEPACK COPIED MATERIAL ONLY
==============================================================

Preserve SaveOnly semantics.

SaveOnly applies when the dependency's packTreeId is listed in SavePack metadata
as copied.

Under SaveOnly:

    - only copied packs inside that SavePack are eligible
    - the global registry is not consulted
    - absence of the copied pack is a hard error

Saved packs under this model:

    - are identified by full identity
      (author + packTreeId + version)
    - are owned by the SavePack
    - are immutable unless explicitly replaced
    - are visible only within that save context

This preserves save-local isolation and prevents global content from silently
repairing missing copied SavePack material.


PRESERVATION RULE 9: GLOBALPINNED IS PINNED GLOBAL IDENTITY ONLY
================================================================

Preserve GlobalPinned semantics.

GlobalPinned applies when the dependency is bound to a pin in SavePack metadata.

Under GlobalPinned:

    - only the pinned concrete identity is eligible
    - resolution occurs in the global registry only
    - a missing pinned identity is a hard error
    - the pin does not imply copying or saving

Pins express explicit historical intent and bypass selector-based candidate
choice.

This preserves reproducibility intent without pretending the pinned pack was
copied into the SavePack.


PRESERVATION RULE 10: GLOBALNORMAL IS ORDINARY GLOBAL REGISTRY ONLY
===================================================================

Preserve GlobalNormal semantics.

GlobalNormal applies in all other dependency cases.

Under GlobalNormal:

    - resolution occurs in the global registry only
    - SavePack content is not consulted
    - selection follows the deterministic GlobalNormal procedure

GlobalNormal does not apply to:

    - SaveOnly copied material
    - GlobalPinned pinned identity
    - copied SavePack material
    - pinned graph identity
    - explicit selected graph semantics


PRESERVATION RULE 11: VERSION AND FINGERPRINT ENFORCEMENT STAY HARD
===================================================================

Preserve version and fingerprint enforcement.

If a semantic-version requirement is present, only candidates allowed by that
requirement remain eligible.

If a concrete version is requested:

    - only that version is eligible
    - mismatch is a hard failure

If a version range is requested:

    - only matching versions are eligible
    - surrounding policy may restrict further

If no semantic-version requirement exists, or if "*" is used:

    - all versions remain eligible

If no eligible versions remain, dependency resolution fails with
VersionMismatchError.

If an accepted entry declares a fingerprint:

    - the resolved dependency must also match that fingerprint
    - fingerprint mismatch is a non-success outcome for that accepted entry

Fingerprint expectation is an exact-artifact lock for that accepted entry.


PRESERVATION RULE 12: GLOBALNORMAL SELECTION IS DETERMINISTIC AND SCOPED
========================================================================

Preserve GlobalNormal selection procedure.

No scoring model exists.

Procedure:

    Step 1 - version handling
        - if a concrete version is specified:
          only that version remains eligible
        - if a range or versionless form is specified:
          the highest semantic version is preferred
        - packs without version metadata are ordered last

    Step 2 - author relationship ordering
        1) exact requested author
        2) same author as the requesting pack
        3) different known author
        4) unknown author

    Step 3 - root precedence
        custom
        first-party
        third-party

    Step 4 - final tie-break
        lexicographic order of:
        "<author>@<packTreeId>@<version>"

Selection rule:

    - the first eligible candidate is selected
    - silent substitution is forbidden

Watch item:

    This is correct for GlobalNormal.

    It must not be applied to SaveOnly or GlobalPinned.

    It must not override SavePack copied material, pinned dependencies, or
    explicit selected graph/activation graph semantics.


PRESERVATION RULE 13: PROVIDER ROLES ARE NOT CAPABILITY CALLS
=============================================================

Preserve requiredRoles / providedRoles separation.

DA-13 defines role requirements separately from hard pack dependencies.

Manifest field for role requirements:

    requiredRoles

Manifest field for role providers:

    providedRoles

A required role means the pack expects one provider of that named role to be
available when the surrounding runtime logic uses it.

requiredRoles is a dependency/resolution-layer declaration rather than a runtime
capability call.

A pack declaring providedRoles says that it can serve as a provider for those
roles.

requiredRoles does not itself force loader reordering or magical
dependency-graph mutation.

PackManager may use role metadata to help users build sensible load lists, but
runtime behavior remains explicit.

Watch item:

    DA-13 says that if several providers declare the same role, runtime/provider
    selection behavior is defined outside this document.

    It also says that by current locked design, the first registered provider
    wins by default, and specific-provider request may be used programmatically.

    This needs later provider/runtime audit.

Reason:

    "first registered provider wins" touches runtime registration order and may
    become nondeterministic if registration order is not itself deterministic,
    trace-visible, and policy-governed.

Recommended future check:

    The provider-registration owner should define:

        - what "registered" means
        - who may register providers
        - where provider registration lives
        - whether registration order is deterministic
        - whether activation order controls registration order
        - whether PackManager load-list order controls registration order
        - whether provider selection is trace-visible
        - whether provider selection grants capability-like power
        - whether specific-provider request bypasses policy or permission


PRESERVATION RULE 14: ADVISORY PACK RELATIONS ARE HINTS
=======================================================

Preserve advisory pack relation semantics.

Advisory fields:

    - supportedPacks
    - conflictingPacks
    - recommendedPacks

They reuse the shared pack-specifier structure.

The surrounding advisory field defines the meaning.

supportedPacks means:

    - this pack is specifically known to be compatible with those packs
    - if they are available, this pack is expected to work with them

conflictingPacks means:

    - if one of those packs is present, issues are known or expected

recommendedPacks means:

    - those packs are good to use together
    - they express useful synergy or ecosystem guidance

Advisory pack relations are hints.

Advisory pack relations are not:

    - hard dependencies
    - hard activation blockers
    - generic resolution constraints
    - loader/runtime warning rules by themselves

Advisory pack relations are for:

    - PackManager
    - UI
    - compatibility presentation
    - helping the user assemble a load list

Advisory pack relations do not participate in dependency graph enforcement or
activation validity.

A shared pack-specifier structure does not upgrade advisory metadata into
dependency semantics.


PRESERVATION RULE 15: AMBIGUITY REQUIRES EXTERNAL INVOLVEMENT
==============================================================

Preserve ambiguity handling.

If deterministic dependency selection cannot produce one unambiguous candidate,
dependency resolution pauses.

Ambiguity is surfaced explicitly.

User decision or accepted policy input is then required.

Once such a decision is made:

    - it is persisted by the surrounding consumer
    - future runs remain deterministic

The resolver does not guess.

DA-17 owns remembered-decision scope and no silent broadening.


PRESERVATION RULE 16: DEPENDENCY FAILURE IS EXPLICIT AND NOT SILENT FALLBACK
============================================================================

Preserve failure conditions.

Dependency resolution fails explicitly if:

    - no candidates remain after hard constraints
    - dependency-resolution source rules are violated
    - a pinned or saved identity is missing
    - ambiguity cannot be resolved
    - no permitted decision path exists

Dependency-resolution failure is:

    - explicit
    - classified
    - observable
    - never silently resolved by fallback


PRESERVATION RULE 17: LAST-KNOWN-GOOD IS NOT DEPENDENCY-RESOLUTION FALLBACK
===========================================================================

Preserve last-known-good fallback boundary.

Turnix stores the last successfully activated graph per SavePack.

Resolution always attempts fresh dependency resolution first.

Last-known-good is not consulted during dependency resolution.

Last-known-good fallback is used only after activation failure.

Fallback procedure:

    1) abort current activation
    2) attempt activation using the stored graph
    3) propagate failure if fallback fails

Correct model:

    Dependency resolution remains strict.

    Activation may later decide to attempt last-known-good graph fallback.

    That fallback is activation/recovery behavior, not dependency success.


PRESERVATION RULE 18: DEPENDENCY IS NOT ACTIVATION
==================================================

Preserve dependency / activation separation.

Declaring or resolving a dependency does not:

    - activate a pack
    - load code
    - grant runtime access

Activation semantics are defined elsewhere.


SOURCE BOUNDARY STATEMENT: DEPENDENCIES ARE CONTRACTS, ADVISORY RELATIONS ARE HINTS
===================================================================================

DA-13 may state the source-level boundary:

    Dependencies are hard functional requirements.

    Dependency declarations are contracts, not preferences.

    Advisory pack relations are hints, not dependencies.

    Shared pack-specifier syntax does not imply shared semantics.

    Dependency resolution uses DA-11 generic mechanics but adds dependency-source
    policy, version and fingerprint enforcement, and dependency-context outcome
    rules above those mechanics.

    Declaring or resolving a dependency does not load code, activate a pack,
    grant visibility, grant capabilities, grant runtime access, define
    activation order, or consult last-known-good fallback during dependency
    resolution.

This source-level boundary is useful because DA-13 owns dependency semantics and
dependency-specific resolution policy.

The detailed ownership map belongs in the guardrail section below.


REWRITE-BOUNDARY GUARDRAILS, NOT SUGGESTED DA-13 CONTENT
=========================================================

This section is not suggested DA-13 wording.

It exists to prevent architectural drift during rewrite when the editor knows
DA-13 but may not have every subject-owning document loaded.

Use these as guardrails only.

Dependency-boundary guardrails:

    Dependency is hard contract, not preference.

    Shared pack-specifier syntax is not shared semantics.

    "packs" means hard dependencies only.

    List order in "packs" carries no semantic meaning by itself.

    Dependency context does not change PackRequest structure.

    Dependency context adds surrounding dependency policy.

    Fingerprint is exact-artifact lock, not compatibility hint.

    Dependency failure does not silently disable, downgrade, substitute, or
    continue.

    SaveOnly is SavePack copied material only.

    GlobalPinned is pinned global identity only.

    GlobalNormal is ordinary global registry only.

    Dependency-resolution sources are mutually exclusive.

    The resolver does not mix dependency-resolution sources silently.

    The resolver does not fall back between dependency-resolution sources
    silently.

    GlobalNormal root precedence does not apply to SaveOnly or GlobalPinned.

    GlobalNormal root precedence does not override copied SavePack material,
    pinned dependencies, or explicit selected graph semantics.

    requiredRoles is dependency/resolution-layer declaration, not runtime
    capability call.

    providedRoles does not itself grant capability permission.

    First-registered-provider behavior is safe only if registration order is
    deterministic, scoped, trace-visible, and authority-safe.

    Advisory pack relations are hints.

    Advisory pack relations are not hard dependencies.

    Advisory pack relations are not hard activation blockers.

    Advisory pack relations are not generic resolution constraints.

    Advisory pack relations are not loader/runtime warning rules by themselves.

    Ambiguity requires external decision.

    The resolver does not guess.

    Dependency failure is explicit and classified.

    Last-known-good fallback is not dependency-resolution fallback.

    Dependency is not activation.

    Dependency resolution does not load code.

    Dependency resolution does not grant runtime access.

    Dependency resolution does not grant capability permission.

Owning-document boundaries:

    DA-03 owns trace substrate, trace retention, trace loss, and trace DAG
    semantics.

    DA-09 owns root discovery boundaries and discovered candidate production.

    DA-10 owns pack identity, hierarchy, visibility, and SavePack visibility.

    DA-11 owns PackRequest parsing grammar and generic Phase-A / Phase-B
    resolution mechanics.

    DA-12 owns importing, module loading, and code-boundary rules.

    DA-14 owns activation lifecycle, activation failure behavior, and
    last-known-good activation/recovery semantics.

    DA-16 owns capability schemas, permission prompts, enforcement mechanics,
    service semantics, capability invocation, and Job-backed capability
    semantics.

    DA-17 owns warning, allow, deny, remember, user decision, remembered policy,
    ambiguity decision scope, and no-silent-broadening semantics.

    DA-19 owns Job lifecycle, timeout, cancellation, progress, result, and
    terminal outcome semantics.

    DA-20 owns AppInstance lifecycle and runtime identity semantics.

    DA-21 owns RPC, channel, and transport semantics.

    DA-23 owns ExecutionAdapter and runtime code execution boundaries.

    DA-24 owns OrchestrationUnit lifecycle and mutation-authority semantics.

    DA-28 owns RuntimeHost workspace ownership and command-surface semantics.

    DA-29 owns PackManager catalog presentation, advisory relation presentation,
    graph preparation, repair, load-list assistance, and PackManager-domain
    trace semantics.

Local rewrite rule:

    DA-13 may name these concepts when needed to protect dependency and advisory
    relation semantics.

    DA-13 should not define their detailed behavior.

    DA-13 should remain the dependency declaration, dependency-context
    PackRequest, accepted-entry normalization, dependency-source policy,
    version/fingerprint enforcement, provider-role requirement, advisory
    relation, ambiguity handling, failure-condition, and LKG boundary document.


WATCH ITEMS
===========

Provider-role registration watch:

    Later provider/runtime documents must define deterministic, scoped,
    trace-visible provider registration and selection behavior.

    The phrase "first registered provider wins by default" is acceptable only if
    registration order itself is deterministic and authority-safe.

    Provider-role behavior must not become capability invocation or permission
    by accident.

GlobalNormal root precedence watch:

    custom > first-party > third-party is correct inside GlobalNormal.

    Keep verifying that this does not apply to SaveOnly, GlobalPinned, copied
    SavePack material, pinned graph identity, or explicit selected graph
    semantics.

DA-17 decision watch:

    Ambiguity decisions and accepted policy input must not silently broaden
    across packs, versions, SavePacks, RuntimeHosts, trust classes, or provider
    roles.

Trace wording watch:

    Follow the DA-03 cross-document trace wording check.

    Dependency trace obligations are dependency-domain emission obligations.

    Trace retention and loss remain governed by DA-03.

PackManager watch:

    DA-29 should use advisory pack relations for presentation and graph
    preparation without turning them into hard dependencies or activation
    blockers.

SavePack/AppInstance watch:

    SavePack/AppInstance documents should define copied, pinned, and selected
    graph behavior consistently with SaveOnly and GlobalPinned.


CURRENT FINAL RECOMMENDATION
============================

Keep DA-13.

No heavy rewrite is recommended.

Main watch items:

    1. Provider-role registration:
        Later provider/runtime documents must define deterministic, scoped,
        trace-visible provider registration and selection behavior.

        The phrase "first registered provider wins by default" is acceptable
        only if registration order itself is deterministic and authority-safe.

    2. GlobalNormal root precedence:
        custom > first-party > third-party is correct inside GlobalNormal.

        Keep verifying that this does not apply to SaveOnly, GlobalPinned,
        copied SavePack material, pinned graph identity, or explicit selected
        graph semantics.

    3. DA-17 decisions:
        Ambiguity decisions and accepted policy input must not silently broaden
        across packs, versions, SavePacks, RuntimeHosts, trust classes, or
        provider roles.

    4. Trace wording:
        Follow the DA-03 cross-document trace wording check.

Preserve:

    - dependencies as hard contracts
    - shared pack-specifier structure not implying shared semantics
    - "packs" field as hard dependencies only
    - PackRequest in dependency context adding dependency policy over DA-11
    - accepted-entry normalization and exact fingerprint locks
    - hard dependency failure not silently degrading
    - mutually exclusive dependency-resolution sources
    - SaveOnly as SavePack copied material only
    - GlobalPinned as pinned global identity only
    - GlobalNormal as ordinary global registry only
    - version and fingerprint enforcement
    - deterministic and scoped GlobalNormal selection
    - provider roles not becoming runtime capability calls
    - advisory pack relations as hints
    - ambiguity requiring external involvement
    - dependency failure as explicit and not silent fallback
    - last-known-good not being dependency-resolution fallback
    - dependency not being activation

Do not expand DA-13 into detailed PackRequest parsing grammar, generic Phase-A /
Phase-B resolution mechanics, trace schema/storage, activation lifecycle,
visibility rules beyond dependency gating, PackManager heuristics, PackManager
load-list generation, capability invocation, capability permission, provider
runtime registration, Job lifecycle, AppInstance lifecycle, RuntimeHost behavior,
or concrete activation recovery behavior.

--- doc-meta ---
DOC_ID: REWRITE-DA-13
DOC_FILE: docs/design/rewrite_recommendations/REWRITE-DA-13_RECOMMENDATION.txt
DOC_REV: 2
DOC_GIT: d70445c
----------------
