TURNIX DV3 AUDIT NOTE
=====================

Source file:
    docs/design/DA-19_JOBS_LONG_RUNNING_WORK_AND_CANCELLATION.txt

Branch:
    Dv3

Loaded file metadata:
    DOC_REV: 1
    DOC_GIT: 7b9fa01
    GitHub blob SHA seen by assistant:
        00ce33a8d0a7812e93346f723558a489cff55c75

Audit note status:
    First-pass Dv3 architecture audit note.

Audit scope:
    This note audits architectural meaning, doctrine, and direction.
    It does not audit formatting unless formatting/structure affects meaning,
    machine interpretation, rule priority, or human usability as reference.

Important excluded source:
    AI_WORKFLOW_BOOTSTRAP.txt was intentionally not read.
    It is part of the workflow system being audited separately and should not
    bias this architecture pass.


SUMMARY
=======

DA-19 is strong and highly Dv3-compatible.

It defines Job as an explicit observable lifecycle object for requested work.

This is the correct Dv3 split:

    Job = observable lifecycle handle for requested work
    OrchestrationUnit = execution-authority unit
    Transaction = mutation staging and commit boundary
    RPC/transport = communication path
    Capability = permissioned service/action surface
    Execution mechanism = implementation primitive

Overall recommendation:

    KEEP

No heavy rewrite is recommended.


CURRENT CLASSIFICATION
======================

Overall classification:

    KEEP

Detailed classification:

    01. Interpretation Basis
        KEEP

    02. Core Concept
        KEEP
        Very strong. Job is observable lifecycle object, not implementation
        primitive.

    03. Job Identity
        KEEP
        Good separation from result payload, OrchestrationUnit, RPC request,
        and execution mechanism.

    04. Job Ownership and Correlation
        KEEP
        Good correlation model without making origin authority.

    05. Relation to Orchestration Units
        KEEP
        Critical DA-24 alignment.

    06. Job Lifecycle State Model
        KEEP
        Strong explicit states and terminal rules.

    07. Result and Error Ownership
        KEEP
        Good distinction between Succeeded, Failed, Cancelled, TimedOut, and
        Superseded.

    08. Progress
        KEEP
        Correct: progress is observational, not authoritative mutation.

    09. Cancellation and Execution Stopping
        KEEP
        Strong distinction between cancellation request, cancellation outcome,
        and execution-stop attempt.

    10. Timeout
        KEEP
        Correct distinction from RPC transport timeout.

    11. Supersession
        KEEP
        Strong and consistent with DA-24.

    12. Late Completion After Terminal Outcome
        KEEP
        Essential async safety rule.

    13. Safe-point Pause and Debugger Interaction
        KEEP
        Good, but depends on DA-18 and DA-08.

    14. Job Trace Reason Identifiers
        KEEP
        Good domain-owned trace reason set.

    15. Relation to RPC and Transport
        KEEP
        Strong separation.

    16. Relation to Capabilities
        KEEP
        Good, but DA-16 must preserve this.

    17. Explicitly Open Questions
        KEEP

    18. Locked Summary
        KEEP


WHAT IS ALREADY NAILED
======================

DA-19 correctly says a Job is an explicit lifecycle object representing
requested work.

A Job is not merely:

    - Promise
    - coroutine
    - callback
    - thread
    - task
    - process
    - model request
    - transport message

Runtime primitives may implement Jobs, but those primitives do not define Job
lifecycle semantics.

A Job identity remains distinct from:

    - RPC request identity
    - OrchestrationUnit identity
    - underlying execution identity
    - result payload identity


CORE TRIAD
==========

DA-19 aligns with DA-24 and DA-04.

Correct triad:

    Job answers:
        What requested work is waiting, running, succeeded, failed, cancelled,
        timed out, superseded, or paused?

    OrchestrationUnit answers:
        What owns or coordinates execution authority?

    Transaction answers:
        What staged mutation may become committed truth?

DA-19 preserves these boundaries.


JOB IS NOT COMMITTED STATE
==========================

DA-19 correctly says a Job does not become authoritative committed state merely
because it exists, runs, or reaches terminal outcome.

Job effects become authoritative only through the documents that own those
effects:

    - state
    - transaction
    - Window
    - capability
    - persistence

A Job result envelope does not become authoritative committed state merely
because the Job succeeded.

This preserves DA-04.


JOB / ORCHESTRATIONUNIT SEPARATION
==================================

DA-19 correctly says:

    Job lifecycle identity and OrchestrationUnit mutation authority remain
    distinct concepts.

    State mutation authority remains constrained by DA-04 even when work is
    represented as a Job.

    A Job terminal outcome does not automatically commit, abort, or roll back
    an OrchestrationUnit.

This exactly satisfies the DA-24 dependency.


LIFECYCLE MODEL
===============

DA-19's primary states are:

    - Pending
    - Running
    - Succeeded
    - Failed
    - Cancelled
    - TimedOut
    - Superseded

Terminal states are:

    - Succeeded
    - Failed
    - Cancelled
    - TimedOut
    - Superseded

A Job reaches exactly one terminal state.

Terminal state is final.

Closed Jobs are not restarted.

Retry is new work unless a later document explicitly defines a retry
relationship.

Retry does not reuse the previous jobId.

This should remain.


PROGRESS
========

Progress is observational lifecycle evidence.

Progress is not authoritative mutation.

Progress updates do not commit authoritative state by themselves.

Progress does not prove eventual success.

This is correct and matches DA-03/DA-04.


CANCELLATION AND EXECUTION STOPPING
===================================

DA-19 correctly separates:

    - cancellation request
    - cancellation outcome
    - execution-stop attempt
    - execution-stop result

Cancellation is explicit.

Cancellation is intentional.

Cancellation is not failure.

A cancellation request does not guarantee immediate cancellation.

Cancelling before execution begins does not require execution-stop because no
underlying execution exists yet.

Cancelling after execution begins may attempt to stop the underlying execution
mechanism, but Job cancellation outcome and execution-stop outcome remain
distinct.

This should remain.


TIMEOUT
=======

Timeout is explicit.

Job timeout is a Job lifecycle concern.

Job timeout is not RPC transport timeout.

RPC transport timeout is not Job timeout.

Timeout uses a monotonic clock.

Timeout policy source is explicit.

TimedOut is distinct from Cancelled and Failed.

This is correct.


SUPERSESSION
============

Superseded is a first-class terminal non-error outcome.

Supersession is:

    - explicit
    - intentional
    - not failure
    - not timeout
    - not ordinary user cancellation, although user action may cause it

Superseded means newer accepted input, newer work, or higher-priority work made
the old Job no longer relevant.

Superseded does not roll back authoritative state already committed.

Superseded does not allow new authoritative mutation from the superseded Job
after terminal transition.

Open transactional staging is resolved by DA-04.

This should remain.


LATE COMPLETION AFTER TERMINAL OUTCOME
======================================

DA-19's late-completion rule is essential.

Late completion after terminal outcome:

    - does not change the Job terminal state
    - does not produce a Job result
    - does not deliver a reply/result
    - may be traced as diagnostic evidence
    - may not commit new authoritative mutation after the terminal transition

This prevents cancelled, timed-out, or superseded work from appearing to
succeed later.


TRACE OWNERSHIP
===============

DA-19 correctly owns Job-domain trace reason identifiers.

DA-03 owns the trace substrate.

DA-19 owns only the Job-domain ownership of trace reasons and required
correlation evidence.

This fits the emerging Dv3 trace split:

    DA-03 = trace substrate
    DA-04 = state/transaction trace obligations
    DA-19 = Job trace reasons
    DA-24 = OrchestrationUnit trace reasons
    DA-28 = RuntimeHost trace reasons


RPC AND TRANSPORT RELATION
==========================

DA-19 correctly says Job lifecycle is distinct from RPC transport lifecycle.

A transport acknowledgement does not prove that a Job exists.

A Job may exist even if the transport that caused it later disconnects.

A transport timeout does not automatically imply Job timeout.

A Job timeout does not automatically imply transport timeout.

A request may be rejected before a Job exists.

A request may be accepted as a Job.

This is exactly the right boundary.


CAPABILITY RELATION
===================

DA-19 correctly says a capability invocation may complete without creating a
Job.

Long-running, cancellable, progress-reporting, timeout-managed, or observable
capability execution may create or attach to a Job.

A Job handle is a lifecycle handle.

A Job handle is not:

    - raw service object
    - direct reference to implementation internals

DA-16 must preserve this.


MAIN WATCH ITEM: RESULT PAYLOAD WORDING
=======================================

DA-19 says a Job result exists only for Succeeded.

This is good and should remain.

Later concrete envelope specs must be careful not to smuggle partial output,
diagnostic metadata, cancellation metadata, timeout metadata, or supersession
metadata into "result" in a way that weakens this rule.

Recommended later convention:

    result = successful result only

    failure/cancellation/timeout/supersession/diagnostic/partial data =
    separate metadata or event structures

No DA-19 rewrite is needed for this now because exact result/error envelopes
remain OPEN.


NO MAJOR DV3 CONFLICT FOUND
===========================

DA-19 does not collapse Job into:

    - RPC request
    - transport lifecycle
    - capability
    - OrchestrationUnit
    - transaction
    - committed state
    - execution mechanism
    - thread/task/process/coroutine
    - result payload

This is exactly what the document should prevent.


CURRENT FINAL RECOMMENDATION
============================

Keep DA-19.

No heavy rewrite is recommended.

Future audits should check:

    - DA-16 Capabilities preserves the rule that not every capability call is a
      Job.
    - IS-01 RPC preserves Job/RPC identity separation.
    - IS-02 WebSocket flow preserves Job/transport lifecycle separation.
    - DA-18 Debugger preserves safe-point pause semantics.
    - DA-08 Window semantics preserve Job outcome / Window finalization
      separation.

--- doc-meta ---
DOC_ID: REWRITE-DA-19
DOC_FILE: docs/design/rewrite_recommendations/REWRITE-DA-19_RECOMMENDATION.txt
DOC_REV: 1
DOC_GIT: 0000000
----------------
