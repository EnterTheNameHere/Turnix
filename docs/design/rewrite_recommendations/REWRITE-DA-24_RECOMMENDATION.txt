TURNIX DV3 AUDIT NOTE
=====================

Source file:
    docs/design/DA-24_ORCHESTRATION_UNITS_AND_MUTATION_AUTHORITY.txt

Branch:
    Dv3

Loaded file metadata:
    DOC_REV: 1
    DOC_GIT: 08f870e
    GitHub blob SHA seen by assistant:
        80a3c2668a291c88af072e2761c7c09b49c2dccc

Audit note status:
    Mode 1 Dv3 architecture rewrite recommendation update.
    Action-focused revision using Checkpoint 06.

Audit scope:
    This note audits architectural meaning, doctrine, and direction.
    It does not audit formatting unless formatting/structure affects meaning,
    machine interpretation, rule priority, or human usability as reference.

Important excluded source:
    AI_WORKFLOW_BOOTSTRAP.txt was intentionally not read.
    It is part of the workflow system being audited separately and should not
    bias this architecture pass.


SUMMARY
=======

DA-24 is Dv3-compatible.

No heavy rewrite is recommended.

DA-24 defines how Turnix identifies and controls execution that may mutate
Turnix-managed state.

Preserve the central rule:

    OrchestrationUnit coordinates mutation authority.

    DA-04 still owns committed-state truth.

Main required clarification:

    Preserve the intended meaning of "execution-authority unit".

    It means an OrchestrationUnit owns or coordinates a mutation flow.

    It does not mean arbitrary contained code receives direct committed-state
    authority.

Current required source-level changes:

    1. Preserve or add a compact OrchestrationUnit authority stop-line.

    2. Add DA-05 / DA-06 / DA-07 stop-lines where OrchestrationUnit mutation
       touches committed values, configuration, migration, persistent I/O,
       SavePack material, or persistence/recovery implications.

    3. Add DA-25 / DA-26 stop-lines where OrchestrationUnit evidence,
       finalized Window evidence, artifacts, exports, comparison, Replay,
       Resume, Retry, or audit workflows are mentioned.

    4. Clarify that OrchestrationUnit outcome does not define Replay, Resume,
       Retry, ComparisonResult, artifact/export authority, or audit workflow
       outcome.

Overall recommendation:

    KEEP WITH PAYLOAD / CONFIGURATION / PERSISTENCE /
    ARTIFACT-WORKFLOW STOP-LINE UPDATE


CURRENT CLASSIFICATION
======================

KEEP WITH PAYLOAD / CONFIGURATION / PERSISTENCE /
ARTIFACT-WORKFLOW STOP-LINE UPDATE

DA-24 should not be rewritten heavily.

Future edits should preserve:

    - OrchestrationUnit coordinates mutation authority
    - OrchestrationUnit authority does not bypass DA-04 commit authority
    - OrchestrationUnit authority does not bypass DA-05 committed-value
      representation
    - OrchestrationUnit authority does not bypass DA-06 configuration semantics
    - OrchestrationUnit authority does not bypass DA-07 persistent-I/O authority
    - adapter-executed code does not directly mutate committed state
    - RuntimeHost operations do not become OrchestrationUnit outcomes by default
    - Job and OrchestrationUnit remain separate
    - Capability and OrchestrationUnit remain separate
    - RPC request and acknowledgement do not automatically become
      OrchestrationUnit lifecycle events
    - Window finalization does not become OrchestrationUnit outcome by default
    - AppInstance lifecycle does not become OrchestrationUnit lifecycle
    - artifact/export authority does not become OrchestrationUnit outcome by
      default
    - ComparisonResult, Replay, Resume, Retry, and audit workflow outcomes do
      not become OrchestrationUnit outcomes by default
    - PackManager-triggered graph or lifecycle operations do not bypass
      OrchestrationUnit authority boundaries
    - single-lane AppInstance orchestration remains the default
    - OrchestrationUnit trace ownership remains domain-specific


ACTUAL DA-24 CHANGE 1: PRESERVE ORCHESTRATIONUNIT AUTHORITY STOP-LINE
=====================================================================

DA-24 should contain a compact source-level boundary statement.

Recommended source-level boundary:

    OrchestrationUnit is a kernel-tracked execution-authority unit that may own
    or coordinate mutation-bearing work.

    OrchestrationUnit authority is scoped authority to coordinate a mutation
    flow.

    It is not general backend authority.

    It is not direct committed-state authority for arbitrary contained code.

    It does not replace DA-04 transaction and commit boundaries.

    It does not replace DA-05 committed-value representation.

    It does not replace DA-06 configuration semantics.

    It does not replace DA-07 persistent-I/O authority.

    It does not replace Job, Capability, RPC, ExecutionAdapter, RuntimeHost,
    Window, AppInstance, debugger, policy, PackManager, artifact/export,
    comparison, Replay, Resume, Retry, or audit workflow semantics.

This source-level boundary belongs in DA-24 because DA-24 owns
OrchestrationUnit lifecycle and mutation-authority semantics.

Detailed ownership mapping belongs in the guardrail section, not in the source
document.


ACTUAL DA-24 CHANGE 2: ADD DA-05 / DA-06 / DA-07 STOP-LINES
===========================================================

DA-24 source touches:

    - mutation-bearing work
    - committed state
    - configuration mutation request processing
    - migration work that changes authoritative state
    - persistent I/O authority
    - persistence and recovery implications
    - SavePack and AppInstance mutation contexts

Checkpoint 06 adds completed ownership boundaries:

    DA-05 owns ValueRef, Chunk, ChunkId, payload identity, decode, integrity,
    and committed-value representation.

    DA-06 owns configuration semantics, configuration storage layout,
    configuration migration, and policy-file placement.

    DA-07 owns persistent I/O authority, root boundaries, Reader / Writer
    providers, storage safety, backup/snapshot behavior, and streaming I/O
    rules.

Recommended source-level boundary:

    OrchestrationUnit may coordinate mutation-bearing work that affects
    committed values, configuration, migration, or persisted material.

    DA-24 defines OrchestrationUnit mutation-authority coordination.

    DA-05 defines committed-value representation.

    DA-06 defines configuration semantics and migration meaning where
    configuration is involved.

    DA-07 defines persistent-I/O authority.

    OrchestrationUnit existence, execution, completion, failure, cancellation,
    supersession, suspension, or trace evidence does not bypass those domains.

Reason:

    DA-24 coordinates mutation authority.

    It does not own committed-value representation, configuration semantics, or
    persistent I/O authority.


ACTUAL DA-24 CHANGE 3: ADD DA-25 / DA-26 STOP-LINES
===================================================

DA-24 source touches retained OrchestrationUnit evidence inside finalized Window
evidence and future persistence/recovery implications.

Checkpoint 06 now defines:

    DA-25 owns WindowManifest, WindowExport, Minimal Audit Bundle, optional
    digest representation, partial export, missing material, and offline
    inspection artifact semantics.

    DA-26 owns ComparisonResult, equality modes, Replay, Resume, Retry, and
    audit workflow semantics.

Recommended source-level boundary:

    OrchestrationUnit evidence may contribute to Window evidence or later audit
    material.

    OrchestrationUnit evidence does not by itself define Window artifact,
    WindowExport, Minimal Audit Bundle, digest, partial export, missing material,
    ComparisonResult, Replay outcome, Resume outcome, Retry outcome, or audit
    workflow outcome.

    DA-25 owns artifact/export semantics.

    DA-26 owns comparison, Replay, Resume, Retry, and audit workflow semantics.

Reason:

    DA-24 owns OrchestrationUnit lifecycle and mutation-authority outcome.

    It should not become the owner of artifact/export or workflow semantics.


PRESERVATION RULE 1: MUTATION AUTHORITY DOES NOT BYPASS COMMIT AUTHORITY
========================================================================

Preserve DA-24's mutation authority model.

Bad model:

    OrchestrationUnit runs work.

    Therefore it can mutate truth directly.

Correct model:

    OrchestrationUnit owns or coordinates mutation flow.

    Mutation still crosses DA-04 transaction and commit boundaries.

OrchestrationUnit may be where decisions happen, where work is sequenced, where
transactions are opened, where tools, models, adapters, PackManager operations,
RuntimeHost operations, capability calls, configuration mutations, migrations,
artifact/export operations, comparison workflows, or audit workflows are
coordinated.

Committed truth still belongs to the DA-04 commit model.

Committed value representation still belongs to DA-05.

Configuration semantics still belong to DA-06.

Persistent I/O authority still belongs to DA-07.


PRESERVATION RULE 2: ADAPTER-EXECUTED CODE DOES NOT DIRECTLY MUTATE COMMITTED STATE
==================================================================================

Preserve ExecutionAdapter / OrchestrationUnit separation.

Adapter-executed mutation seeking Turnix guarantees re-enters through:

    - orchestration
    - capability
    - transaction
    - commit boundaries

Same-language or same-process execution does not bypass committed-state
authority.

Direct in-process calls may carry a mutation request, but they do not become
mutation authority by themselves.

Adapter lifecycle state does not become OrchestrationUnit lifecycle state by
itself.

Adapter failure does not become OrchestrationUnit failure by default.

Adapter execution acceptance does not mean committed-value representation,
configuration, persistence, artifact/export, or workflow success.

DA-23 owns abstract ExecutionAdapter authority boundaries.

DA-27 owns adapter lifecycle, trust, isolation, and adapter failure/disconnect
semantics.


PRESERVATION RULE 3: RUNTIMEHOST OPERATIONS DO NOT BECOME ORCHESTRATION OUTCOMES BY DEFAULT
===========================================================================================

Preserve RuntimeHost / OrchestrationUnit separation.

RuntimeHost command acceptance does not imply OrchestrationUnit completion.

RuntimeHost operation outcome does not by itself define OrchestrationUnit
terminal outcome unless the owning workflow maps those outcomes explicitly.

This prevents CLI, command surface, RuntimeHost operation, transport ACK, entry
surface behavior, or management surface behavior from becoming orchestration
outcome authority.

RuntimeHost operations that seek Turnix mutation guarantees still re-enter
through orchestration, capability, transaction, policy, persistence, and commit
boundaries before mutation becomes authoritative.

DA-28 owns RuntimeHost semantics.


PRESERVATION RULE 4: JOB AND ORCHESTRATIONUNIT REMAIN SEPARATE
==============================================================

Preserve Job / OrchestrationUnit separation.

A Job terminal outcome is not automatically an OrchestrationUnit terminal
outcome.

An OrchestrationUnit terminal outcome is not automatically a Job terminal
outcome.

Correct Dv3 distinction:

    OrchestrationUnit = kernel-tracked execution-authority unit that owns or
    coordinates mutation flow.

    Job = observable lifecycle handle for requested work.

Avoid the weaker wording:

    Job = bounded operation

A Job may represent bounded requested work, but DA-19 defines Job as the
observable lifecycle handle, not the execution primitive or operation itself.

Job retained-result semantics remain owned by DA-19 and related workflow
documents.

OrchestrationUnit outcome does not define retained-result retrieval,
artifact/export material, Replay, Resume, Retry, or audit workflow outcome.


PRESERVATION RULE 5: CAPABILITY AND ORCHESTRATIONUNIT REMAIN SEPARATE
=====================================================================

Preserve Capability / OrchestrationUnit separation.

A capability request is not automatically an OrchestrationUnit.

A capability result is not automatically an OrchestrationUnit outcome.

Capability-mediated work may create, attach to, or participate in Jobs or
OrchestrationUnits only through the owning runtime rules.

Correct Dv3 distinction:

    Capability = named, permissioned, Turnix-mediated operation.

    OrchestrationUnit = kernel-tracked execution-authority unit that owns or
    coordinates mutation flow.

Capability permission does not grant committed-state authority by itself.

Capability permission does not grant persistent-I/O authority by itself.

Capability permission does not grant artifact/export or DA-26 workflow
authority by itself.

Job-backed capability execution remains capability-mediated and does not become
OrchestrationUnit authority by implication.


PRESERVATION RULE 6: RPC DOES NOT DEFINE ORCHESTRATION OUTCOME
==============================================================

Preserve RPC / OrchestrationUnit separation.

RPC request is not automatically an OrchestrationUnit.

RPC acknowledgement is not OrchestrationUnit completion.

RPC request acceptance is not OrchestrationUnit success unless the owning
operation contract explicitly defines acceptance as the complete result.

Transport timeout, disconnect, and cancellation-message handling do not directly
define OrchestrationUnit terminal outcome.

If RPC or transport behavior affects OrchestrationUnit state, the owning
workflow and DA-24 semantics define the OrchestrationUnit outcome.

RPC subscription delivery does not define OrchestrationUnit truth.

DA-21 owns RPC, channel, transport, timeout, disconnect, and cancellation
message semantics.


PRESERVATION RULE 7: WINDOW FINALIZATION DOES NOT DEFINE ORCHESTRATION OUTCOME
==============================================================================

Preserve Window / OrchestrationUnit separation.

Window finalization is not OrchestrationUnit outcome by default.

SafePointWindow creation does not by itself mean OrchestrationUnit Completed.

AbortedWindow creation does not by itself mean OrchestrationUnit Failed,
Cancelled, or Superseded.

OrchestrationUnit terminal outcome does not by itself create SafePointWindow or
AbortedWindow.

Window finalization and OrchestrationUnit outcome may be causally related, but
the relationship must be explicit in the owning workflow.

DA-08 owns Window, ActiveWindow, SafePointWindow, AbortedWindow, journal
evidence, and finalized runtime history semantics.

DA-25 owns Window artifact/export semantics.


PRESERVATION RULE 8: ORCHESTRATION OUTCOMES STAY DISTINCT
=========================================================

Preserve OrchestrationUnit terminal outcomes:

    - Completed
    - Failed
    - Cancelled
    - Superseded

Preserve Superseded as a non-error terminal outcome.

Superseded is:

    - not failure
    - not timeout
    - not rollback by itself
    - does not roll back authoritative state already committed

Open transactional staging is resolved by DA-04.

Job terminal outcomes, adapter outcomes, RuntimeHost outcomes, RPC outcomes,
Window outcomes, artifact/export outcomes, comparison outcomes, Replay
outcomes, Resume outcomes, Retry outcomes, audit workflow outcomes, and policy
decisions do not automatically substitute for these OrchestrationUnit outcomes.

OrchestrationUnit Completed may include committed authoritative mutation, no
authoritative mutation, or discarded transactional staging, but it does not
itself define artifact/export completion or DA-26 workflow completion.


PRESERVATION RULE 9: SINGLE-LANE APPINSTANCE ORCHESTRATION REMAINS DEFAULT
==========================================================================

Preserve the single-lane default.

Turnix uses deterministic single-lane orchestration within one AppInstance
unless an explicit concurrency mode is defined elsewhere.

Turnix does not execute multiple OrchestrationUnits concurrently when those
units may mutate the same AppInstance state.

Awaited model calls, adapter calls, transport calls, RuntimeHost calls,
persistent I/O, export work, audit work, comparison work, or other I/O may
suspend the current unit.

Suspension does not authorize another state-mutating unit to run concurrently
inside the same AppInstance unless explicit concurrency mode permits it.

Reason:

    Async/suspension behavior must not accidentally become concurrency
    authority.

DA-20 owns AppInstance lifecycle and runtime identity.

DA-04 owns committed-state mutation authority.

DA-07 owns persistent I/O authority.

DA-25 owns artifact/export semantics.

DA-26 owns comparison, Replay, Resume, Retry, and audit workflow semantics.

Future concurrency modes must define:

    - mutation conflict boundaries
    - transaction interaction
    - ordering rules
    - trace obligations
    - persistence and recovery implications
    - artifact/export interaction if artifacts or exports are affected
    - Replay / Resume / Retry and audit workflow interaction if workflows are
      affected


PRESERVATION RULE 10: ORCHESTRATIONUNIT TRACE OWNERSHIP STAYS DOMAIN-SPECIFIC
=============================================================================

Preserve OrchestrationUnit trace ownership.

DA-24 owns OrchestrationUnit trace reason identifiers.

DA-03 owns the trace substrate.

DA-24 trace evidence does not replace:

    - DA-04 transaction trace evidence
    - DA-05 payload / committed-value evidence
    - DA-06 configuration or migration evidence
    - DA-07 persistent-I/O evidence
    - DA-08 Window trace/evidence
    - DA-16 capability trace evidence
    - DA-17 decision/policy trace evidence
    - DA-19 Job trace evidence
    - DA-20 AppInstance trace evidence
    - DA-21 RPC trace evidence
    - DA-23 / DA-27 adapter trace evidence
    - DA-25 artifact/export trace evidence
    - DA-26 comparison / Replay / Resume / Retry / audit workflow evidence
    - DA-28 RuntimeHost trace evidence
    - DA-29 PackManager trace evidence

Trace wording should follow the DA-03 cross-document trace wording check.


WATCH ITEM: EXECUTION-AUTHORITY WORDING
=======================================

The phrase "execution-authority unit" is useful, but later readers could misread
it as:

    anything inside the unit has direct authority

Preserve this intended meaning:

    OrchestrationUnit has authority to coordinate or own a mutation flow.

    It does not grant arbitrary contained code direct committed-state authority.

This is especially important for:

    - adapter-executed code
    - same-language Python pack code
    - model/tool callbacks
    - future plugin-like behavior
    - PackManager-triggered graph or lifecycle operations
    - RuntimeHost-triggered operations
    - capability-backed operations
    - artifact/export operations
    - comparison, Replay, Resume, Retry, and audit workflow operations


WATCH ITEM: PAYLOAD / CONFIGURATION / PERSISTENCE AUTHORITY
===========================================================

DA-24 touches committed mutation, configuration mutation, migration work, and
persistence/recovery implications.

Future edits should preserve:

    - DA-05 owns committed-value representation
    - DA-06 owns configuration semantics and configuration migration
    - DA-07 owns persistent-I/O authority
    - OrchestrationUnit coordinates mutation flow but does not own those
      representation, configuration, or persistence meanings
    - direct backend code, adapter-executed code, and RuntimeHost implementation
      code do not bypass these boundaries by entering an OrchestrationUnit


WATCH ITEM: ARTIFACT / EXPORT / WORKFLOW AUTHORITY
==================================================

DA-24 allows OrchestrationUnit evidence to contribute to Window evidence.

Future audit/export/replay documents must preserve:

    - OrchestrationUnit trace evidence may support explanation
    - OrchestrationUnit evidence is not artifact/export authority
    - OrchestrationUnit evidence is not ComparisonResult
    - OrchestrationUnit outcome is not Replay outcome
    - OrchestrationUnit outcome is not Resume outcome
    - OrchestrationUnit outcome is not Retry outcome
    - OrchestrationUnit outcome is not audit workflow outcome
    - DA-25 owns artifact/export semantics
    - DA-26 owns comparison, Replay, Resume, Retry, and audit workflow semantics


WATCH ITEM: DEBUGGER SEMANTICS
==============================

DA-18 should preserve safe-point pause and mutation-boundary semantics.

Debugger pause must not become OrchestrationUnit terminal outcome.

Debugger breakpoint behavior must not grant mutation authority.

Debugger tooling that initiates mutation must still use DA-04 transaction and
commit boundaries.

Debugger snapshot/restore, if later defined, must not bypass DA-24 mutation
authority or DA-26 Replay / Resume / Retry semantics.


REWRITE-BOUNDARY GUARDRAILS, NOT SUGGESTED DA-24 CONTENT
========================================================

This section is not suggested DA-24 wording.

It exists to prevent architectural drift during rewrite when the editor knows
DA-24 but may not have every related document loaded.

Use these as guardrails only.

Orchestration-boundary guardrails:

    OrchestrationUnit coordinates mutation authority.

    OrchestrationUnit is not arbitrary contained-code authority.

    OrchestrationUnit is not general backend authority.

    OrchestrationUnit is not committed-state truth.

    OrchestrationUnit is not committed-value representation authority.

    OrchestrationUnit is not configuration authority.

    OrchestrationUnit is not persistent-I/O authority.

    OrchestrationUnit is not artifact/export authority.

    OrchestrationUnit is not ComparisonResult.

    OrchestrationUnit is not Replay / Resume / Retry workflow authority.

    OrchestrationUnit is not audit workflow authority.

    OrchestrationUnit is not transaction.

    OrchestrationUnit terminal outcome is not automatic transaction commit.

    OrchestrationUnit terminal outcome is not automatic transaction abort.

    OrchestrationUnit terminal outcome is not automatic Job terminal outcome.

    Job terminal outcome is not automatic OrchestrationUnit terminal outcome.

    Capability request is not automatically OrchestrationUnit.

    Capability result is not automatically OrchestrationUnit outcome.

    RPC request is not automatically OrchestrationUnit.

    RPC acknowledgement is not OrchestrationUnit completion.

    RuntimeHost command acceptance is not OrchestrationUnit completion.

    RuntimeHost operation outcome is not OrchestrationUnit outcome by default.

    Adapter execution acceptance is not OrchestrationUnit completion.

    Adapter execution failure is not OrchestrationUnit failure by default.

    Window finalization is not OrchestrationUnit outcome by default.

    SafePointWindow is not OrchestrationUnit completion by default.

    AbortedWindow is not OrchestrationUnit failure by default.

    Artifact/export completion is not OrchestrationUnit completion by default.

    OrchestrationUnit completion is not artifact/export completion by default.

    ComparisonResult is not OrchestrationUnit outcome by default.

    Replay outcome is not OrchestrationUnit outcome by default.

    Resume outcome is not OrchestrationUnit outcome by default.

    Retry outcome is not OrchestrationUnit outcome by default.

    Audit workflow outcome is not OrchestrationUnit outcome by default.

    Browser/frontend state loss is not OrchestrationUnit outcome.

    PackManager-triggered graph or lifecycle operations do not bypass
    OrchestrationUnit authority boundaries.

    Same-language execution does not bypass OrchestrationUnit authority rules.

    Same-process execution does not bypass OrchestrationUnit authority rules.

    Suspension/await does not create concurrency authority.

    Single-lane AppInstance orchestration remains the default unless explicit
    concurrency mode permits otherwise.

Owning-document guardrails:

    DA-03 owns trace substrate, retention, loss, trace DAG, and span semantics.

    DA-04 owns memory, state, transaction, commit mechanics, and committed-state
    authority.

    DA-05 owns ValueRef, Chunk, ChunkId, payload identity, decode, integrity, and
    committed-value representation.

    DA-06 owns configuration semantics, configuration storage layout,
    configuration migration, and policy-file placement.

    DA-07 owns persistent I/O authority, root boundaries, Reader / Writer
    providers, persistence safety, backup/snapshot behavior, and streaming I/O
    rules.

    DA-08 owns Window, ActiveWindow, SafePointWindow, AbortedWindow, journal
    evidence, and finalized runtime history semantics.

    DA-16 owns service, capability, and permission semantics.

    DA-17 owns user decision and remembered policy semantics.

    DA-18 owns debugger pause, breakpoint, and execution-control behavior.

    DA-19 owns Job lifecycle, Job timeout, Job cancellation, Job progress, Job
    terminal outcome, and Job result semantics.

    DA-20 owns AppInstance lifecycle and runtime identity semantics.

    DA-21 owns RPC, channel, transport, timeout, disconnect, and cancellation
    message semantics.

    DA-22 owns UI, View, and frontend runtime semantics.

    DA-23 owns abstract execution adapter boundary semantics.

    DA-25 owns Window artifacts, WindowManifests, WindowExports, Minimal Audit
    Bundles, optional digest representation, partial export, missing material,
    and offline inspection artifact semantics.

    DA-26 owns ComparisonResult, equality modes, Replay, Resume, Retry, and
    audit workflow semantics.

    DA-27 owns adapter lifecycle, trust, isolation, failure, disconnect, and
    adapter-domain trace semantics.

    DA-28 owns RuntimeHost identity, workspace ownership, startup modes, entry
    surfaces, command-surface semantics, and RuntimeHost trace reason
    identifiers.

    DA-29 owns PackManager graph-management semantics.

Local rewrite rule:

    DA-24 may name these concepts when needed to protect OrchestrationUnit
    lifecycle and mutation-authority boundaries.

    DA-24 should not define their detailed behavior.

    DA-24 should remain the OrchestrationUnit lifecycle, mutation-authority,
    outcome, single-lane AppInstance orchestration, and OrchestrationUnit-domain
    trace document.


CURRENT FINAL RECOMMENDATION
============================

Keep DA-24.

No heavy rewrite is recommended.

Recommended future edits:

    - preserve the core rule:
          OrchestrationUnit coordinates mutation authority.
          DA-04 still owns committed-state truth.
    - preserve execution-authority wording as coordination authority, not
      arbitrary contained-code authority
    - add DA-05 / DA-06 / DA-07 stop-lines where OrchestrationUnit mutation
      touches committed values, configuration, migration, persistent I/O,
      SavePack material, or persistence/recovery implications
    - add DA-25 / DA-26 stop-lines where OrchestrationUnit evidence,
      finalized Window evidence, artifacts, exports, comparison, Replay,
      Resume, Retry, or audit workflows are mentioned
    - preserve OrchestrationUnit separation from:
          DA-04 transaction and commit authority
          DA-05 committed-value representation
          DA-06 configuration semantics
          DA-07 persistent-I/O authority
          DA-08 Window finalization
          DA-16 capability semantics
          DA-17 policy and decision semantics
          DA-19 Job lifecycle
          DA-20 AppInstance lifecycle
          DA-21 RPC and transport semantics
          DA-23 / DA-27 adapter boundary and lifecycle semantics
          DA-25 artifact/export semantics
          DA-26 comparison, Replay, Resume, Retry, and audit workflow semantics
          DA-28 RuntimeHost operations and command surfaces
          DA-29 PackManager graph-management semantics

Do not expand DA-24 into detailed memory/state/transaction mechanics, payload
representation, configuration semantics, persistent I/O authority, trace
substrate, Window finalization, capability/permission, user-decision policy,
debugger execution-control, Job lifecycle, AppInstance lifecycle,
RPC/channel/transport, UI/View, ExecutionAdapter boundary, adapter lifecycle,
Window artifact/export semantics, comparison, Replay, Resume, Retry, audit
workflow semantics, RuntimeHost command-surface, or PackManager
graph-management semantics.

--- doc-meta ---
DOC_ID: REWRITE-DA-24
DOC_FILE: docs/design/rewrite_recommendations/REWRITE-DA-24_RECOMMENDATION.txt
DOC_REV: 6
DOC_GIT: 43e6151
----------------
