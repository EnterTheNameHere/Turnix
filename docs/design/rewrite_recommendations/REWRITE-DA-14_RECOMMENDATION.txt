TURNIX DV3 AUDIT NOTE
=====================

Source file:
    docs/design/DA-14_ACTIVATION_LIFECYCLE_AND_EXECUTION_GUARANTEES.txt

Branch:
    Dv3

Loaded file metadata:
    DOC_REV: 16
    DOC_GIT: 35f9a71
    GitHub blob SHA seen by assistant:
        a251dd43566589cb2f81832ec739e9a078b32049

Audit note status:
    Consolidated Dv3 architecture rewrite recommendation.
    Action-focused revision pass for activation lifecycle, PackContext
    authority, activation entrypoints, activation/execution separation,
    immediate execution boundaries, JavaScript entry semantics, activation-
    independent code, activation order, PackManager wording, resolvePack /
    ensureActivated separation, orchestrated execution guarantees, trace wording,
    activation failure, last-known-good boundary, and cross-document guardrails.

Audit scope:
    This note audits architectural meaning, doctrine, and direction.
    It does not audit formatting unless formatting/structure affects meaning,
    machine interpretation, rule priority, or human usability as reference.

Important excluded source:
    AI_WORKFLOW_BOOTSTRAP.txt was intentionally not read.
    It is part of the workflow system being audited separately and should not
    bias this architecture pass.


SUMMARY
=======

DA-14 is strong and highly Dv3-compatible.

No heavy rewrite is recommended.

DA-14 defines activation as the sole mechanism that transitions a pack from
inert metadata to executable participation under Turnix control.

DA-14 should preserve the core rule:

    Activation answers whether Turnix has intentionally brought this pack into
    recognized executable participation under Turnix orchestration.

Activation is a controlled, explicit, one-time state transition.

Before activation:

    - a pack exists only as metadata
    - its code has no execution authority under Turnix
    - adapter loading or native module evaluation does not make it active

After activation:

    - the pack may participate in runtime behavior
    - execution is allowed only through Turnix-controlled orchestration pathways
    - PackContext defines the pack-facing authority context

Activation is not:

    - discovery
    - indexing
    - dependency resolution
    - importing
    - adapter selection
    - execution authorization
    - adapter loading
    - visibility granting
    - capability permission
    - PackManager repair
    - RuntimeHost authority
    - Job terminal outcome
    - committed-state mutation
    - native immediate execution
    - background work guarantee

Main watch items:

    1. Activation order / PackManager wording:
        Treat PackManager as graph-preparation and management surface unless a
        specific activation authority path says otherwise.

        Activation itself remains Turnix-controlled and constrained by DA-14,
        DA-16, DA-17, DA-23, and DA-24.

    2. Last-known-good fallback:
        If defined later, it must remain explicit post-activation-failure
        recovery, not silent dependency-resolution fallback.

    3. Trace wording:
        Follow the DA-03 cross-document trace wording check.

    4. Future entrypoints:
        Future activation entrypoints are allowed only if explicitly defined by
        Turnix and preserving the same PackContext/orchestration boundary.

Rewrite-boundary guardrails:

    DA-14 owns activation lifecycle and execution-guarantee boundaries.

    DA-14 should not absorb detailed generic resolution, dependency source
    selection, importing/module-loading, ExecutionAdapter lifecycle/trust,
    trace schema/storage, capability schema, permission prompting, user decision,
    remembered policy, Job lifecycle, OrchestrationUnit lifecycle,
    RuntimeHost command-surface, PackManager graph-preparation, or concrete
    last-known-good recovery semantics.

    Those details belong to their subject-owning documents.

Overall recommendation:

    KEEP WITH SMALL ACTIVATION-ORDER / PACKMANAGER WORDING WATCH


CURRENT CLASSIFICATION
======================

Overall classification:

    KEEP WITH SMALL ACTIVATION-ORDER / PACKMANAGER WORDING WATCH

Detailed classification:

    01. Interpretation Basis
        KEEP

    02. Core Concept
        KEEP

    03. Lifecycle Phases
        KEEP

    04. What Activated Means
        KEEP

    05. Pack-level Activation
        KEEP

    06. PackContext (ctx) - Execution Authority
        KEEP

    07. Activation Entrypoint
        KEEP
        onLoad(ctx) is currently the sole entrypoint, with future entrypoints
        allowed only if explicitly defined by Turnix.

    08. Activation vs Execution
        KEEP

    09. Immediate Execution and Language Constructs
        KEEP

    10. Runtime Entries - JavaScript Semantics
        KEEP

    11. Entry vs Entries - Execution Rules
        KEEP

    12. Activation-independent Code
        KEEP

    13. Activation Order
        KEEP WITH WATCH
        PackManager wording should be interpreted as graph preparation /
        management unless a specific activation authority path says otherwise.

    14. resolvePack vs ensureActivated
        KEEP

    15. Orchestrated Execution Guarantee
        KEEP
        Trace wording should follow the DA-03 cross-document trace wording
        check.

    16. Activation Failure and NotActivatedError
        KEEP

    17. Activation Result
        KEEP

    18. Explicitly Open Questions
        KEEP

    19. Locked Summary
        KEEP


PRESERVATION RULE 1: ACTIVATION IS THE EXECUTABLE-PARTICIPATION TRANSITION
==========================================================================

Preserve DA-14's core concept.

Activation answers exactly one question:

    Has Turnix intentionally brought this pack into recognized executable
    participation under Turnix orchestration?

Activation is a controlled, explicit, one-time state transition.

Before activation:

    - a pack exists only as metadata
    - its code has no execution authority under Turnix
    - adapter loading or native module evaluation does not make it active

After activation:

    - the pack may participate in runtime behavior
    - execution is allowed only through Turnix-controlled orchestration pathways
    - PackContext defines the pack-facing authority context

This is the core activation boundary.


PRESERVATION RULE 2: ACTIVATION IS NOT EARLIER PIPELINE STAGES
==============================================================

Preserve activation separation from earlier stages.

Activation is not:

    - discovery
    - indexing
    - dependency resolution
    - importing
    - adapter selection
    - execution authorization
    - adapter loading
    - visibility granting
    - capability permission

Earlier phases may occur before activation, but none of them implies
activation.

Preserve the chain:

    discovery finds candidates

    indexing/visibility makes identity and visibility available

    resolution selects candidates

    dependency resolution validates hard requirements

    importing obtains permitted code artifacts

    adapter preparation prepares execution environment

    activation intentionally starts pack participation

No earlier phase becomes activation.


PRESERVATION RULE 3: LIFECYCLE PHASES ARE ORDERED AND EXTERNALLY OBSERVABLE
===========================================================================

Preserve logically ordered, externally observable phases:

    1. Discovery
    2. Indexing
    3. Resolution
    4. Execution adapter preparation where applicable
    5. Activation
    6. Running or Finalized

Activation is the first moment Turnix may intentionally execute pack code with
PackContext authority.

Execution adapter preparation may include:

    - adapter selection
    - execution authorization
    - adapter creation
    - adapter readiness
    - artifact loading

Execution adapter preparation is not activation.

After activation, pack code may execute during Running only through
Turnix-orchestrated execution pathways.

Code that executes outside Turnix orchestration remains outside Turnix
guarantees.


PRESERVATION RULE 4: ACTIVATED MEANS PACKCONTEXT-MEDIATED PARTICIPATION
========================================================================

Preserve the meaning of Activated.

When a pack is activated:

    - Turnix creates a PackContext for that pack instance
    - Turnix dispatches the pack's activation entrypoint
    - the pack may participate in Turnix-managed runtime behavior

When the activated pack's executable artifact belongs to an adapter runtime,
activation dispatch occurs through the appropriate ExecutionAdapter under
DA-23.

Activated packs may register services, register capabilities, subscribe to
Turnix events, allocate runtime resources, and participate in Turnix-managed
orchestration.

Before activation:

    - pack metadata may be resolved
    - the pack may appear in graphs
    - adapter selection may have occurred
    - artifact loading may have occurred
    - pack code must not execute under Turnix authority


PRESERVATION RULE 5: ACTIVATION APPLIES TO PACK AS A WHOLE
==========================================================

Preserve pack-level activation.

Activation applies to the pack as a whole.

Activation does not apply to:

    - individual files
    - individual modules
    - individual imports
    - individual capabilities
    - individual adapter-loaded artifacts

A pack is either activated or not activated.

Partial, staged, deferred, or per-file activation is not defined by DA-14.

Adapter loading does not create partial activation.

The open questions may allow staged or phased activation to be considered later
without silently changing current semantics.


PRESERVATION RULE 6: PACKCONTEXT IS PACK-FACING EXECUTION AUTHORITY
===================================================================

Preserve PackContext authority semantics.

PackContext is the pack-facing execution-authority context.

PackContext is created only by Turnix.

PackContext is bound to exactly one pack instance.

PackContext represents execution authority on behalf of that pack.

PackContext is not:

    - a raw backend service object exposed to pack code
    - unrestricted access to Kernel internals

PackContext operations route through Kernel-owned boundaries.

Code executing with a PackContext:

    - executes under the authority of that pack
    - is subject to that pack's visibility, permissions, and policy
    - remains constrained by capability, transaction, persistence, trace,
      adapter, and lifecycle rules

Representation difference across adapters does not change authority meaning.

In-process PackContext calls do not bypass capability mediation, policy,
transaction, trace, persistence, or lifecycle rules.


PRESERVATION RULE 7: PASSING PACKCONTEXT IS EXPLICIT DELEGATION
===============================================================

Preserve PackContext delegation semantics.

Passing ctx to other code is an explicit delegation of authority.

The delegating pack remains fully responsible for actions performed using its
ctx.

This handles activation-independent library code:

    The provider pack does not become activated or authorized merely because its
    library function is called.

    The caller supplies ctx.

    The caller's authority is used.

    The caller remains responsible.

Passing PackContext into activation-independent code does not change the
provider pack's activation state.


PRESERVATION RULE 8: ACTIVATION ENTRYPOINT IS TURNIX-DEFINED
============================================================

Preserve activation entrypoint semantics.

DA-14 currently says the sole activation entrypoint is:

    onLoad(ctx)

Locked current rules:

    - onLoad(ctx) is called exactly once per pack instance
    - onLoad(ctx) is called only by Turnix
    - ctx is always provided by Turnix
    - failure aborts activation of that pack

No other function, construct, or mechanism activates a pack.

Adapter implementations do not introduce alternate activation entrypoints by
themselves.

Future activation entrypoints, if introduced, follow the same rules and are
explicitly defined by Turnix.

This resolves the DA-12 watch item:

    DA-12's "only onLoad(ctx)" wording is acceptable because DA-14 explicitly
    leaves a controlled future-entrypoint path.


PRESERVATION RULE 9: ACTIVATION AND EXECUTION REMAIN SEPARATE
=============================================================

Preserve activation / execution separation.

Activation:

    - is a one-time state transition
    - establishes PackContext authority
    - occurs currently through onLoad(ctx)
    - may dispatch through an execution adapter when applicable

Execution:

    - occurs only after activation when Turnix-recognized
    - may happen multiple times
    - occurs only when invoked by Turnix orchestration

No execution mechanism activates a pack.

No execution mechanism substitutes for onLoad(ctx).

Adapter execution without PackContext authority is not Turnix-recognized
activated execution.


PRESERVATION RULE 10: IMMEDIATE EXECUTION IS UNSUPPORTED
========================================================

Preserve immediate execution boundaries.

Some languages permit immediate execution constructs:

    - Immediately Invoked Function Expressions
    - module-level executable statements
    - top-level side effects

Turnix does not rely on, endorse, or coordinate immediate execution.

Immediate execution is discouraged in pack code.

Immediate execution does not activate a pack.

Immediate execution runs outside Turnix orchestration.

Code that executes immediately on import:

    - runs without PackContext authority
    - is not tracked by Turnix
    - is outside lifecycle guarantees
    - is not recoverable or abortable by Turnix guarantees

Such execution may technically occur, but its behavior is unsupported.

These rules apply uniformly to:

    - Python
    - JavaScript
    - future supported languages

Same-language execution does not make immediate execution supported.

Adapter loading does not make immediate execution supported.


PRESERVATION RULE 11: JAVASCRIPT ENTRY AND ENTRIES STAY DISTINCT
================================================================

Preserve JavaScript entry semantics.

DA-14 distinguishes:

    runtimes.javascript.entry

from:

    runtimes.javascript.entries

runtimes.javascript.entry:

    - is imported by Turnix during activation
    - provides the module from which onLoad(ctx) is invoked
    - may be loaded through the browser JavaScript adapter
    - successful completion marks JavaScript activation success

runtimes.javascript.entries:

    - are never imported automatically by Turnix as activation entries
    - never receive ctx automatically
    - never have onLoad(ctx) invoked
    - are treated strictly as library modules

This aligns with DA-12.


PRESERVATION RULE 12: ACTIVATION-INDEPENDENT CODE USES CALLER AUTHORITY
=======================================================================

Preserve activation-independent code semantics.

Activation-independent code:

    - must not assume its own pack is activated
    - must not implicitly depend on ctx
    - must not perform Turnix side effects at module top level

Such code may:

    - accept a PackContext explicitly as a parameter
    - execute under the authority of the caller pack that provides that ctx

In that case:

    - the caller pack owns responsibility
    - the authority is that of the caller, not the provider

If code requires activation-specific state of its own pack, that code is
initialized from that pack's activation entry.

Passing PackContext into activation-independent code does not change the
provider pack's activation state.

This gives library-like packs a clean model without turning import into
activation.


PRESERVATION RULE 13: ACTIVATION ORDER MUST NOT BECOME PACKMANAGER AUTHORITY
============================================================================

Preserve activation order boundaries.

DA-14 says activation order is determined exclusively by:

    - PackManager
    - resolved dependencies
    - visibility constraints
    - user policy
    - future category-based ordering if later accepted

Packs must not assume:

    - another pack is activated unless explicitly required
    - services exist unless explicitly requested
    - capabilities are available unless explicitly granted

Adapter readiness does not define activation order.

Adapter loading does not define activation order.

Watch item:

    "PackManager determines activation order" should be read carefully after
    DA-29.

Recommended interpretation:

    PackManager may prepare or propose the activation graph/load order.

    Kernel/activation runtime applies activation under DA-14, DA-16, DA-17,
    DA-23, and DA-24 boundaries.

    PackManager helpfulness must not silently migrate into activation authority
    or hidden graph repair.

This is a wording watch, not a major source conflict.


PRESERVATION RULE 14: RESOLVEPACK AND ENSUREACTIVATED STAY SEPARATE
===================================================================

Preserve resolvePack / ensureActivated separation.

resolvePack(selector):

    - resolves metadata
    - performs visibility checks
    - does not select an adapter for execution
    - does not authorize execution
    - does not load code
    - does not activate packs

ensureActivated(selector):

    - ensures the target pack is activated
    - may trigger activation
    - returns activation metadata only
    - activation metadata is immutable and diagnostic
    - does not return code or symbols
    - may raise NotActivatedError

ensureActivated may cause adapter-mediated activation when the target pack's
executable artifact belongs to an adapter runtime.

ensureActivated does not expose adapter internals.


PRESERVATION RULE 15: TURNIX GUARANTEES ONLY ORCHESTRATED EXECUTION
===================================================================

Preserve the orchestrated execution guarantee.

Turnix guarantees behavior only for execution invoked and tracked through Turnix
orchestration.

Guaranteed execution pathways include:

    - activation entrypoints such as onLoad(ctx)
    - capability calls dispatched by Turnix
    - Turnix event dispatch for handlers registered through Turnix
    - Turnix-managed schedulers if later accepted
    - IoManager-mediated operations, including streaming reads and writes

Execution started outside Turnix orchestration is unsupported.

Unsupported execution may still run, but Turnix provides:

    - no guarantees
    - no tracing guarantees
    - no lifecycle control
    - no cleanup guarantees

Adapter-mediated execution has Turnix guarantees only when invoked and tracked
through Turnix orchestration.

In-process execution has no extra guarantees merely because it runs in the same
process as the backend implementation.

Trace wording should follow the DA-03 cross-document trace wording check.


PRESERVATION RULE 16: ACTIVATION TRACE WORDING FOLLOWS DA-03
============================================================

Preserve activation trace ownership.

DA-14 uses trace-visible and trace evidence wording.

This should follow the DA-03 cross-document trace wording check.

Correct interpretation:

    Activation trace obligations are activation-domain emission obligations
    where applicable.

    DA-03 owns the tracing substrate, retention, and loss semantics.

    Activation trace evidence preserves causality where retained evidence
    exists.

    Trace evidence does not redefine import, dependency resolution, adapter
    lifecycle, capability permission, or Job lifecycle semantics.

DA-14 already explicitly says activation trace evidence does not redefine those
other domains.


PRESERVATION RULE 17: ACTIVATION FAILURE IS VISIBLE AND DOES NOT SILENTLY FALL BACK
===================================================================================

Preserve activation failure behavior.

Activation failure indicates a system-level error such as:

    - unresolved dependency
    - policy violation
    - visibility violation
    - execution authorization failure
    - adapter loading failure
    - activation entry failure

Consequences:

    - activation failure leaves the pack inactive
    - dependent activations do not proceed
    - failure is surfaced to the user
    - silent fallback is forbidden

NotActivatedError must not be swallowed by pack code.

Swallowing NotActivatedError hides orchestration failure.

Activation failure is not a Job terminal outcome merely because the activation
path used adapter-executed code.


PRESERVATION RULE 18: ENSUREACTIVATED RETURNS DIAGNOSTICS, NOT CODE
===================================================================

Preserve activation result semantics.

ensureActivated may return:

    - activation status
    - warnings
    - diagnostics

ensureActivated does not return:

    - executable code
    - module namespaces
    - runtime symbols
    - adapter internals

This keeps ensureActivated separate from import and adapter internals.


PRESERVATION RULE 19: LAST-KNOWN-GOOD IS POST-ACTIVATION-FAILURE RECOVERY ONLY
=============================================================================

Preserve the last-known-good boundary.

DA-13 says last-known-good fallback is used only after activation failure.

DA-14 does not define last-known-good fallback behavior here.

That is acceptable because DA-14 says activation failure surfaces and silent
fallback is forbidden.

If last-known-good activation fallback is defined elsewhere, it must remain:

    - explicit
    - trace-visible
    - policy-bound
    - post-activation-failure recovery

It must not turn dependency resolution failure into silent success.


SOURCE BOUNDARY STATEMENT: ACTIVATION IS CONTROLLED EXECUTABLE PARTICIPATION
============================================================================

DA-14 may state the source-level boundary:

    Activation is the sole mechanism that transitions a pack from inert metadata
    to recognized executable participation under Turnix orchestration.

    Activation is not discovery, indexing, dependency resolution, importing,
    adapter selection, execution authorization, adapter loading, visibility
    granting, capability permission, PackManager repair, RuntimeHost authority,
    Job terminal outcome, committed-state mutation, native immediate execution,
    or background work guarantee.

    Adapter loading, native module evaluation, imported module evaluation,
    immediate execution, and PackManager graph preparation do not make a pack
    active.

    A pack is active only when Turnix creates PackContext for that pack instance
    and dispatches the Turnix-defined activation entrypoint under the activation
    lifecycle rules.

This source-level boundary is useful because DA-14 owns activation lifecycle and
execution-guarantee semantics.

The detailed ownership map belongs in the guardrail section below.


REWRITE-BOUNDARY GUARDRAILS, NOT SUGGESTED DA-14 CONTENT
=========================================================

This section is not suggested DA-14 wording.

It exists to prevent architectural drift during rewrite when the editor knows
DA-14 but may not have every subject-owning document loaded.

Use these as guardrails only.

Activation-boundary guardrails:

    Activation is executable participation, not metadata existence.

    Activation is not discovery.

    Activation is not indexing.

    Activation is not dependency resolution.

    Activation is not importing.

    Activation is not adapter selection.

    Activation is not execution authorization.

    Activation is not adapter loading.

    Activation is not visibility granting.

    Activation is not capability permission.

    Activation is not PackManager repair.

    Activation is not RuntimeHost authority.

    Activation is not Job terminal outcome.

    Activation is not OrchestrationUnit terminal outcome.

    Activation is not committed-state mutation.

    Activation is not native immediate execution.

    Activation is not background work guarantee.

    Pack activation is pack-level, not per-file.

    Pack activation is pack-level, not per-module.

    Pack activation is pack-level, not per-import.

    Pack activation is pack-level, not per-capability.

    Adapter loading does not create partial activation.

    Module evaluation does not activate a pack.

    Import-time side effects do not activate a pack.

    Immediate execution is outside Turnix guarantees.

    PackContext is not raw backend access.

    PackContext is not unrestricted Kernel access.

    PackContext operations route through Kernel-owned boundaries.

    Passing PackContext is explicit delegation.

    Activation-independent library code uses caller authority when caller
    supplies ctx.

    onLoad(ctx) is current activation entrypoint, not native import symbol
    access.

    Future activation entrypoints must be explicitly defined by Turnix.

    Runtime entries other than activation entry are library modules, not
    activation targets.

    PackManager may prepare or propose activation graph/load order, but does not
    become activation authority by helpfulness.

    ensureActivated returns activation metadata/diagnostics, not executable code
    or symbols.

    NotActivatedError is orchestration failure and must not be swallowed.

    Last-known-good fallback is not dependency-resolution fallback.

Owning-document boundaries:

    DA-03 owns trace schema, trace DAG semantics, trace storage, trace retention,
    and trace loss.

    DA-11 owns generic resolution mechanics.

    DA-12 owns importing, module loading, code-boundary rules, turnixImport, and
    native import limits.

    DA-13 owns dependency semantics, dependency source selection, and the
    last-known-good dependency-resolution boundary.

    DA-16 owns service, capability, permission, capability registration,
    capability invocation, and Job-backed capability semantics.

    DA-17 owns user decision, remembered policy, policy violation, and
    remembered-decision scope.

    DA-19 owns Job lifecycle, timeout, cancellation, progress, result, and
    terminal outcome semantics.

    DA-21 owns RPC, channel, and transport semantics.

    DA-23 owns ExecutionAdapter boundary semantics, adapter-mediated execution,
    same-language execution boundaries, and adapter lifecycle/trust details
    where delegated to DA-27.

    DA-24 owns OrchestrationUnit lifecycle and mutation-authority semantics.

    DA-28 owns RuntimeHost workspace ownership, entry surfaces, and command
    surfaces.

    DA-29 owns PackManager graph-preparation, graph management, repair,
    advisory presentation, and PackManager-domain trace semantics.

Local rewrite rule:

    DA-14 may name these concepts when needed to protect activation lifecycle
    and execution-guarantee boundaries.

    DA-14 should not define their detailed behavior.

    DA-14 should remain the activation lifecycle, PackContext authority,
    activation entrypoint, activation/execution separation, immediate execution,
    orchestrated execution, activation failure, activation result, and
    activation-domain trace document.


WATCH ITEMS
===========

Activation order / PackManager wording watch:

    Treat PackManager as graph-preparation and management surface unless a
    specific activation authority path says otherwise.

    Activation itself remains Turnix-controlled and constrained by DA-14, DA-16,
    DA-17, DA-23, and DA-24.

Last-known-good fallback watch:

    If defined later, it must remain explicit post-activation-failure recovery,
    not silent dependency-resolution fallback.

Trace wording watch:

    Follow the DA-03 cross-document trace wording check.

    Activation trace obligations are activation-domain emission obligations.

    Trace retention and loss remain governed by DA-03.

Future entrypoints watch:

    Future activation entrypoints are allowed only if explicitly defined by
    Turnix and preserving the same PackContext/orchestration boundary.

Service/capability registration watch:

    DA-16 should preserve service/capability registration during activation as
    mediated capability/service behavior, not raw authority.

Policy watch:

    DA-17 should preserve activation failure policy and remembered decisions
    without silent broadening.

Adapter watch:

    DA-23 and DA-27 should preserve adapter-mediated activation diagnostics
    without making adapter loading activation.

OrchestrationUnit watch:

    DA-24 should preserve activation-time mutation boundaries if activation
    creates or enters OrchestrationUnits.


CURRENT FINAL RECOMMENDATION
============================

Keep DA-14.

No heavy rewrite is recommended.

Main watch items:

    1. Activation order / PackManager wording:
        Treat PackManager as graph-preparation and management surface unless a
        specific activation authority path says otherwise.

        Activation itself remains Turnix-controlled and constrained by DA-14,
        DA-16, DA-17, DA-23, and DA-24.

    2. Last-known-good fallback:
        If defined later, it must remain explicit post-activation-failure
        recovery, not silent dependency-resolution fallback.

    3. Trace wording:
        Follow the DA-03 cross-document trace wording check.

    4. Future entrypoints:
        Future activation entrypoints are allowed only if explicitly defined by
        Turnix and preserving the same PackContext/orchestration boundary.

Preserve:

    - activation as executable-participation transition
    - activation not being earlier pipeline stages
    - ordered and externally observable lifecycle phases
    - Activated meaning as PackContext-mediated participation
    - pack-level activation
    - PackContext as pack-facing execution authority
    - PackContext delegation semantics
    - Turnix-defined activation entrypoints
    - activation / execution separation
    - immediate execution as unsupported
    - JavaScript entry / entries distinction
    - activation-independent code using caller authority
    - activation order not becoming PackManager authority
    - resolvePack / ensureActivated separation
    - Turnix guarantees applying only to orchestrated execution
    - DA-03-compatible activation trace wording
    - visible activation failure and no silent fallback
    - ensureActivated returning diagnostics, not code
    - last-known-good as post-activation-failure recovery only

Do not expand DA-14 into detailed generic resolution, dependency source
selection, importing/module-loading, ExecutionAdapter lifecycle/trust, trace
schema/storage, capability schema, permission prompting, user decision,
remembered policy, Job lifecycle, OrchestrationUnit lifecycle, RuntimeHost
command-surface, PackManager graph-preparation, or concrete last-known-good
recovery semantics.

--- doc-meta ---
DOC_ID: REWRITE-DA-14
DOC_FILE: docs/design/rewrite_recommendations/REWRITE-DA-14_RECOMMENDATION.txt
DOC_REV: 2
DOC_GIT: d70445c
----------------
