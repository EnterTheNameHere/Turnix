TURNIX DV3 AUDIT NOTE
=====================

Source file:
    docs/design/DA-29_PACK_MANAGEMENT_CATALOG_OPERATIONS_AND_GRAPH_PREPARATION.txt

Branch:
    Dv3

Loaded file metadata:
    DOC_REV: 1
    DOC_GIT: 1ea6c71
    GitHub blob SHA seen by assistant:
        d438d7c8049270fcb8de0fb36deb7cdc895a05b3

Audit note status:
    Consolidated Dv3 architecture rewrite recommendation.
    Action-focused revision pass for boundary compression, stale audit-report
    removal, and PackManager guardrail clarification.

Audit scope:
    This note audits architectural meaning, doctrine, and direction.
    It does not audit formatting unless formatting/structure affects meaning,
    machine interpretation, rule priority, or human usability as reference.

Important excluded source:
    AI_WORKFLOW_BOOTSTRAP.txt was intentionally not read.
    It is part of the workflow system being audited separately and should not
    bias this architecture pass.


SUMMARY
=======

DA-29 is Dv3-compatible.

No heavy rewrite is recommended.

Preserve PackManager as the official Turnix pack-management and
graph-preparation domain.

PackManager may inspect, present, prepare, validate, repair, modify, and
explain pack catalogs and selected pack graph intent.

PackManager must not become hidden:

    - Kernel authority
    - discovery
    - generic resolution
    - importing
    - activation
    - mechanical loading
    - PackContext authority
    - capability permission authority
    - persistence authority
    - policy authority
    - user-decision authority
    - SavePack authority
    - AppInstance authority
    - RuntimeHost authority
    - View authority
    - adapter execution authority

Main required clarification:

    Preserve "repair" as explicit, scoped, policy/user-accepted
    graph-preparation repair.

    Do not allow "repair" to mean automatic hidden correction.

Overall recommendation:

    KEEP


CURRENT CLASSIFICATION
======================

KEEP

DA-29 should not be rewritten heavily.

Future edits should preserve:

    - PackManager as management and graph-preparation domain
    - catalog is not graph
    - graph is not activation
    - selected graph and activation plan do not execute code
    - activation plan does not grant PackContext authority
    - risk acceptance does not make a graph safe
    - repair is explicit and scoped, not hidden automatic correction
    - PackManager writes use Kernel-mediated persistence, configuration,
      transaction, and policy boundaries
    - PackManager Views are presentation and interaction surfaces, not authority
    - PackManager-domain trace reasons do not replace trace reasons owned by
      other domains


ACTUAL DA-29 CHANGE 1: PRESERVE PACKMANAGER STOP-LINE
=====================================================

DA-29 should contain a compact source-level boundary statement.

Recommended source-level boundary:

    PackManager is the official Turnix pack-management and graph-preparation
    domain.

    PackManager may inspect, present, prepare, validate, repair, modify, and
    explain pack catalogs and selected pack graph intent.

    PackManager does not become discovery, generic resolution, importing,
    activation, mechanical loading, PackContext authority, capability
    permission, persistence authority, policy authority, SavePack authority,
    AppInstance authority, RuntimeHost authority, View authority, adapter
    execution authority, or Kernel authority.

    PackManager repair means explicit, scoped, policy/user-accepted
    graph-preparation repair.

    Repair does not mean automatic hidden correction.

This boundary belongs in DA-29 because DA-29 owns PackManager semantic limits.

Detailed ownership mapping belongs in the guardrail section, not in the source
document.


PRESERVATION RULE 1: PACKMANAGER IS MANAGEMENT DOMAIN, NOT AUTHORITY ROOT
=========================================================================

Preserve PackManager's role.

PackManager helps Turnix manage, inspect, explain, validate, repair, and
prepare pack graph intent.

PackManager helps choose and explain what Turnix will try.

Turnix still performs accepted graph, activation plan, policy, and runtime
operations only through Kernel-mediated boundaries.

PackManager does not silently:

    - discover
    - resolve
    - import
    - activate
    - load
    - grant PackContext authority
    - grant capability permission
    - mutate persistence
    - mutate active AppInstance graph state
    - mutate SavePack-owned copied pack graph
    - remember decisions
    - broaden decisions
    - accept risky graphs
    - update active packs
    - bypass policy
    - bypass Kernel authority


PRESERVATION RULE 2: CATALOG IS NOT GRAPH, GRAPH IS NOT ACTIVATION
==================================================================

Preserve catalog model distinctions.

Catalog presentation preserves the distinction between:

    - existence
    - visibility
    - resolution eligibility
    - dependency satisfaction
    - selected graph membership
    - activation state
    - importability
    - capability availability

Correct model:

    A catalog is not a graph.

    A selected graph is graph intent, not activation.

    An activation plan is graph intent refined for activation order and
    readiness, not execution.

    Activation is not import.

    Import is not capability authority.

PackManager must keep these meanings separate.


PRESERVATION RULE 3: CATALOG OPERATIONS DO NOT LAZILY RESCAN
============================================================

Preserve the no-lazy-rescan rule.

The pack catalog is derived from discovery and indexing.

Reading or listing catalog data does not perform discovery.

Catalog operations do not lazily rescan the filesystem unless an explicit
rescan operation is requested and accepted.


PRESERVATION RULE 4: VISIBILITY AND RESOLUTION REMAIN SEPARATE FROM PRESENTATION
================================================================================

Preserve visibility and resolution boundaries.

PackManager may present visibility graphs, hidden packs, shadowed packs,
save-local packs, and visibility failures when permitted.

Visibility presentation does not grant visibility.

Visibility does not grant capability authority.

PackManager does not silently bypass visibility constraints to make a graph
work.

PackManager may call generic resolution with explicit resolution context.

PackManager does not:

    - redefine PackRequest grammar
    - merge manifests
    - turn resolution into discovery
    - silently switch resolution contexts
    - persist resolution decisions by hidden side effect


PRESERVATION RULE 5: HARD DEPENDENCIES AND ADVISORY RELATIONS REMAIN DISTINCT
============================================================================

Preserve the hard/advisory split.

PackManager consumes hard dependencies and advisory pack relations.

PackManager does not convert advisory relations into:

    - hard dependencies
    - hard activation blockers
    - loader rules

by itself.

Policy or user decision may turn an advisory warning into a graph-selection
decision for a specific selected graph, but that decision is:

    - explicit
    - scoped
    - trace-visible
    - not a manifest rewrite

Hard dependency failure remains hard failure unless accepted policy or user
decision explicitly resolves it under the owning documents.

Advisory conflict override is not hard dependency satisfaction.


PRESERVATION RULE 6: SELECTED GRAPH AND ACTIVATION PLAN DO NOT EXECUTE CODE
===========================================================================

Preserve graph-preparation boundaries.

A selected pack graph is explicit graph intent.

An activation plan is selected graph intent refined for activation order and
activation readiness.

PackManager may prepare an activation plan.

PackManager does not activate packs by preparing an activation plan.

PackManager does not execute pack code by preparing selected graph intent.

PackManager does not import code by preparing selected graph intent.

PackManager does not grant PackContext authority by preparing selected graph
intent.

An accepted graph or activation plan may still fail during activation.

Such failure is activation evidence, not proof that PackManager owned
activation.


PRESERVATION RULE 7: RISK ACCEPTANCE AND REPAIR DO NOT CREATE SAFETY
====================================================================

Preserve expected-break, risky-graph, and repair semantics.

Risk acceptance does not make the graph safe.

Risk acceptance does not rewrite pack metadata.

Risk acceptance does not satisfy hard dependencies unless an allowed explicit
decision resolves that hard dependency failure.

Risk acceptance does not grant capability permission.

Risk acceptance does not grant execution authorization for unsafe code.

Expected-break evidence does not convert failure into success.

Repair means:

    explicit, scoped, policy/user-accepted graph-preparation repair

Repair does not mean:

    automatic hidden correction

Repair must not silently:

    - resolve ambiguity
    - accept risky graphs
    - broaden remembered decisions
    - rewrite manifests
    - bypass visibility
    - satisfy hard dependencies
    - update active AppInstance packs
    - mutate SavePack-owned copied pack graph
    - write roots, config, state, or policy decisions


PRESERVATION RULE 8: PACKMANAGER DOES NOT IMPORT, ACTIVATE, OR AUTHORIZE ADAPTER EXECUTION
=========================================================================================

Preserve import, activation, and adapter boundaries.

PackManager does not import pack code during catalog listing or graph
preparation.

PackManager does not use native imports to inspect pack runtime symbols.

PackManager does not bypass:

    - turnixImport
    - dependency consistency
    - visibility
    - activation
    - execution authority

PackManager does not call onLoad(ctx).

PackManager does not create PackContext authority.

PackManager may present adapter requirements, adapter availability, adapter
trust warnings, or adapter incompatibilities.

Presentation of adapter evidence does not authorize adapter execution.

Adapter execution authorization remains distinct from PackManager graph
preparation.


PRESERVATION RULE 9: PACKMANAGER DOES NOT GRANT CAPABILITY PERMISSION
=====================================================================

Preserve capability and permission boundaries.

PackManager operations may be exposed through RuntimeHost commands,
capabilities, RPC, or other mediated request surfaces.

Powerful or persistent operations require permission, policy, persistence, and
trace handling.

PackManager does not grant capability permission.

PackManager does not resolve capability export conflicts silently.

A PackManager service, if one exists internally, is runtime wiring only.

A PackManager service is not a cross-pack authority bypass.


PRESERVATION RULE 10: PACKMANAGER WRITES USE KERNEL-MEDIATED BOUNDARIES
=======================================================================

Preserve persistence and active-AppInstance safety boundaries.

PackManager writes may include:

    - catalog caches
    - package source configuration
    - installed pack records
    - selected graph records
    - SavePack graph evidence
    - copied pack material
    - pinned pack references
    - user decisions through DA-17-owned mechanisms
    - tool state

These writes occur only through Kernel-mediated persistence, configuration,
transaction, and policy boundaries.

PackManager does not silently mutate active AppInstance graph state.

PackManager does not silently update packs active in a running AppInstance.

PackManager does not silently make changes to a SavePack-owned copied pack
graph.

PackManager graph changes affecting a running AppInstance require explicit
lifecycle, policy, and trace handling.


PRESERVATION RULE 11: PACKMANAGER VIEW IS PRESENTATION, NOT AUTHORITY
=====================================================================

Preserve View and UI boundaries.

PackManager may expose Views.

PackManager Views are presentation and interaction surfaces.

Displaying a PackManager View does not grant pack-management authority to the
displaying surface.

Frontend PackManager code remains adapter-executed frontend code.

Frontend PackManager code does not directly mutate pack catalogs, selected
graphs, roots, SavePacks, or configuration.

PackManager UI does not own the semantics it presents.


PRESERVATION RULE 12: PACKMANAGER TRACE OWNERSHIP STAYS DOMAIN-SPECIFIC
=======================================================================

Preserve PackManager-domain trace ownership.

DA-29 owns PackManager-domain trace reason identifiers.

DA-03 owns the trace substrate.

PackManager trace reason identifiers do not replace trace reasons owned by
discovery, resolution, dependency, activation, capability, policy, Job,
AppInstance, RPC, adapter, OrchestrationUnit, Window, RuntimeHost, persistence,
configuration, or committed-state documents.

Trace wording should follow the DA-03 cross-document trace wording check.


WATCH ITEM: REPAIR WORDING
==========================

Preserve "repair" as explicit, scoped, policy/user-accepted
graph-preparation repair.

Do not allow repair wording to imply automatic hidden correction.

This is the main local DA-29 watch item.


WATCH ITEM: IMPLEMENTATION SPECS
================================

Future implementation specs may define concrete:

    - catalog result envelopes
    - resolved graph schemas
    - activation-plan schemas
    - PackManager UI layouts
    - registry/search/install/update/remove protocols
    - command syntax
    - RPC envelope fields

Those implementation specs must not redefine DA-29's authority boundaries.


REWRITE-BOUNDARY GUARDRAILS, NOT SUGGESTED DA-29 CONTENT
=========================================================

This section is not suggested DA-29 wording.

It exists to prevent architectural drift during rewrite when the editor knows
DA-29 but may not have every related document loaded.

Use these as guardrails only.

PackManager-boundary guardrails:

    PackManager is management and graph-preparation domain, not authority root.

    Catalog is not graph.

    Graph is not activation.

    Activation plan does not execute code.

    Activation plan does not import code.

    Activation plan does not grant PackContext authority.

    Catalog listing does not lazily rescan.

    Visibility presentation does not grant visibility.

    Resolution presentation does not persist resolution decisions by hidden side
    effect.

    Advisory relation is not hard dependency.

    Risk acceptance does not make the graph safe.

    Expected-break evidence does not convert failure into success.

    Repair is explicit, scoped, policy/user-accepted graph-preparation repair.

    Repair is not automatic hidden correction.

    PackManager does not call onLoad(ctx).

    PackManager does not authorize adapter execution.

    PackManager does not grant capability permission.

    PackManager writes use Kernel-mediated persistence, configuration,
    transaction, and policy boundaries.

    PackManager View is presentation, not authority.

    PackManager UI does not own the semantics it presents.

Owning-document guardrails:

    DA-03 owns trace substrate, retention, loss, and trace DAG semantics.

    DA-04 owns authoritative committed state, transactions, and commit
    boundaries.

    DA-08 owns Windows, finalized runtime history, and retained historical
    evidence.

    DA-09 owns root discovery and filesystem scanning boundaries.

    DA-10 owns pack identity, hierarchy, and SavePack visibility semantics.

    DA-11 owns generic PackRequest resolution mechanics.

    DA-12 owns importing, module loading, and code-boundary rules.

    DA-13 owns hard dependency, provider-role, pinned, copied, and advisory
    relation field semantics.

    DA-14 owns activation lifecycle and PackContext authority.

    DA-15 owns visibility/export access-control mechanics.

    DA-16 owns service, capability, permission, and Job-backed capability
    semantics.

    DA-17 owns user decision and remembered policy semantics.

    DA-19 owns Job lifecycle, timeout, cancellation, progress, result, and
    terminal outcome semantics.

    DA-20 owns AppInstance lifecycle and runtime identity semantics.

    DA-21 owns RPC, channel, and transport semantics.

    DA-22 owns UI, View, and frontend runtime semantics.

    DA-23 owns execution adapter boundary semantics.

    DA-24 owns OrchestrationUnit mutation-authority semantics.

    DA-27 owns adapter lifecycle, trust, isolation, concrete adapter classes,
    and adapter-domain trace reason identifiers.

    DA-28 owns RuntimeHost lifecycle, startup mode, command surface, workspace
    ownership, and local-first exposure semantics.

Local rewrite rule:

    DA-29 may name these concepts when needed to protect PackManager semantic
    boundaries.

    DA-29 should not define their detailed behavior.

    DA-29 should remain the PackManager, pack catalog, graph-preparation,
    repair-boundary, risk-presentation, PackManager View, PackManager write, and
    PackManager-domain trace document.


CURRENT FINAL RECOMMENDATION
============================

Keep DA-29.

No heavy rewrite is recommended.

Preserve PackManager as the official Turnix pack-management and
graph-preparation domain without turning it into authority root.

Preserve the meaning of "repair" as explicit, scoped, policy/user-mediated
graph-preparation repair, not hidden automatic correction.

Do not expand DA-29 into detailed root discovery, pack identity, visibility,
generic resolution, importing, dependency field, activation, PackContext,
capability, permission, policy, Job lifecycle, AppInstance lifecycle,
RPC/channel/transport, Window evidence, UI/View, ExecutionAdapter, adapter
lifecycle, RuntimeHost, concrete catalog envelope, concrete resolved graph,
concrete activation-plan, concrete PackManager UI, concrete registry/install/
update/remove protocol, command syntax, or RPC envelope semantics.

--- doc-meta ---
DOC_ID: REWRITE-DA-29
DOC_FILE: docs/design/rewrite_recommendations/REWRITE-DA-29_RECOMMENDATION.txt
DOC_REV: 5
DOC_GIT: b9b2187
----------------
