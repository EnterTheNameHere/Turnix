TURNIX DV3 AUDIT NOTE
=====================

Source file:
    docs/design/DA-01_PROJECT_INVARIANTS_AND_PHILOSOPHY.txt

Branch:
    Dv3

Loaded file metadata:
    DOC_REV: 10
    DOC_GIT: 1aff0d0
    GitHub blob SHA seen by assistant:
        4bad8714bfbb5197e9e5099370ce38e00c99b3aa

Audit note status:
    First-pass Dv3 architecture audit note.
    Updated after DA-28 RuntimeHost audit.
    Updated after DA-03 tracing audit and DA-04 memory/state audit.
    Updated after DA-29 PackManager audit.

Audit scope:
    This note audits architectural meaning, doctrine, and direction.
    It does not audit formatting unless formatting/structure affects meaning,
    machine interpretation, rule priority, or human usability as reference.

Important excluded source:
    AI_WORKFLOW_BOOTSTRAP.txt was intentionally not read.
    It is part of the workflow system being audited separately and should not
    bias this architecture pass.


SUMMARY
=======

DA-01 is a strong grounding document and should be kept.

It already contains several important Dv3-compatible ideas:

    - Turnix is not merely a plugin loader.
    - Turnix is not merely a dynamic script runner.
    - Turnix values continuity, determinism, explicitness, reversibility, and
      explicit uncertainty.
    - Kernel owns runtime authority but should not be over-specified.
    - RuntimeHost, adapters, transports, RPC, capabilities, and SavePack are
      already separated conceptually.
    - Same host language does not imply same authority.
    - Pack-provided Python is not automatically trusted backend implementation
      code just because Turnix backend is written in Python.
    - Turnix does not guarantee behavior outside Turnix orchestration.

Main required Dv3 change:

    DA-01 currently still points Turnix toward being a pack-built application
    system.

    Dv3 moves Turnix into the runtime domain.

    Turnix should be described primarily as a runtime for persistent,
    local-first, identity-stable, traceable, orchestrated systems.

    Packs remain first-class, but they are not the whole identity of Turnix.

Update after DA-28:

    DA-28 confirms that RuntimeHost is the running host boundary, not Kernel,
    not AppInstance, not SavePack, not CLI, not adapter, and not transport.

    DA-28 also sharpens the intended meaning of local-first:

        local-first, not local-only

    Local-first should mean authoritative runtime and state remain under
    user-controlled local infrastructure, not necessarily inside one process
    or on one physical machine.

Update after DA-03 and DA-04:

    DA-03 clarifies that trace is evidence, not committed state.

    DA-04 clarifies that committed state is the authoritative state boundary.

    Therefore DA-01 should not imply that retained trace evidence is required
    for committed-state correctness.

    The better doctrine is:

        domain-required trace emission is not optional as an obligation

        trace persistence is policy-governed and may be lossy when loss is
        represented honestly

        trace loss weakens evidential completeness, but does not change
        committed-state correctness

Update after DA-29:

    DA-29 confirms that pack management is an official Turnix domain, but it
    does not make packs, AppPacks, catalog UI, launcher behavior, or PackManager
    itself the identity of Turnix.

    PackManager helps choose and explain what Turnix will try.

    Turnix still performs only accepted graph, activation plan, policy, and
    operation through Kernel-mediated boundaries.

    Therefore the DA-01 runtime-domain identity rewrite remains valid:

        Turnix is not an AppPack runner.
        Turnix is not PackManager.
        Packs are first-class, but not identity-defining.


CURRENT CLASSIFICATION
======================

Overall classification:

    KEEP BUT UPDATE

More precise classification:

    01. Interpretation Basis
        KEEP

    02. Core Identity and Project Goal
        KEEP BUT GENERALIZE

    02.8 Runtime Authority Ordering
        KEEP

    02.12-02.13 Same Language Authority
        KEEP

    03. Core Truth Doctrine
        KEEP

    04. Locked Invariants
        KEEP BUT PATCH TRACE OPTIONALITY USING DA-03/DA-04 MODEL

    04.19 Local-first
        KEEP BUT CLARIFY WITH DA-28 LANGUAGE

    04.25 No Guarantees Outside Orchestration
        KEEP

    05. Adjustable Design Attitude
        KEEP

    06. Explicitly Open Questions
        KEEP

    07. Why This Area Exists
        KEEP


PRIMARY DV3 ISSUE: TURNIX IDENTITY
==================================

Current problem:

    The file currently describes Turnix as a persistent, local,
    identity-stable system "built on modular packs".

Why this is a problem:

    The phrase "built on modular packs" can make packs sound like the
    foundation of Turnix's identity.

    That points toward:

        Turnix = pack-based app system

    But Dv3 wants:

        Turnix = runtime domain
        packs = first-class composition/distribution/deployment mechanism
        apps = one kind of thing the runtime can host

    The earlier direction risks making AppPack feel mandatory for all valid
    Turnix usage. That would conflict with the emerging Dv3 idea that Turnix
    subsystems should also be usable in embedded, scripted, test, replay, or
    server contexts without requiring AppPack as the conceptual root.

New intended doctrine:

    Turnix does not merely run AppPacks.
    Turnix runs AppInstances.
    AppPack is one declarative way to create an AppInstance.

Recommended change:

    Rewrite the opening identity section so Turnix is described as a runtime,
    not as a pack-built app system.

Suggested replacement direction:

    02.1. Turnix exists to provide a persistent, local-first, identity-stable
          runtime for traceable and orchestrated systems.

    02.2. Turnix is not defined by any single launch mode, pack format, user
          interface, host process, model provider, or application packaging
          strategy.

    02.3. Modular packs are a first-class Turnix mechanism for describing,
          distributing, composing, extending, and persisting runtime behavior,
          but packs do not define the whole identity of Turnix.

    02.4. Turnix is not:
          - a plugin loader
          - a dynamic script runner
          - a web app framework
          - a convenience-first modding system
          - a pack-only application launcher

    02.5. Turnix is:
          - a runtime continuity engine
          - a deterministic resolver
          - a policy-driven orchestrator
          - an explainable system
          - a controlled execution and state boundary

Reasoning:

    This keeps the original philosophy while changing the center of gravity.

    The old text says, in effect:

        persistent local identity-stable system built on modular packs

    The new text says:

        persistent local-first identity-stable runtime for traceable
        orchestrated systems, with packs as first-class mechanisms

    That better matches Dv3.

Update after DA-29:

    DA-29 reinforces this identity correction.

    PackManager is an official Turnix pack-management and graph-preparation
    domain, but it is not Turnix identity.

    PackManager may prepare, explain, validate, repair, and manage selected pack
    graph intent, but it does not become Kernel authority, discovery, generic
    resolution, importing, activation, execution adapter, mechanical loader,
    capability permission, persistence authority, View authority, or policy
    owner.

    This means DA-01 should not describe Turnix as:

        - a pack manager
        - a launcher
        - a catalog system
        - an AppPack runner
        - a graph-preparation tool
        - a pack-management application

    PackManager belongs inside Turnix as a management domain.

    It does not define Turnix.


SECOND ISSUE: TRACE OPTIONALITY
===============================

Current problem:

    In the best-effort section, traces are listed with logs and analytics as
    explicitly optional data.

Why this is a problem:

    Dv3 elevates tracing from "debugging/diagnostic convenience" to structured
    runtime evidence.

    However, DA-03 and DA-04 clarify that trace must not be confused with
    committed state.

Corrected Dv3 trace doctrine after DA-03 and DA-04:

    Trace is evidence, not committed state.

    Committed state is authoritative only after crossing the DA-04 commit
    boundary.

    Trace evidence can prove or explain only within retained trace evidence.

    Trace persistence is policy-governed, bounded, and may be lossy when loss
    is represented honestly.

    Trace loss weakens evidential completeness, but does not change
    committed-state correctness.

Current Dv3 trace mantra:

    If not traced, it did not happen.

Correct interpretation:

    If an action is not present in retained trace evidence, the trace cannot
    prove that it happened.

    If trace evidence was lost, the loss itself must be represented honestly
    when possible.

Incorrect interpretation to avoid:

    If trace persistence fails, committed state becomes invalid.

Required distinction:

    There are at least three relevant categories:

        1. Domain-required trace emission
           A subject-owning document may require that a domain attempt to emit
           trace evidence for a meaningful action.

           This is an emission obligation.

        2. Trace persistence / retention
           The trace substrate does not guarantee that all emitted trace
           evidence is durably retained forever.

           Retention is policy-governed and may be bounded or lossy when loss
           is represented honestly.

        3. Optional diagnostic / verbose trace
           Development-mode or diagnostic enrichment that may be best-effort
           and non-authoritative.

Recommended change:

    Patch the best-effort section to avoid saying "traces are optional" as a
    global doctrine.

    But do not overcorrect by making trace persistence equivalent to committed
    state persistence.

Suggested replacement direction:

    04.10. Best-effort behavior is acceptable only for explicitly optional
           non-authoritative data such as:
           - diagnostic logs
           - verbose diagnostic traces
           - analytics

    04.11. Domain-required trace evidence is not optional as an emission
           obligation.

    04.12. Trace persistence and retention are policy-governed. Retained trace
           evidence may be bounded, rotated, or lossy when loss is represented
           honestly.

    04.13. Trace loss weakens evidential completeness. It does not by itself
           change committed-state correctness.

    04.14. Committed-state truth is governed by the authoritative commit model,
           not by trace completeness.

Reasoning:

    This preserves the old intent that some observability data can be best
    effort, while preventing the architecture from treating all traces as
    disposable.

    It also preserves the DA-03/DA-04 distinction:

        trace explains and proves within retained evidence
        committed state defines authoritative state truth

    Without this distinction, DA-01 may conflict with DA-03 and DA-04 in either
    direction:

        too weak:
            traces are merely optional diagnostics

        too strong:
            trace persistence becomes state authority

    The corrected middle is:

        required trace emission where a domain owns such an obligation
        honest loss evidence when trace is missing/lost
        committed-state correctness remains governed by DA-04


THIRD ISSUE: LOCAL-FIRST CLARIFICATION
======================================

Current state:

    The file says Turnix is local-first and assumes local filesystem, limited
    hardware, and limited context window.

This is mostly correct.

Potential future problem:

    Dv3 now allows thinking about Turnix as a runtime that may eventually run
    locally, on a server, remotely, as a worker, or through connected clients.

    The current local-first doctrine should not accidentally become:

        local-only forever

Update after DA-28:

    DA-28 gives the better wording and should be treated as the clearer
    source for this issue.

    Local-first does not require same-machine execution.

    Local-first means authoritative runtime and state remain under
    user-controlled local infrastructure.

    This can include:

        - RuntimeHost and frontend on the same machine
        - RuntimeHost on one LAN machine and frontend on another
        - home server RuntimeHost with local SavePack/state ownership
        - powerful local AI machine hosting execution while lighter clients
          present UI

Recommended clarification:

    Local-first should mean local operation remains the baseline authority,
    persistence model, failure model, and recovery model.

    It should not ban explicit, policy-governed LAN, server, remote-resource,
    or federated modes.

Suggested replacement direction:

    04.19. Turnix is local-first.

    04.20. Local-first means authoritative runtime and state remain under
           user-controlled local infrastructure.

    04.21. Local-first does not require same-machine execution.

    04.22. Explicit, policy-governed LAN, server, remote-resource, or
           federated modes may exist without replacing local-first as the
           baseline authority, persistence model, failure model, and recovery
           model.

    04.23. Turnix assumes:
           - local filesystem
           - limited hardware
           - limited context window

    04.24. Server-scale patterns are rejected by default, including:
           - lazy global registries
           - infinite retries
           - implicit availability

Reasoning:

    This keeps local-first as the default philosophy while leaving room for
    runtime-hosted/server/remote modes under explicit policy.

    DA-28 confirms that local-first should mean user-controlled local
    infrastructure, not necessarily one process, one machine, or no LAN.

    This also matches the existing open-question section, which already does
    not fully ban remote assets, remote compilers, or federated indexes.


WHAT SHOULD NOT CHANGE
======================

The same-language authority doctrine should remain.

Reason:

    It already protects one of the most important Dv3 boundaries:

        Internal Turnix Python is trusted backend implementation.
        Pack-provided Python is external code.

    Pack-provided code should not gain direct backend authority merely because
    it is written in the same language or happens to run in the same process.

This should survive as a major invariant.

The runtime authority ordering should remain.

Reason:

    It already supports Dv3:

        Kernel owns authority.
        RuntimeHost exposes controlled entry surfaces.
        Adapters execute declared pack code.
        Transports carry messages.
        RPC defines the logical communication boundary.
        Capabilities define what code may do.
        SavePack stores durable executed state.

    This is compatible with the idea that Kernel is important but intentionally
    not over-specified.

Update after DA-28:

    DA-28 confirms this ordering.

    RuntimeHost is not Kernel authority. It is the running host boundary through
    which Kernel-owned authority is started, exposed, coordinated, and closed.

    Entry surfaces and command surfaces may request, observe, or control through
    mediated runtime paths, but they do not become authority themselves.

    This means DA-01 should keep the authority ordering short and doctrinal,
    while DA-28 owns the detailed RuntimeHost explanation.

Update after DA-04:

    The phrase "SavePack stores durable executed state" may eventually need to
    be checked against later SavePack/AppInstance documents.

    DA-04 confirms that committed state, not trace, is authoritative.

    If SavePack wording is revised later, likely preferred wording may become:

        SavePack stores durable AppInstance state.

    Do not change this in DA-01 until SavePack/AppInstance documents are
    audited.

The "no guarantees outside orchestration" invariant should remain.

Reason:

    This strongly supports the Dv3 Job / OrchestrationUnit model.

    If code creates timers, background threads, ad-hoc async tasks, or
    side-effect-only module execution outside Turnix orchestration, Turnix
    should not promise reversibility, tracing, recovery, or lifecycle control
    for that behavior.

Update after DA-24:

    DA-24 confirms that mutation outside orchestration does not acquire Turnix
    guarantees by implication.

    DA-24 also confirms that OrchestrationUnit authority does not bypass DA-04
    transaction and commit rules.

Update after DA-29:

    DA-29 confirms this invariant from the pack-management side.

    PackManager can help prepare and explain selected pack graph intent, but it
    does not silently perform runtime-significant operations outside the owning
    authority boundaries.

    PackManager does not silently activate, import, load, execute, mutate
    SavePack state, mutate active AppInstance graph state, remember decisions,
    broaden policy, grant capability permission, or bypass Kernel-mediated
    operation paths.


READABILITY / MACHINE INTERPRETATION NOTE
=========================================

The document intentionally avoids RFC-style MUST/SHOULD/MAY language and says
normative meaning comes from sentence structure and context.

This is acceptable.

However, it means examples inside locked-invariant sections can become global
doctrine for a machine assistant.

Observed risk:

    Listing "traces" beside "logs" and "analytics" under optional best-effort
    data may be interpreted as a broad statement that traces are nonessential.

Updated risk after DA-03 and DA-04:

    Overcorrecting the trace wording may create the opposite problem.

    A machine assistant could incorrectly infer that retained trace evidence is
    required for committed-state correctness.

Updated risk after DA-29:

    Words such as "repair", "prepare", "manage", or "help choose" can be
    misread as permission for PackManager to automatically fix pack graphs.

    DA-29 prevents that. PackManager repair must mean explicit, scoped,
    policy/user-mediated graph-preparation repair, not hidden automatic
    correction.

Recommended handling:

    Keep the natural doctrine style, but be careful when giving examples in
    high-authority sections.

    If an example has categories with different authority levels, split them.

    In this file, trace should be split into:

        - optional diagnostic/verbose traces
        - domain-required trace emission obligations
        - policy-governed trace persistence/retention/loss evidence

    Also preserve the DA-04 rule:

        committed-state correctness is not trace-backed correctness

    If PackManager or packs are mentioned in DA-01, preserve the distinction
    between:

        - pack management
        - selected graph intent
        - resolution
        - activation
        - importing
        - execution
        - capability permission
        - persistence authority
        - runtime identity


RECOMMENDED EDIT ORDER
======================

1. Rewrite the identity section first.

   Goal:
       Move Turnix from "pack-built system" toward "runtime domain".

2. Patch trace optionality using DA-03/DA-04 wording.

   Goal:
       Prevent contradiction with Dv3 trace doctrine while preserving the
       distinction between trace evidence and committed state.

       Required final doctrine:

           trace emission may be required by domain obligation
           trace persistence is policy-governed
           trace loss weakens evidential completeness
           committed-state correctness is governed by DA-04

3. Add local-first clarification using DA-28 wording.

   Goal:
       Preserve local-first as baseline authority without banning future
       explicit LAN/server/remote-resource/federated modes.

       Make clear that local-first means user-controlled local infrastructure,
       not same-machine-only.

4. Keep pack identity general.

   Goal:
       Preserve packs as first-class without making Turnix into AppPack runner,
       PackManager, catalog system, launcher, or graph-preparation tool.

       DA-29 owns detailed PackManager semantics.

5. Do not yet heavily rewrite Kernel, RuntimeHost, adapter, RPC, capability,
   SavePack, or PackManager wording here.

   Goal:
       DA-01 should remain the top-level philosophy anchor, not become the
       detailed subsystem spec.


POSSIBLE FUTURE FOLLOW-UP
=========================

Later documents should define in more detail:

    - RuntimeHost
    - Kernel
    - AppInstance
    - AppPack
    - SavePack
    - ExecutionAdapter
    - RPC protocol vs transport
    - Capabilities
    - Jobs
    - OrchestrationUnit
    - Trace categories
    - Memory latest-state guarantee
    - IOManager authority
    - PackManager
    - standalone subsystem usage

DA-01 should only anchor the doctrine.

It should not become the full Turnix architecture specification.


UPDATE AFTER DA-28
==================

DA-28 was audited after the original DA-01 note.

DA-28 confirms several DA-01 recommendations:

    - RuntimeHost must remain below Kernel authority.
    - Startup modes are entry workflows, not separate runtime semantics.
    - Command surfaces are not authority.
    - CLI, browser frontend, launcher mode, remote client, deployment shape,
      and command syntax must not become Turnix identity.
    - Local-first does not require same-machine execution.
    - Local-first means authoritative runtime and state remain under
      user-controlled local infrastructure.

Impact on DA-01 recommendations:

    The DA-01 identity rewrite remains valid.

    The DA-01 local-first recommendation should be strengthened using DA-28's
    wording:

        local-first, not local-only
        user-controlled local infrastructure, not necessarily one process or
        one physical machine

    DA-01 should keep RuntimeHost wording short. DA-28 owns the detailed
    RuntimeHost doctrine.

    DA-01 should not expand into command surface, workspace ownership, startup
    mode, LAN exposure, or RuntimeHost lifecycle detail.


UPDATE AFTER DA-03 AND DA-04
============================

DA-03 and DA-04 refine the earlier DA-01 trace recommendation.

Earlier DA-01 recommendation said, in effect:

    authoritative runtime trace is not optional

That was directionally useful because DA-01 had been treating traces too much
like optional diagnostics.

However, after DA-03 and DA-04, the wording should be more precise.

DA-03 says:

    - trace is structured evidence
    - trace is not logging
    - trace is not committed state
    - trace retention is policy-governed
    - trace loss is acceptable only when represented as loss evidence
    - trace proves only within retained trace evidence

DA-04 says:

    - committed state is authoritative only after commit
    - traces are non-authoritative material
    - committed-state correctness is not trace-backed correctness
    - retained authoritative material restores committed state, not trace alone

Therefore DA-01 should say:

    domain-required trace emission is not optional as an obligation

not:

    trace persistence is always mandatory

Corrected trace doctrine for DA-01:

    Trace evidence is not merely optional diagnostics.

    Domain-owning documents may require trace emission for meaningful actions.

    Trace persistence and retention are policy-governed.

    Trace loss must not be hidden or presented as complete evidence.

    Trace loss weakens evidential completeness.

    Trace loss does not by itself change committed-state correctness.

    Committed-state truth is governed by DA-04's commit model.

Impact on DA-01 recommendations:

    The DA-01 trace optionality patch remains valid, but its wording should be
    refined.

    Do not make DA-01 imply that retained trace evidence is as authoritative as
    committed state.

    Do not make DA-01 imply trace-backed correctness.

    Keep DA-01 at doctrine level and let DA-03/DA-04 own the detailed split.


UPDATE AFTER DA-24
==================

DA-24 confirms the "no guarantees outside orchestration" invariant.

DA-24 says mutation outside orchestration does not acquire Turnix guarantees by
implication.

DA-24 also says OrchestrationUnit authority does not bypass DA-04 transaction
and commit rules.

Impact on DA-01 recommendations:

    Keep the "no guarantees outside orchestration" invariant.

    Do not expand DA-01 into detailed OrchestrationUnit doctrine.

    DA-01 should only preserve the top-level rule:

        Turnix guarantees apply only inside Turnix-owned orchestration,
        transaction, capability, persistence, policy, and trace boundaries.


UPDATE AFTER DA-29
==================

DA-29 confirms and sharpens the DA-01 runtime-domain identity rewrite from the
pack-management side.

DA-29 says PackManager is the official Turnix pack-management and
graph-preparation domain.

However, DA-29 also says PackManager is not:

    - Kernel authority
    - discovery
    - generic resolution
    - importing
    - activation
    - execution adapter
    - mechanical loader
    - capability permission
    - persistence authority
    - View authority
    - policy owner
    - AppInstance lifecycle owner
    - SavePack authority
    - Turnix identity

PackManager helps choose and explain what Turnix will try.

Turnix still performs only accepted graph, activation plan, policy, and
operation through Kernel-mediated boundaries.

Impact on DA-01 recommendations:

    The DA-01 identity rewrite remains valid.

    DA-01 should not describe Turnix as a pack manager, launcher, AppPack
    runner, catalog system, or graph-preparation tool.

    Packs remain first-class, but not identity-defining.

    PackManager belongs inside Turnix as an official management domain.

    PackManager does not define Turnix.

    DA-01 should not expand into detailed PackManager semantics. DA-29 owns
    PackManager catalog, graph-preparation, repair, risk, advisory, activation
    plan, and access-boundary semantics.


CURRENT FINAL RECOMMENDATION
============================

Keep DA-01.

Rewrite the project identity so Turnix is clearly a runtime, not merely an
AppPack/modular-pack application system.

Preserve packs as first-class, but not identity-defining.

Do not describe Turnix as PackManager, catalog system, launcher, AppPack runner,
or graph-preparation tool.

Patch trace wording so trace is not treated as merely optional diagnostics.

Use DA-03/DA-04 wording for trace:

    domain-required trace emission is not optional as an obligation

    trace persistence is policy-governed and may be lossy when loss is
    represented honestly

    trace loss weakens evidential completeness

    committed-state correctness is governed by DA-04, not by trace completeness

Clarify local-first using DA-28 wording:

    local-first, not local-only
    user-controlled local infrastructure, not necessarily same-machine-only

Do not weaken the same-language authority doctrine. It is already correct and
important.

Do not weaken the "no guarantees outside orchestration" invariant. DA-24 and
DA-29 confirm it from different sides.

Do not expand DA-01 into a detailed RuntimeHost, trace, memory,
OrchestrationUnit, Job, or PackManager specification. DA-28, DA-03, DA-04,
DA-24, DA-19, and DA-29 already own those layers.

--- doc-meta ---
DOC_ID: REWRITE-DA-01
DOC_FILE: docs/design/rewrite_recommendations/REWRITE-DA-01_RECOMMENDATION.txt
DOC_REV: 4
DOC_GIT: 48a6ef2
----------------
