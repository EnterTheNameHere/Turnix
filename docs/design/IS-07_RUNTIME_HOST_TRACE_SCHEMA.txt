===============================================================================
IMPLEMENTATION SPECIFICATION IS-07 - RUNTIME HOST TRACE SCHEMA
===============================================================================

Status:
-------
EVOLVING

Description:
------------
Defines the concrete RuntimeHost-domain trace field schema used by
RuntimeHost lifecycle, Workspace ownership, command surface, startup mode,
LAN exposure, client connection, and RuntimeHost operation trace evidence.

This document implements RuntimeHost trace-context obligations and provides
shared field naming for RuntimeHost-adjacent implementation specifications.

Purpose:
--------
This document defines the concrete field vocabulary for RuntimeHost-domain
trace evidence so RuntimeHost-adjacent implementation specifications do not
invent incompatible trace payload shapes.

It defines:
- RuntimeHost trace record field vocabulary
- common RuntimeHost trace context fields
- RuntimeHost, TurnixRuntime, and process identity fields
- Workspace ownership trace fields
- command trace fields
- startup-mode trace fields
- entry-surface and client-connection trace fields
- LAN exposure trace fields
- RuntimeHost operation trace fields
- Application, ApplicationRun, and SaveBundle correlation fields
- error, reason, and outcome field conventions
- trace correlation field conventions
- schema extension rules

It does NOT define:
- trace event structure, trace DAG semantics, span mechanics, storage, or
  query behavior (see DA-03)
- RuntimeHost semantics or RuntimeHost trace reason ownership (see DA-28)
- Workspace ownership and host-lock behavior (see IS-03)
- command-surface behavior or command result envelope behavior (see IS-04)
- startup-mode behavior or startup result envelope behavior (see IS-05)
- LAN exposure and client-connection behavior (see IS-06)
- Application or ApplicationRun trace reason ownership
- RPC trace correlation semantics (see DA-21)
- UI-domain trace reason ownership (see DA-22)
- adapter-domain trace reason ownership (see DA-27)
- OrchestrationUnit trace reason ownership (see DA-24)
- concrete JSON serialization format for the full trace store
- concrete trace file layout
- concrete UI display of trace records
- security policy, authentication policy, or redaction rules

Upstream Documents:
------------------
- CMR-01
- CMR-02
- CMR-03
- CMR-04
- CMR-05
- DA-03
- DA-20
- DA-21
- DA-22
- DA-24
- DA-27
- DA-28
- IS-03
- IS-04
- IS-05
- IS-06

Downstream Documents:
--------------------
- later RuntimeHost trace store implementation specifications
- later RuntimeHost evidence/export implementation specifications
- later CLI trace inspection implementation specifications
- later browser trace viewer implementation specifications
- later LAN/client diagnostic implementation specifications


===============================================================================
01. INTERPRETATION BASIS
===============================================================================

01.1. This document expresses normative meaning through explicit,
      deterministic prose.

01.2. Normative meaning in Turnix derives from sentence structure and
      document context rather than from RFC-style keywords.

01.3. The CORE META-RULES documents define the general rule language and
      interpretation model used by this document.

01.4. This document defines implementation-level RuntimeHost-domain trace
      field schema within that shared interpretation model.

01.5. This implementation specification does not create new trace substrate
      semantics.

01.6. This implementation specification defines RuntimeHost-domain field
      vocabulary layered onto domain semantics owned by other documents.


===============================================================================
02. CORE CONCEPT
===============================================================================

02.1. RuntimeHost trace schema answers:

          "Which concrete fields identify RuntimeHost-domain trace evidence
           across host lifecycle, Workspace ownership, commands, startup
           modes, clients, LAN exposure, and host-level operations?"

02.2. RuntimeHost trace schema provides field vocabulary.

02.3. RuntimeHost trace schema does not own trace event storage.

02.4. RuntimeHost trace schema does not own trace DAG semantics.

02.5. RuntimeHost trace schema does not own span mechanics.

02.6. RuntimeHost trace schema does not own trace reason meanings.

02.7. DA-03 owns the trace substrate.

02.8. DA-28 owns RuntimeHost-domain trace reason identifiers and their
      semantic meaning.

02.9. IS-07 defines concrete RuntimeHost-domain trace fields used by
      RuntimeHost implementation specifications.

02.10. Field presence depends on event kind, available context, and owning
       workflow.

02.11. Absence of a field does not imply absence of the represented domain
       fact unless the emitting schema or workflow requires that field.


===============================================================================
03. RUNTIMEHOST TRACE FIELD GROUPS
===============================================================================

03.1. RuntimeHost-domain trace fields are grouped by purpose.

03.2. Field groups include:
      - base trace fields
      - RuntimeHost identity fields
      - Workspace fields
      - ownership fields
      - startup-mode fields
      - command fields
      - RuntimeHost operation fields
      - entry-surface fields
      - client-connection fields
      - LAN and exposure fields
      - transport and channel fields
      - cross-domain correlation fields
      - error, reason, and outcome fields
      - trace correlation fields
      - extension fields

03.3. Field grouping is for schema organization and implementation clarity.

03.4. Field grouping does not create separate trace event classes by itself.

03.5. One RuntimeHost-domain trace record may contain fields from multiple
      groups when the event crosses multiple domains.

03.6. Fields from another domain may appear only as correlation fields unless
      the owning domain document permits more detailed inclusion.

03.7. Correlation fields do not transfer authority or ownership from the
      referenced domain into RuntimeHost trace evidence.


===============================================================================
04. BASE TRACE FIELDS
===============================================================================

04.1. A RuntimeHost-domain trace record contains base trace context fields
      supplied by the trace substrate, projected by the RuntimeHost trace
      emission layer, or represented as RuntimeHost-domain attributes.

04.2. RuntimeHost-layer base field vocabulary includes:
      - schemaVersion
      - traceRecordId
      - traceReason
      - timestampUtc
      - traceLevel
      - sourceDomain
      - message
      - details

04.3. schemaVersion identifies the RuntimeHost trace field schema version.

04.4. traceRecordId identifies one trace record when the trace substrate or
      serialization surface exposes record identity through that name.

04.5. traceReason contains the RuntimeHost-domain trace reason identifier or a
      permitted implementation-specific trace reason extension.

04.6. timestampUtc records when the trace evidence was emitted or accepted by
      the trace substrate when that projected name is used.

04.7. traceLevel classifies severity or presentation level when such
      classification is supported.

04.8. sourceDomain is RuntimeHost for RuntimeHost-domain trace records when
      source-domain projection is emitted.

04.9. message contains a human-facing summary when available.

04.10. details contains structured event-specific data when available.

04.11. These names are RuntimeHost trace-schema field names.

04.12. These names do not define canonical DA-03 serialization names.

04.13. When a RuntimeHost-layer field corresponds to a DA-03-owned substrate
       field, IS-07 treats that name as a projection, alias, or attribute
       mapping only.

04.14. Concrete trace substrate fields remain governed by DA-03.

04.15. IS-07 defines only the RuntimeHost-domain field vocabulary layered onto
       the trace substrate.


===============================================================================
05. DA-03 FIELD LAYERING AND SERIALIZATION MAPPING
===============================================================================

05.1. IS-07 consumes the DA-03 trace event model.

05.2. DA-03-owned trace substrate fields retain their DA-03 meaning when used
      by RuntimeHost-domain trace records.

05.3. DA-03-owned trace substrate fields include:
      - eventId
      - timestamp
      - level
      - domain
      - message
      - reason
      - attributes
      - parentEventIds

05.4. IS-07 does not redefine DA-03-owned trace substrate fields.

05.5. IS-07 does not define a competing canonical substrate schema.

05.6. IS-07 defines RuntimeHost-domain field vocabulary layered onto the
      DA-03 trace event model.

05.7. RuntimeHost-domain fields are represented as trace attributes unless a
      trace serialization implementation specification defines a direct
      serialized field mapping.

05.8. Trace serialization implementation specifications may define concrete
      serialized names, aliases, nesting, indexing, and storage layout for
      DA-03 substrate fields and IS-07 RuntimeHost-domain fields.

05.9. Serialization mapping does not change DA-03 trace substrate semantics.

05.10. Serialization mapping does not change DA-28 RuntimeHost-domain trace
       reason meanings.

05.11. Serialization mapping does not change IS-07 RuntimeHost-domain field
       meanings.

05.12. A field alias does not create a second authority source for the same
       trace fact.


===============================================================================
06. RUNTIMEHOST IDENTITY FIELDS
===============================================================================

06.1. RuntimeHost identity fields identify the host-level context.

06.2. RuntimeHost identity fields include:
      - runtimeHostId
      - runtimeId
      - runtimeHostLifecyclePhase
      - runtimeHostActiveSpanId
      - runtimeHostStartRequestId
      - kernelContextId
      - processId
      - processIdentity

06.3. runtimeHostId identifies the RuntimeHost boundary when such identity is
      defined.

06.4. runtimeId identifies the TurnixRuntime execution when such identity is
      exposed to RuntimeHost-domain trace emission.

06.5. runtimeHostLifecyclePhase records the RuntimeHost lifecycle phase when
      the emitting workflow exposes it.

06.6. runtimeHostActiveSpanId identifies the RuntimeHost active-region span
      when applicable.

06.7. runtimeHostStartRequestId identifies the request that caused
      RuntimeHost startup when applicable.

06.8. kernelContextId identifies the active Kernel or KernelContext when such
      identity is defined.

06.9. processId records the operating-system process identifier when
      available.

06.10. processIdentity records implementation-defined process identity context
       when processId alone is insufficient.

06.11. runtimeHostId remains distinct from runtimeId.

06.12. kernelContextId does not make KernelContext a mandatory architecture
       object.

06.13. RuntimeHost identity fields do not grant Kernel authority.


===============================================================================
07. WORKSPACE FIELDS
===============================================================================

07.1. Workspace fields identify the Workspace or runtime scope bound to the
      RuntimeHost.

07.2. Workspace fields include:
      - workspaceId
      - workspacePath
      - workspaceIdentitySource
      - workspaceBindingStatus
      - workspaceBindingErrorCategory

07.3. workspaceId identifies the Workspace when a stable Workspace identity is
      available.

07.4. workspacePath records the path or path summary when safe and available.

07.5. workspaceIdentitySource records how Workspace identity was derived when
      the implementation distinguishes identity sources.

07.6. workspaceBindingStatus records binding status when the trace record is
      about Workspace binding.

07.7. workspaceBindingErrorCategory records a binding-specific error category
      when Workspace binding fails.

07.8. workspacePath is not a substitute for workspaceId when workspaceId is
      defined.

07.9. workspaceId is not runtimeHostId.

07.10. workspaceId is not runtimeId.

07.11. workspacePath is trace evidence only.

07.12. workspacePath does not grant filesystem authority, Workspace
       ownership, persistence authority, or committed-state mutation authority.


===============================================================================
08. OWNERSHIP FIELDS
===============================================================================

08.1. Ownership fields describe Workspace ownership evidence under IS-03.

08.2. Ownership fields include:
      - ownershipMode
      - accessMode
      - ownershipStatus
      - ownerRuntimeId
      - existingOwnerRuntimeId
      - ownerRuntimeHostId
      - existingOwnerRuntimeHostId
      - ownerToken
      - ownerNonce
      - lockState
      - hostMachineId
      - userId
      - coordinatorProcessId
      - coordinatorProcessStartIdentity
      - startedAtUtc
      - heartbeatAtUtc
      - requestedOwnershipMode
      - ownershipResult
      - staleOwnerEvidenceCategory
      - recoveryResult

08.3. ownershipMode records writable ownership mode when writable ownership
      exists.

08.4. accessMode records access behavior such as read-only inspection when no
      writable ownership is created.

08.5. ownershipStatus records the current ownership interpretation for the
      event.

08.6. ownerRuntimeId records the TurnixRuntime execution identity in an
      ownership claim when available.

08.7. existingOwnerRuntimeId records an already-present runtime owner when a
      second owner is rejected or recovery is considered.

08.8. ownerRuntimeHostId records the RuntimeHost boundary identity in an
      ownership claim when that identity is retained separately.

08.9. existingOwnerRuntimeHostId records an already-present RuntimeHost
      boundary identity when that identity is retained separately.

08.10. ownerRuntimeHostId is not a substitute for ownerRuntimeId when both
       identities are defined.

08.11. ownerToken records the unpredictable ownership claim token when
       available.

08.12. ownerNonce records an implementation-defined ownership nonce when that
       value is distinct from ownerToken.

08.13. lockState records the ownership record lock state under IS-03.

08.14. hostMachineId records the owner machine identity when available.

08.15. userId records owner user identity evidence when available.

08.16. coordinatorProcessId records the operating-system process that
       represents the owning runtime execution when available.

08.17. coordinatorProcessStartIdentity records process lifetime identity when
       the implementation distinguishes a concrete process lifetime from PID
       reuse.

08.18. startedAtUtc records when the ownership claim began.

08.19. heartbeatAtUtc records the latest heartbeat time observed or written.

08.20. requestedOwnershipMode records the requested ownership mode.

08.21. ownershipResult records the result of an ownership attempt or ownership
       recovery attempt.

08.22. staleOwnerEvidenceCategory classifies stale owner evidence when
       available.

08.23. recoveryResult records stale-owner recovery outcome when available.

08.24. Read-only inspection uses accessMode rather than ownershipMode.

08.25. Ownership fields distinguish Workspace identity, TurnixRuntime
       execution identity, RuntimeHost boundary identity, process identity,
       machine identity, user identity, and owner token identity.

08.26. Ownership trace fields do not grant Workspace ownership by themselves.

08.27. Ownership trace fields do not grant persistence authority or
       committed-state mutation authority by themselves.


===============================================================================
09. STARTUP-MODE FIELDS
===============================================================================

09.1. Startup-mode fields describe IS-05 startup mode handling.

09.2. Startup-mode fields include:
      - startupRequestId
      - startupMode
      - startupStatus
      - startupInputSource
      - startupFailureCategory
      - startupProjectionSource

09.3. startupRequestId identifies one startup request.

09.4. startupMode records the startup mode category.

09.5. startupStatus records startup-mode result status when applicable.

09.6. startupInputSource records the entry source for startup input when
      available.

09.7. startupFailureCategory records startup-mode failure category when
      applicable.

09.8. startupProjectionSource records whether startup result status was
      projected from another result surface, such as command result, when that
      projection is explicit.

09.9. startupRequestId is not commandId.

09.10. startupStatus does not redefine commandStatus, Job terminal outcome,
       Application lifecycle outcome, ApplicationRun lifecycle outcome, or
       OrchestrationUnit terminal outcome.


===============================================================================
10. COMMAND FIELDS
===============================================================================

10.1. Command fields describe IS-04 command-surface handling.

10.2. Command fields include:
      - commandId
      - commandCategory
      - commandStatus
      - commandInputSource
      - commandSourceIdentity
      - commandProjectionSource
      - commandErrorCategory
      - commandResultReference

10.3. commandId identifies one received command.

10.4. commandCategory records the semantic command category.

10.5. commandStatus records command handling status when applicable.

10.6. commandInputSource records whether the command originated from CLI,
      frontend, RPC, remote client, test harness, launcher, or another known
      source.

10.7. commandSourceIdentity records implementation-defined source identity
      when available.

10.8. commandProjectionSource records whether command result status was
      projected from another result surface, such as RuntimeHost operation
      result, when that projection is explicit.

10.9. commandErrorCategory records command error category when applicable.

10.10. commandResultReference identifies a retained command result when a
       retained result surface exists.

10.11. commandStatus does not redefine RuntimeHost operation outcome, Job
       terminal outcome, Application lifecycle outcome, ApplicationRun
       lifecycle outcome, adapter lifecycle outcome, or OrchestrationUnit
       terminal outcome.

10.12. commandSourceIdentity is trace evidence only.

10.13. commandSourceIdentity does not grant command authority by itself.


===============================================================================
11. RUNTIMEHOST OPERATION FIELDS
===============================================================================

11.1. RuntimeHost operation fields describe host-level operations under DA-28
      and command/startup implementation documents.

11.2. RuntimeHost operation fields include:
      - runtimeOperationId
      - runtimeOperationCategory
      - runtimeOperationStatus
      - runtimeOperationSource
      - runtimeOperationParentId
      - runtimeOperationResultReference
      - runtimeOperationErrorCategory
      - runtimeOperationSupersessionSource
      - runtimeOperationDetachReason
      - runtimeOperationRetentionReason

11.3. runtimeOperationId identifies one RuntimeHost operation.

11.4. runtimeOperationCategory records the semantic operation category.

11.5. runtimeOperationStatus records operation status when applicable.

11.6. runtimeOperationSource records the source surface or workflow that
      requested the operation when available.

11.7. runtimeOperationParentId records parent operation identity when an
      operation is decomposed into child operations.

11.8. runtimeOperationResultReference identifies retained operation result
      evidence when such a surface exists.

11.9. runtimeOperationErrorCategory records operation failure category when
      applicable.

11.10. runtimeOperationSupersessionSource records what newer work, accepted
       input, or higher-priority work superseded the operation when
       applicable.

11.11. runtimeOperationDetachReason records why an operation detached from a
       waiting entry surface when applicable.

11.12. runtimeOperationRetentionReason records why operation result or
       evidence was retained when applicable.

11.13. runtimeOperationStatus does not redefine commandStatus, startupStatus,
       Job terminal outcome, Application lifecycle outcome, ApplicationRun
       lifecycle outcome, adapter lifecycle outcome, or OrchestrationUnit
       terminal outcome.


===============================================================================
12. ENTRY-SURFACE FIELDS
===============================================================================

12.1. Entry-surface fields identify RuntimeHost entry surfaces.

12.2. Entry-surface fields include:
      - entrySurfaceId
      - entrySurfaceKind
      - entrySurfaceStatus
      - entrySurfaceSource
      - entrySurfaceErrorCategory

12.3. entrySurfaceId identifies one entry surface when the implementation
      exposes entry-surface identity.

12.4. entrySurfaceKind records the kind of entry surface.

12.5. entrySurfaceStatus records entry-surface status when applicable.

12.6. entrySurfaceSource records the source that requested or created the
      entry surface when available.

12.7. entrySurfaceErrorCategory records entry-surface error category when
      applicable.

12.8. entrySurfaceId is not runtimeHostId.

12.9. entrySurfaceId is not viewId unless a later implementation
      specification defines an explicit mapping.

12.10. entrySurfaceSource is trace evidence only.


===============================================================================
13. CLIENT-CONNECTION FIELDS
===============================================================================

13.1. Client-connection fields describe IS-06 client connection handling.

13.2. Client-connection fields include:
      - clientConnectionId
      - clientClassification
      - clientConnectionStatus
      - clientConnectionErrorCategory
      - frontendClientId
      - remoteEndpointSummary
      - pairingId
      - authenticatedPrincipalId
      - reconnectSourceConnectionId
      - reconnectResult

13.3. clientConnectionId identifies one client connection attempt or accepted
      client connection.

13.4. clientClassification records local client, LAN client, remote client,
      test client, unknown client, or another explicitly defined client class.

13.5. clientConnectionStatus records client connection status when
      applicable.

13.6. clientConnectionErrorCategory records client connection error category
      when applicable.

13.7. frontendClientId identifies a frontend client when available.

13.8. remoteEndpointSummary records a safe implementation-defined summary of
      the remote endpoint when available.

13.9. pairingId records pairing identity when applicable.

13.10. authenticatedPrincipalId records authenticated principal identity when
       applicable.

13.11. reconnectSourceConnectionId records the prior connection identity used
       for reconnect when available.

13.12. reconnectResult records reconnect outcome when applicable.

13.13. clientConnectionId is not user identity.

13.14. frontendClientId is not Application identity.

13.15. frontendClientId is not ApplicationRun identity.

13.16. authenticatedPrincipalId does not grant capability permission by
       itself.

13.17. pairingId does not grant authority by itself.

13.18. remoteEndpointSummary is trace evidence only.


===============================================================================
14. LAN AND EXPOSURE FIELDS
===============================================================================

14.1. LAN and exposure fields describe bind scope and exposure behavior under
      IS-06.

14.2. LAN and exposure fields include:
      - bindScope
      - exposureStatus
      - exposureScope
      - localBindAddressSummary
      - lanBindAddressSummary
      - exposurePolicyDecisionId
      - exposureErrorCategory

14.3. bindScope records localOnly, lanExplicit, remoteExplicit, disabled, or
      another explicitly defined bind scope.

14.4. exposureStatus records exposure handling status when applicable.

14.5. exposureScope records the practical exposure scope selected or reached
      by the implementation when available.

14.6. localBindAddressSummary records local bind information when safe and
      available.

14.7. lanBindAddressSummary records LAN bind information when safe and
      available.

14.8. exposurePolicyDecisionId records the policy decision related to
      exposure when applicable.

14.9. exposureErrorCategory records exposure-specific error category when
      applicable.

14.10. remoteExplicit is a reserved bind scope unless a later authoritative
       security and deployment design enables it.

14.11. bindScope does not grant command authority or capability permission.

14.12. bind address summaries are trace evidence only.

14.13. bind address summaries do not grant client identity, user identity,
       authenticated principal identity, or Workspace ownership.


===============================================================================
15. TRANSPORT AND CHANNEL FIELDS
===============================================================================

15.1. Transport and channel fields correlate RuntimeHost-domain evidence with
      RPC transport evidence when available.

15.2. Transport and channel fields include:
      - transportKind
      - channelId
      - rpcMessageId
      - rpcCorrelationId
      - disconnectCategory
      - timeoutCategory

15.3. transportKind records the concrete transport kind when applicable.

15.4. channelId identifies the RPC channel when available.

15.5. rpcMessageId identifies the related RPC message when available.

15.6. rpcCorrelationId identifies the related RPC correlation identity when
      available.

15.7. disconnectCategory records disconnect classification when applicable.

15.8. timeoutCategory records timeout classification when applicable.

15.9. Transport and channel fields are correlation fields in
      RuntimeHost-domain trace evidence.

15.10. Transport and channel fields do not redefine DA-21 RPC, channel, or
       transport semantics.


===============================================================================
16. CROSS-DOMAIN CORRELATION FIELDS
===============================================================================

16.1. Cross-domain correlation fields link RuntimeHost-domain events to other
      Turnix domains.

16.2. Cross-domain correlation fields include:
      - applicationId
      - applicationRunId
      - appDefinitionSourceId
      - activeAppPackId
      - saveBundleId
      - viewId
      - adapterKind
      - adapterInstanceId
      - capabilityId
      - jobId
      - orchestrationUnitId
      - transactionId
      - decisionId
      - windowId
      - safePointWindowId
      - abortedWindowId

16.3. applicationId identifies the related Application when applicable.

16.4. applicationRunId identifies the related ApplicationRun when applicable.

16.5. appDefinitionSourceId identifies the accepted AppDefinition source when
      such identity exists.

16.6. activeAppPackId identifies the AppPack when the accepted AppDefinition
      source is an AppPack.

16.7. activeAppPackId is AppPack-specific correlation.

16.8. activeAppPackId is not universal Application definition identity.

16.9. saveBundleId identifies the related SaveBundle when applicable.

16.10. saveBundleId does not make SaveBundle a Pack.

16.11. saveBundleId does not grant persistence authority.

16.12. viewId identifies the related View when applicable.

16.13. adapterKind identifies adapter kind when applicable.

16.14. adapterInstanceId identifies adapter instance when applicable.

16.15. capabilityId identifies related capability when applicable.

16.16. jobId identifies related Job when applicable.

16.17. orchestrationUnitId identifies related OrchestrationUnit when
       applicable.

16.18. transactionId identifies related transaction when applicable.

16.19. decisionId identifies related decision point or policy decision when
       applicable.

16.20. windowId identifies related Window evidence when applicable.

16.21. safePointWindowId identifies related SafePointWindow evidence when
       applicable.

16.22. abortedWindowId identifies related AbortedWindow evidence when
       applicable.

16.23. Cross-domain correlation fields do not transfer authority or semantic
       ownership into RuntimeHost trace evidence.

16.24. Cross-domain correlation fields do not replace trace evidence owned by
       the referenced domain.

16.25. Cross-domain correlation fields do not grant Application identity,
       ApplicationRun identity, SaveBundle ownership, View authority,
       adapter authority, capability permission, Job authority,
       OrchestrationUnit authority, transaction authority, decision authority,
       or Window finalization authority.


===============================================================================
17. ERROR, REASON, AND OUTCOME FIELDS
===============================================================================

17.1. Error, reason, and outcome fields classify why a RuntimeHost-domain
      event occurred or how it ended.

17.2. Error, reason, and outcome fields include:
      - errorCategory
      - failureReasonCategory
      - stopReasonCategory
      - crashReasonCategory
      - endReasonCategory
      - rejectionReasonCategory
      - cancellationReasonCategory
      - supersessionSource
      - detachReason
      - retentionReason
      - outcome

17.3. errorCategory records a domain-specific error category when applicable.

17.4. failureReasonCategory records failure classification when applicable.

17.5. stopReasonCategory records stop classification when applicable.

17.6. crashReasonCategory records crash classification when applicable.

17.7. endReasonCategory records active-span end classification when
      applicable.

17.8. rejectionReasonCategory records rejection classification when
      applicable.

17.9. cancellationReasonCategory records cancellation classification when
      applicable.

17.10. supersessionSource records the source of host-operation supersession
       when applicable.

17.11. detachReason records detachment classification when applicable.

17.12. retentionReason records retention classification when applicable.

17.13. outcome records a concise outcome label when the emitting workflow
       exposes one.

17.14. These fields classify RuntimeHost-domain evidence only.

17.15. These fields do not redefine Job terminal outcome, Application
       lifecycle outcome, ApplicationRun lifecycle outcome, adapter lifecycle
       outcome, OrchestrationUnit terminal outcome, transaction outcome,
       command outcome, startup outcome, Workspace ownership outcome,
       transport outcome, RPC outcome, View outcome, or Window finalization
       outcome.


===============================================================================
18. TRACE CORRELATION FIELDS
===============================================================================

18.1. Trace correlation fields preserve causal and navigational links.

18.2. Trace correlation fields include:
      - traceCorrelationId
      - parentTraceRecordId
      - causeTraceRecordId
      - sourceTraceRecordId
      - relatedTraceRecordIds
      - spanId
      - parentSpanId

18.3. traceCorrelationId identifies a correlation group when available.

18.4. parentTraceRecordId identifies parent trace evidence when available.

18.5. causeTraceRecordId identifies cause evidence when available.

18.6. sourceTraceRecordId identifies source evidence when available.

18.7. relatedTraceRecordIds identifies additional related trace evidence when
      available.

18.8. spanId identifies the related span when available.

18.9. parentSpanId identifies the parent span when available.

18.10. DA-03 owns trace DAG and span semantics.

18.11. IS-07 only defines RuntimeHost-domain field names used to store or
       refer to correlation values.


===============================================================================
19. FIELD PRESENCE RULES
===============================================================================

19.1. RuntimeHost-domain trace records include runtimeHostId when the event is
      associated with an existing RuntimeHost identity.

19.2. RuntimeHost-domain trace records may omit runtimeHostId when the event
      occurs before RuntimeHost identity exists.

19.3. RuntimeHost-domain trace records include runtimeId when the event is
      associated with an existing TurnixRuntime execution identity and that
      identity is exposed to trace emission.

19.4. Startup classification records include startupRequestId when a startup
      request identity exists.

19.5. Command records include commandId when command identity exists.

19.6. Workspace ownership records include workspaceId when Workspace identity
      exists.

19.7. Workspace ownership records include ownerRuntimeId when writable
      ownership evidence includes runtime execution identity.

19.8. Workspace ownership records include ownershipMode when writable
      ownership exists.

19.9. Read-only inspection records include accessMode when access mode is
      recorded.

19.10. Client connection records include clientConnectionId when client
       connection identity exists.

19.11. LAN exposure records include bindScope when exposure or bind scope is
       being classified.

19.12. RuntimeHost operation records include runtimeOperationId when operation
       identity exists.

19.13. Application-related records include applicationId when Application
       identity exists and the emitting workflow exposes it.

19.14. ApplicationRun-related records include applicationRunId when
       ApplicationRun identity exists and the emitting workflow exposes it.

19.15. SaveBundle-related records include saveBundleId when SaveBundle
       identity exists and the emitting workflow exposes it.

19.16. Error records include the most specific available errorCategory or
       reason category.

19.17. Records do not invent unknown identities merely to fill optional
       fields.

19.18. Records do not copy stale or inferred identities into fields as if they
       were confirmed.

19.19. Records may include unavailable, unknown, or redacted markers only when
       the concrete trace schema defines those markers.


===============================================================================
20. EVIDENCE-ONLY IDENTITY AND REDACTION LIMITS
===============================================================================

20.1. Identity-looking fields in RuntimeHost trace records are trace evidence.

20.2. Trace evidence does not grant authority by itself.

20.3. userId, authenticatedPrincipalId, frontendClientId,
      remoteEndpointSummary, workspacePath, localBindAddressSummary,
      lanBindAddressSummary, pairingId, commandSourceIdentity, and related
      fields do not let a client or trace emitter forge identity.

20.4. These fields do not grant command authority.

20.5. These fields do not grant capability permission.

20.6. These fields do not grant authenticated principal status.

20.7. These fields do not grant user identity.

20.8. These fields do not grant Application identity.

20.9. These fields do not grant ApplicationRun identity.

20.10. These fields do not grant Workspace ownership.

20.11. These fields do not grant SaveBundle ownership.

20.12. These fields do not grant committed-state mutation authority.

20.13. Paths, endpoint summaries, user identifiers, principal identifiers, bind
       addresses, and pairing identifiers are security-sensitive trace
       material.

20.14. Exact redaction rules remain outside this document unless a later
       security or serialization specification defines them.

20.15. Redaction does not authorize fabrication of replacement evidence.

20.16. When redaction affects interpretation, the trace record preserves that
       limitation when the concrete trace schema supports doing so.


===============================================================================
21. EXTENSION FIELDS
===============================================================================

21.1. RuntimeHost-domain trace records may include extension fields when a
      later implementation specification defines them.

21.2. Extension fields do not redefine fields defined by this document.

21.3. Extension fields do not redefine DA-28 trace reason meanings.

21.4. Extension fields do not redefine DA-03 trace substrate semantics.

21.5. Extension field names are namespaced or otherwise made distinguishable
      from base IS-07 fields.

21.6. Implementation-specific extension fields are not required for portable
      RuntimeHost trace interpretation unless a later specification promotes
      them into this document.

21.7. Extension fields do not create compatibility loopholes around field
      meanings defined by this document.


===============================================================================
22. RELATION TO IMPLEMENTATION SPECIFICATIONS
===============================================================================

22.1. IS-03 uses IS-07 ownership and Workspace fields when emitting Workspace
      ownership trace evidence.

22.2. IS-04 uses IS-07 command and RuntimeHost operation fields when emitting
      command-surface trace evidence.

22.3. IS-05 uses IS-07 startup-mode fields when emitting startup-mode trace
      evidence.

22.4. IS-06 uses IS-07 entry-surface, client-connection, LAN exposure,
      transport, and channel fields when emitting LAN and client trace
      evidence.

22.5. RuntimeHost implementation specifications use IS-07 field names unless
      they define and justify a compatible extension.

22.6. Trace-store implementation specifications may define concrete
      serialization, storage, indexing, and query behavior while preserving
      the field meanings defined here.

22.7. Security and redaction specifications may define how sensitive fields
      are filtered, summarized, omitted, or marked.

22.8. Such filtering does not change the field meanings defined here.


===============================================================================
23. WHAT IS EXPLICITLY NOT SUPPORTED
===============================================================================

23.1. The following are not supported:
      - using IS-07 to redefine DA-03 trace substrate semantics
      - using IS-07 to redefine DA-28 RuntimeHost trace reason meanings
      - using RuntimeHost trace fields as authority grants
      - treating cross-domain correlation fields as ownership transfer
      - treating commandStatus as Job terminal outcome
      - treating startupStatus as commandStatus replacement by default
      - treating runtimeOperationStatus as commandStatus replacement by default
      - treating ownershipMode as read-only inspection mode
      - treating accessMode as writable ownership
      - treating ownerRuntimeHostId as ownerRuntimeId replacement by default
      - treating bindScope as command authority
      - treating clientConnectionId as user identity
      - treating authenticatedPrincipalId as capability permission
      - treating frontendClientId as Application or ApplicationRun identity
      - treating saveBundleId as persistence authority
      - treating activeAppPackId as universal AppDefinition identity
      - treating transport fields as RPC semantic ownership
      - extension fields redefining IS-07 base fields
      - implementation-specific trace extensions replacing domain-owned trace
        evidence
      - trace redaction fabricating evidence that was not observed


===============================================================================
24. EXPLICITLY OPEN QUESTIONS
===============================================================================

24.1. OPEN questions include:
      - exact traceRecordId representation
      - exact schemaVersion value format
      - exact traceLevel vocabulary
      - exact runtimeHostId representation
      - exact runtimeId representation
      - exact workspaceId representation
      - exact commandId representation
      - exact startupRequestId representation
      - exact runtimeOperationId representation
      - exact entrySurfaceId representation
      - exact clientConnectionId representation
      - exact applicationId representation
      - exact applicationRunId representation
      - exact appDefinitionSourceId representation
      - exact activeAppPackId representation
      - exact saveBundleId representation
      - exact ownerRuntimeId representation
      - exact ownerToken and ownerNonce relationship
      - exact accessMode vocabulary
      - exact ownershipStatus vocabulary
      - exact commandStatus serialization
      - exact startupStatus serialization
      - exact runtimeOperationStatus serialization
      - exact clientConnectionStatus vocabulary
      - exact exposureStatus vocabulary
      - exact extension field namespace convention
      - exact JSON schema publication format
      - exact trace-store serialization format
      - exact redaction rules for paths, endpoints, user ids, principals,
        pairing identifiers, and bind address summaries


===============================================================================
25. LOCKED SUMMARY
===============================================================================

25.1. LOCKED:
      - IS-07 defines RuntimeHost-domain trace field vocabulary.
      - IS-07 does not define trace substrate semantics.
      - DA-03 owns trace substrate, trace DAG, and span semantics.
      - DA-28 owns RuntimeHost-domain trace reason identifiers and their
        semantic meaning.
      - RuntimeHost trace records use sourceDomain RuntimeHost when
        source-domain projection is emitted.
      - RuntimeHost-layer field names do not redefine DA-03 canonical
        substrate field meanings.
      - runtimeHostId is distinct from runtimeId, workspaceId, commandId,
        startupRequestId, runtimeOperationId, clientConnectionId,
        applicationId, applicationRunId, saveBundleId, viewId, adapter
        identity, RPC message identity, transactionId, and decisionId.
      - ownerRuntimeHostId is not a substitute for ownerRuntimeId when both
        identities are defined.
      - read-only inspection uses accessMode rather than ownershipMode.
      - commandStatus, startupStatus, and runtimeOperationStatus remain
        distinct.
      - status and outcome fields do not redefine owning-domain outcomes.
      - cross-domain correlation fields do not transfer authority or semantic
        ownership into RuntimeHost trace evidence.
      - cross-domain correlation fields do not replace trace evidence owned by
        the referenced domain.
      - transport and channel fields are correlation fields and do not redefine
        DA-21 semantics.
      - identity-looking, client, principal, endpoint, path, bind address, and
        pairing fields are trace evidence only.
      - extension fields do not redefine IS-07 base fields, DA-28 trace reason
        meanings, DA-03 trace substrate semantics, or portable RuntimeHost
        trace interpretation.
      - IS-03, IS-04, IS-05, and IS-06 use IS-07 field names for RuntimeHost-
        domain trace evidence.

25.2. OPEN:
      - concrete identifier representations, status vocabularies, accessMode
        vocabulary, owner token/nonce relationship, extension namespace
        convention, JSON schema publication, trace-store serialization, and
        redaction rules.


===============================================================================
END OF IS-07 - RUNTIME HOST TRACE SCHEMA
===============================================================================

--- doc-meta ---
DOC_ID: IS-07
DOC_FILE: docs/design/IS-07_RUNTIME_HOST_TRACE_SCHEMA.txt
DOC_REV: 2
DOC_GIT: e747e09
----------------
