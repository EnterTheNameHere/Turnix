{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://turnix.local/schemas/asi/terminology-inference.schema.json",
  "title": "ASI Terminology Inference",
  "description": "Schema for terminology proposals produced by TerminologyInferrer workflows.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "recordType",
    "candidateId",
    "docId",
    "batchId",
    "proposedTerm",
    "normalizedKey",
    "candidateType",
    "sourcePresence",
    "whyItMatters",
    "riskIfAcceptedIncorrectly",
    "recommendedAction",
    "sourceHints",
    "quality"
  ],
  "properties": {
    "schemaVersion": {
      "const": 1
    },
    "recordType": {
      "const": "terminology_inference"
    },
    "candidateId": {
      "type": "string",
      "minLength": 1,
      "description": "Stable ID for this terminology inference. Suggested form: ti:<DOC_ID>:<normalizedKey>."
    },
    "docId": {
      "type": "string",
      "minLength": 1
    },
    "batchId": {
      "type": "string",
      "minLength": 1
    },
    "proposedTerm": {
      "type": "string",
      "minLength": 1
    },
    "normalizedKey": {
      "type": "string",
      "minLength": 1
    },
    "candidateType": {
      "description": "Type of terminology proposal. TerminologyInferrer proposes; supervisor or later approved workflows decide acceptance.",
      "oneOf": [
        {
          "const": "possible_source_term",
          "description": "Term may be worth adding to the canonical source because the source strongly implies the concept but lacks compact stable wording."
        },
        {
          "const": "asi_alias_only",
          "description": "Term is useful for ASI search/routing but should not become canonical source terminology by default."
        },
        {
          "const": "possible_boundary_phrase",
          "description": "Phrase may be useful as a stable X-is-not-Y boundary wording."
        },
        {
          "const": "possible_relation_name",
          "description": "Phrase may be useful as a stable name for a relation between documents, terms, concepts, or authority areas."
        },
        {
          "const": "possible_source_heading",
          "description": "Proposed heading or label to add to source only after approval; it is not an invented ASI source locator."
        },
        {
          "const": "reject_or_replace",
          "description": "Term appears misleading, unsupported, too implementation-specific, or should be replaced by better wording."
        }
      ]
    },
    "sourcePresence": {
      "description": "How the proposed term appears or does not appear in the canonical source.",
      "oneOf": [
        {
          "const": "literal",
          "description": "The proposed term appears literally in the source."
        },
        {
          "const": "variant_spelling",
          "description": "The source uses a close spelling or wording variant."
        },
        {
          "const": "not_literal_but_strongly_implied",
          "description": "The exact term is absent, but the source strongly defines or implies the concept."
        },
        {
          "const": "not_present_weakly_inferred",
          "description": "The exact term is absent and support is weak or speculative."
        },
        {
          "const": "unsupported",
          "description": "The term is not supported by the source and should normally be rejected or replaced."
        }
      ]
    },
    "whyItMatters": {
      "type": "string",
      "minLength": 1
    },
    "riskIfAcceptedIncorrectly": {
      "type": "string",
      "minLength": 1
    },
    "recommendedAction": {
      "description": "Recommended next workflow action. This is not final acceptance unless a supervisor decision records it elsewhere.",
      "oneOf": [
        {
          "const": "supervisor_review",
          "description": "Send to supervisor or user for acceptance, rejection, or refinement."
        },
        {
          "const": "accept_as_asi_alias",
          "description": "Recommended as ASI-only alias if accepted; does not imply source terminology."
        },
        {
          "const": "consider_source_patch",
          "description": "May deserve source wording change, but this is not source-patch approval."
        },
        {
          "const": "reject",
          "description": "Recommended for rejection."
        },
        {
          "const": "defer",
          "description": "Postpone decision until more source, audit, or cross-document context exists."
        }
      ]
    },
    "suggestedIndexUse": {
      "description": "Possible index use if accepted by the appropriate decision workflow.",
      "oneOf": [
        {
          "const": "none",
          "description": "No suggested index use."
        },
        {
          "const": "alias",
          "description": "Use as ASI alias or search synonym."
        },
        {
          "const": "term_literal",
          "description": "Use as literal term lookup card."
        },
        {
          "const": "term_context",
          "description": "Use as context-specific term meaning card."
        },
        {
          "const": "boundary",
          "description": "Use as boundary card."
        },
        {
          "const": "relation",
          "description": "Use as relation card."
        }
      ],
      "default": "none"
    },
    "sourceHints": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/sourceHint"
      }
    },
    "relatedItemIds": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "relatedDecisionIds": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "aliases": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "searchText": {
      "type": "string"
    },
    "quality": {
      "$ref": "#/$defs/quality"
    }
  },
  "$defs": {
    "sourceHint": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "sourceDocId"
      ],
      "properties": {
        "sourceDocId": {
          "type": "string",
          "minLength": 1
        },
        "sourceFile": {
          "type": "string"
        },
        "sectionHint": {
          "type": "string",
          "description": "CMR-native section, rule, paragraph, or range reference when available. Do not use this as an invented ASI anchor."
        },
        "lineHint": {
          "type": "string"
        },
        "basis": {
          "type": "string"
        }
      }
    },
    "confidence": {
      "oneOf": [
        {
          "const": "high",
          "description": "Strongly supported by source or accepted workflow evidence."
        },
        {
          "const": "medium",
          "description": "Reasonably supported but may need later review or cross-document confirmation."
        },
        {
          "const": "low",
          "description": "Weak or tentative support; use cautiously."
        },
        {
          "const": "needs-review",
          "description": "Requires audit, supervisor review, or stronger source evidence before relying on it."
        }
      ]
    },
    "reviewState": {
      "oneOf": [
        {
          "const": "generated",
          "description": "Generated by a workflow and not yet reviewed."
        },
        {
          "const": "reviewed",
          "description": "Reviewed, but not necessarily accepted as final or authoritative routing metadata."
        },
        {
          "const": "accepted",
          "description": "Accepted for its intended ASI workflow purpose."
        },
        {
          "const": "rejected",
          "description": "Rejected for its intended ASI workflow purpose."
        },
        {
          "const": "superseded",
          "description": "Replaced by a newer or better record."
        },
        {
          "const": "needs-supervisor-decision",
          "description": "Requires supervisor or user decision before downstream use."
        }
      ]
    },
    "quality": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "confidence",
        "reviewState"
      ],
      "properties": {
        "confidence": {
          "$ref": "#/$defs/confidence"
        },
        "reviewState": {
          "$ref": "#/$defs/reviewState"
        },
        "notes": {
          "type": "string"
        }
      }
    }
  }
}
