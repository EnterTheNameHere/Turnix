{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://turnix.local/schemas/asi/extracted-item.schema.json",
  "title": "ASI Extracted Item",
  "description": "Schema for one assistant semantic index extracted item produced from one canonical source document.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "recordType",
    "itemId",
    "docId",
    "batchId",
    "kind",
    "key",
    "normalizedKey",
    "summary",
    "sourceHints",
    "searchText",
    "quality"
  ],
  "properties": {
    "schemaVersion": {
      "const": 1
    },
    "recordType": {
      "const": "extracted_item"
    },
    "itemId": {
      "type": "string",
      "minLength": 1,
      "description": "Stable ID for this extracted item. Suggested form: extract:<DOC_ID>:<kind>:<normalizedKey>:<occurrenceNumber>. occurrenceNumber is a three-digit local sequence number for multiple extracted items with the same DOC_ID, kind, and normalizedKey."
    },
    "docId": {
      "type": "string",
      "minLength": 1
    },
    "batchId": {
      "type": "string",
      "minLength": 1
    },
    "kind": {
      "description": "Semantic category of the extracted item. Source locations must use CMR-native sourceHints rather than invented section anchors.",
      "oneOf": [
        {
          "const": "document_domain",
          "description": "Document-level domain, ownership area, or non-ownership area. Use for what a document owns, primarily covers, or explicitly does not own."
        },
        {
          "const": "term",
          "description": "Literal term, field name, phrase, named concept, or vocabulary item that may need routing. Same literal term across documents does not automatically mean same semantic meaning."
        },
        {
          "const": "boundary",
          "description": "Distinction or X-is-not-Y rule, such as stateUpdate is not committed state mutation or acknowledgement is not Job success."
        },
        {
          "const": "relation",
          "description": "Meaningful relation between documents, terms, concepts, or authority areas, including support, dependency, load-with, neighboring authority, or non-equivalence."
        },
        {
          "const": "alias",
          "description": "Search or retrieval synonym. Alias does not mean source-defined term unless the canonical source explicitly defines or uses it."
        },
        {
          "const": "source_clarification_observation",
          "description": "Observation that the source document may be ambiguous, under-named, weakly worded, missing a useful boundary, or worth improving. This is not source patch approval."
        },
        {
          "const": "implementation_hint",
          "description": "Hint that the item affects implementation, schemas, code, command surfaces, adapters, or runtime behavior. This does not become implementation authority by itself."
        },
        {
          "const": "other",
          "description": "Fallback for useful extracted material that does not fit current categories. Use sparingly; frequent use means the schema likely needs another kind."
        }
      ]
    },
    "key": {
      "type": "string",
      "minLength": 1,
      "description": "Human-facing key, literal term, boundary phrase, or relation name."
    },
    "normalizedKey": {
      "type": "string",
      "minLength": 1,
      "description": "Deterministic normalized form used for matching and sorting."
    },
    "context": {
      "type": "string",
      "description": "Optional local context explaining where this item applies."
    },
    "summary": {
      "type": "string",
      "minLength": 1
    },
    "sourceHints": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/sourceHint"
      }
    },
    "routingHints": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "loadFirst": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/loadHint"
          }
        },
        "candidateLoad": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/loadHint"
          }
        },
        "loadIfNeeded": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/loadHint"
          }
        },
        "doNotTreatAsAuthority": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/nonAuthorityHint"
          }
        }
      }
    },
    "aliases": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "confusableWith": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "searchText": {
      "type": "string",
      "minLength": 1,
      "description": "Search-oriented text. It may repeat terms and aliases intentionally."
    },
    "sourceClarificationHint": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "suggested",
        "reason"
      ],
      "properties": {
        "suggested": {
          "type": "boolean"
        },
        "reason": {
          "type": "string"
        }
      }
    },
    "quality": {
      "$ref": "#/$defs/quality"
    }
  },
  "$defs": {
    "sourceHint": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "sourceDocId"
      ],
      "properties": {
        "sourceDocId": {
          "type": "string",
          "minLength": 1
        },
        "sourceFile": {
          "type": "string"
        },
        "sectionHint": {
          "type": "string",
          "description": "CMR-native section, rule, paragraph, or range reference when available. Do not use this as an invented ASI anchor."
        },
        "lineHint": {
          "type": "string"
        },
        "basis": {
          "type": "string"
        }
      }
    },
    "loadHint": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "docId",
        "role"
      ],
      "properties": {
        "docId": {
          "type": "string",
          "minLength": 1
        },
        "role": {
          "type": "string",
          "minLength": 1
        },
        "basis": {
          "type": "string"
        }
      }
    },
    "nonAuthorityHint": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "docId",
        "reason"
      ],
      "properties": {
        "docId": {
          "type": "string",
          "minLength": 1
        },
        "reason": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "confidence": {
      "oneOf": [
        {
          "const": "high",
          "description": "Strongly supported by source or accepted workflow evidence."
        },
        {
          "const": "medium",
          "description": "Reasonably supported but may need later review or cross-document confirmation."
        },
        {
          "const": "low",
          "description": "Weak or tentative support; use cautiously."
        },
        {
          "const": "needs-review",
          "description": "Requires audit, supervisor review, or stronger source evidence before relying on it."
        }
      ]
    },
    "reviewState": {
      "oneOf": [
        {
          "const": "generated",
          "description": "Generated by a workflow and not yet reviewed."
        },
        {
          "const": "reviewed",
          "description": "Reviewed, but not necessarily accepted as final or authoritative routing metadata."
        },
        {
          "const": "accepted",
          "description": "Accepted for its intended ASI workflow purpose."
        },
        {
          "const": "rejected",
          "description": "Rejected for its intended ASI workflow purpose."
        },
        {
          "const": "superseded",
          "description": "Replaced by a newer or better record."
        },
        {
          "const": "needs-supervisor-decision",
          "description": "Requires supervisor or user decision before downstream use."
        }
      ]
    },
    "quality": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "confidence",
        "reviewState"
      ],
      "properties": {
        "confidence": {
          "$ref": "#/$defs/confidence"
        },
        "reviewState": {
          "$ref": "#/$defs/reviewState"
        },
        "notes": {
          "type": "string"
        }
      }
    }
  }
}
